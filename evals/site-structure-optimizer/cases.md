Simulated seed cases for the merged site-structure-optimizer: architecture-mode hierarchy/URL/hub-spoke, linking-mode orphan/anchor/link-graph, mode inference, the R08/R10 gate-handoff boundary, and a NEEDS_INPUT hard stop.

```yaml
{id: site-structure-optimizer-sim-001, type: eval-case, status: simulated, target_skill: site-structure-optimizer, scenario: "Architecture mode — new SaaS marketing site, plan the whole layout.", input_summary: "Site type = B2B SaaS marketing; 5 key pages; new build; no existing URLs.", expected_behavior: ["State mode: architecture and the chosen site-type model + target depth.", "Output ASCII hierarchy, URL map table, nav spec, hub/spoke plan, and a Mermaid site map with orphans/islands subgraphs.", "Compute an architecture score /100 and emit the handoff summary."], failure_modes: ["Runs linking-mode steps instead.", "Invents traffic/URL metrics as Measured.", "Computes a CORE-EEAT score or runs vetoes itself."]}
```
```yaml
{id: site-structure-optimizer-sim-002, type: eval-case, status: simulated, target_skill: site-structure-optimizer, scenario: "Linking mode — find orphans and fix anchor text on an existing blog.", input_summary: "Sitemap + page list provided; crawl helper available; prospecting an internal-link plan.", expected_behavior: ["State mode: linking and compute a structure score /100 with the stated deductions.", "List orphan pages with disposition and check anchor distribution against R08 (descriptive anchors).", "Deliver a prioritized source/target/anchor plan and handoff summary."], failure_modes: ["Designs a new hierarchy instead of optimizing existing links.", "Labels Estimated click-depth as Measured with no crawl data.", "Silently deletes a high-value orphan without asking."]}
```
```yaml
{id: site-structure-optimizer-sim-003, type: eval-case, status: simulated, target_skill: site-structure-optimizer, scenario: "Spanning request — restructure AND fix links; verify mode ordering + termination.", input_summary: "User asks to reorganize a buried site and also improve internal links; no --mode flag.", expected_behavior: ["Run architecture first, then hand off to linking via Next Best Skill (do not interleave).", "On the second pass, apply the visited-set rule so the chain stops after linking.", "Keep architecture and linking outputs distinct, each with its own score."], failure_modes: ["Interleaves both modes in one pass.", "Recurses architecture→linking→architecture without a visited-set stop.", "Merges the two scores into one number."]}
```
```yaml
{id: site-structure-optimizer-sim-004, type: eval-case, status: simulated, target_skill: site-structure-optimizer, scenario: "Gate boundary — URL migration without redirects and a broken link target.", input_summary: "Restructure changes several URLs with no 301 plan; one proposed contextual target returns 404.", expected_behavior: ["Flag the no-redirect migration and the 404 target as blocking defects and promote to open-loops as pending-decision.", "Hand off veto-level risk (R10 consistency / broken links, T04-adjacent) to content-quality-auditor without writing a hot-cache marker.", "Do not emit a SHIP/FIX/BLOCK verdict or a CORE-EEAT score itself."], failure_modes: ["Writes a veto marker to hot-cache directly.", "Produces a CORE-EEAT gate verdict.", "Ignores the broken target and finalizes the plan."]}
```
```yaml
{id: site-structure-optimizer-sim-005, type: eval-case, status: simulated, target_skill: site-structure-optimizer, scenario: "NEEDS_INPUT hard stop — architecture mode with no site type and no inventory.", input_summary: "User says 'design my site structure' with no site type, no page list, and no crawlable domain.", expected_behavior: ["Set status: NEEDS_INPUT and stop at the Confirm Scope gate.", "Present numbered options (name type + page list / give a domain to crawl / proceed on a stated assumed type).", "Do not fabricate a hierarchy or an architecture score."], failure_modes: ["Guesses a site type silently and proceeds.", "Emits DONE with an invented page inventory.", "Skips the decision gate entirely."]}
```
