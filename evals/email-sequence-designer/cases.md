Simulated seed cases for email-sequence-designer: core lifecycle flow design, per-flow trigger/timing/branch-exit logic, frequency governance + sunset path, the SEND N-dimension score, the NEEDS_INPUT unknown-trigger gate, and the scope-guard boundary between the over-frequency E-guardrail and the N1 unsubscribe veto (which only the auditor enforces).

```yaml
{id: email-sequence-designer-sim-001, type: eval-case, status: simulated, target_skill: email-sequence-designer, scenario: "Ecommerce program missing core flows; design the lifecycle set.", input_summary: "ESP flow export shows only a one-off newsletter; retention profile; signup, cart, and post-purchase events all fire in GA4/ecommerce.", expected_behavior: ["Inventory applicable N flows and flag welcome, abandoned-cart, post-purchase, and win-back as missing.", "Design each with trigger, per-step timing, goal, and branch/exit conditions.", "Emit SEND-N evidence under the retention profile (N=0.30) and hand off to email-creative-builder."], failure_modes: ["Writes email copy instead of flow structure.", "Computes an EQS or fires the N1 veto.", "Omits branch/exit conditions or the typed profile."]}
```
```yaml
{id: email-sequence-designer-sim-002, type: eval-case, status: simulated, target_skill: email-sequence-designer, scenario: "Abandoned-cart flow with discount branch and exit-on-purchase.", input_summary: "User wants a 3-step cart sequence with a discount on step 3 and a hard exit when the order completes; promotional/DR goal.", expected_behavior: ["Specify trigger (cart event), step delays, a click/no-click branch to the discount, and exit-on-conversion + unsubscribe/bounce hard stops.", "Name the Promotional/DR N weight (0.15) and score the timing/cadence and goal-progression sub-items.", "Recommend email-creative-builder to fill each step, not write the copy here."], failure_modes: ["No exit-on-purchase condition, so converted buyers keep receiving the discount.", "Adds the discount claim itself instead of deferring claim wording to the creative builder / D1.", "Treats the flow as promotional-blast copy rather than automation structure."]}
```
```yaml
{id: email-sequence-designer-sim-003, type: eval-case, status: simulated, target_skill: email-sequence-designer, scenario: "Rising complaints on an unengaged segment; design win-back + sunset with governance.", input_summary: "40% of the list is unengaged and spam complaints are climbing; user asks for a re-engagement path plus send caps and quiet hours.", expected_behavior: ["Design a win-back attempt then a suppression/sunset rule on a defined no-open window (engagement-decay sub-item).", "Write a global send cap, quiet hours, and a fatigue guardrail, labeling over-frequency a high-severity SEND-E guardrail, not a veto.", "Score N and hand off to email-quality-auditor to enforce N1 and run the vetoes."], failure_modes: ["Scores over-frequency as an N1 veto fail.", "Sunsets subjects by editing suppression records directly instead of deferring to consent-registry.", "Omits the sunset stop rule so decayed subjects are mailed forever."]}
```
```yaml
{id: email-sequence-designer-sim-004, type: eval-case, status: simulated, target_skill: email-sequence-designer, scenario: "NEEDS_INPUT — trigger event and target segment are unknowable.", input_summary: "User says 'set up a cart flow' but there is no cart-tracking event, no ESP export, and no defined segment.", expected_behavior: ["Stop at the Decision Gate and return NEEDS_INPUT naming the missing trigger source and segment.", "Present numbered options (which trigger source, which segment) with their outcomes instead of guessing an enrollment rule.", "Do not fabricate a cart event or auto-enroll the whole list."], failure_modes: ["Invents a cart trigger and designs the flow anyway.", "Enrolls the entire list with no segment.", "Emits DONE despite the blocking unknowns."]}
```
```yaml
{id: email-sequence-designer-sim-005, type: eval-case, status: simulated, target_skill: email-sequence-designer, scenario: "B2B cold-outbound sequence; scope-guard boundary on unsubscribe.", input_summary: "User wants a 5-step cold-outbound sequence with exit-on-reply; cold-outbound/acquisition goal; asks whether the flow passes the opt-out requirement.", expected_behavior: ["Design the multi-step sequence with reply/no-reply branches, exit-on-reply, and per-step timing; name the Cold-outbound N weight (0.15).", "Design the one-click opt-out into every step but state that adjudicating N1 (absent/broken unsubscribe) is the auditor's job, not this skill's.", "Route to email-quality-auditor for the N1 verdict and EQS; do not declare an N1 pass/fail here."], failure_modes: ["Declares an N1 pass or fail verdict itself.", "Omits exit-on-reply so replied prospects keep getting steps.", "Skips designing the opt-out into the outbound steps."]}
```
