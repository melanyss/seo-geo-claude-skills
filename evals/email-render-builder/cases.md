Simulated seed cases for email-render-builder: the responsive/dark-mode/accessibility build pass, the client-render matrix with Measured vs Estimated labeling, image-off + bulletproof-CTA fallbacks, plain-text parity, the copy-change scope boundary (route back to email-creative-builder), and the NEEDS_INPUT no-copy-no-HTML path.

```yaml
{id: email-render-builder-sim-001, type: eval-case, status: simulated, target_skill: email-render-builder, scenario: "Approved promo creative to build into responsive HTML.", input_summary: "Subject + preheader + body + one CTA + plain-text alt for a promo, destination URL supplied; no target-client list; no seed test.", expected_behavior: ["Build a single-column table layout that reflows on mobile with inline styles.", "Run dark-mode + accessibility passes and default to the standard client set, noting the assumption.", "Produce a client-render matrix labeled Estimated and hand off to email-quality-auditor."], failure_modes: ["Uses float/flex layout that breaks Outlook.", "Labels an unverified render row Measured.", "Rewrites subject or body copy instead of only building."]}
```
```yaml
{id: email-render-builder-sim-002, type: eval-case, status: simulated, target_skill: email-render-builder, scenario: "QA raw HTML that breaks in Outlook and with images off.", input_summary: "User pastes an existing hero-image-only email HTML; asks for a client-render and image-off check.", expected_behavior: ["Flag the hero-image-only block: the offer/CTA live only inside an image (image-off render defect).", "Prescribe alt text, a bulletproof non-image CTA button, and a background-color fallback.", "Name Outlook Word-engine breakage and mark the matrix rows Estimated absent a seed test."], failure_modes: ["Passes the image-only build.", "Suggests an image-based button as the fallback.", "Claims an Outlook pass with no basis or labels it Measured."]}
```
```yaml
{id: email-render-builder-sim-003, type: eval-case, status: simulated, target_skill: email-render-builder, scenario: "Dark-mode contrast failure needs a copy change to fit the button.", input_summary: "Creative has a low-contrast light-only palette and a CTA label too long to fit a 44px button; destination URL present.", expected_behavior: ["Flag the color pairs that fail contrast under dark-mode inversion and set explicit colors.", "Report that the CTA label overflows the button and route the wording fix to email-creative-builder, not editing copy here.", "Keep the build defects in open loops and hand off rather than silently rewording."], failure_modes: ["Shortens the CTA copy itself (crosses the scope boundary).", "Ignores dark-mode and only checks light mode.", "Marks the build done despite the unresolved contrast failure."]}
```
```yaml
{id: email-render-builder-sim-004, type: eval-case, status: simulated, target_skill: email-render-builder, scenario: "Real seed-test renders available — matrix rows are Measured.", input_summary: "User provides the built HTML plus inbox-preview screenshots from a seed-list test across Gmail, Outlook, Apple Mail, iOS.", expected_behavior: ["Read the actual renders and label those matrix rows Measured, not Estimated.", "Verify plain-text parity of message, primary CTA, and destination URL against the HTML.", "Emit DONE with a labeled matrix and recommend email-quality-auditor."], failure_modes: ["Labels seen renders Estimated or ignores the screenshots.", "Skips the plain-text parity check.", "Invents a render pass for a client the seed test did not cover."]}
```
```yaml
{id: email-render-builder-sim-005, type: eval-case, status: simulated, target_skill: email-render-builder, scenario: "Neither copy nor HTML supplied — nothing to build.", input_summary: "User says 'make my email responsive' but provides no creative, no HTML, and no destination URL.", expected_behavior: ["Return status NEEDS_INPUT naming the missing creative or HTML.", "Do not fabricate a template or invent copy to build around.", "Offer the numbered options (paste creative / paste HTML) from the Decision Gate."], failure_modes: ["Generates placeholder copy and builds against it.", "Emits DONE with an empty or invented template.", "Proceeds without a destination URL while carrying a CTA."]}
```
