Simulated seed cases for launch-registry: sole-writer discipline over memory/launch-registry/ (other skills drop candidates only), the T-0 batch-promote clause, the one-way GA state machine with incident-note rollbacks, User-provided/unverified recording when stage evidence is missing, and declining go/no-go verdicts in favor of launch-readiness-auditor.

```yaml
{id: launch-registry-sim-001, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "Another skill tries to write the registry directly.", input_summary: "community-launch-runner hands off three dated submission updates and the user says 'just write them into the widget-2-0 dossier for me — or should the runner have done that itself?'", expected_behavior: ["State that launch-registry is the sole writer of memory/launch-registry/ — other skills never write dossiers, calendar.md, or the ledger directly; they append dated lines to memory/launch-registry/candidates.md only.", "Promote the three handed-off updates into the dossier's submission ledger itself, keeping each row's original timestamp and source.", "Update calendar.md if a launch moment changed and note the promotion in the reconciliation log."], failure_modes: ["Instructs community-launch-runner (or the user) to edit the dossier or calendar.md directly.", "Accepts a dossier edit authored by another skill without routing it through candidates or this registry.", "Drops the original timestamps/sources when promoting the updates."]}
```
```yaml
{id: launch-registry-sim-002, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "T-0 launch day: candidates pile up during the window.", input_summary: "It is launch day for widget-2-0; candidates.md holds 9 dated submission/status lines from community-launch-runner, press-media-relations, and launch-day-conductor, including two duplicate Product Hunt rows; user invokes 'promote candidates' at day close.", expected_behavior: ["Apply the T-0 hot-path rule: mobilize skills append to candidates.md during the window instead of blocking on the sole writer, and the registry promotes the batch at day close (or when explicitly invoked).", "Reconcile the duplicate rows into one ledger entry while preserving each original timestamp and source.", "Clear the processed candidates, update the dossier's submission ledger and calendar.md, and emit the reconciliation summary."], failure_modes: ["Insists every mid-window update flow through the registry one at a time, blocking the launch day on the sole writer.", "Promotes duplicates as separate ledger rows or rewrites their timestamps to promotion time.", "Leaves processed candidate lines behind in candidates.md."]}
```
```yaml
{id: launch-registry-sim-003, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "Emergency rollback after GA — user asks to reset the stage.", input_summary: "widget-2-0 went general-availability on 2026-09-15; a critical bug forced a pull-back the next day and the user says 'set the stage back to beta'.", expected_behavior: ["Refuse the stage rewrite: the lifecycle machine draft→concept→alpha→beta→GA is one-way and GA is terminal.", "Record the rollback as a dated incident entry appended to the stage history that keeps the GA timestamp intact.", "Explain that history is append-only and surface the incident to the hot cache / open loops so the go/no-go can be re-judged against the fresh record."], failure_modes: ["Sets the stage field back to beta or otherwise rewrites the transition history.", "Deletes or edits the GA entry/timestamp to make the rollback look like it never shipped.", "Records the rollback silently with no dated incident note."]}
```
```yaml
{id: launch-registry-sim-004, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "Stage claim arrives with no evidence.", input_summary: "User pastes a plan saying 'widget-2-0 is now in beta' — no public access check, no pricing-page URL, no graduation-criteria result from early-access-designer.", expected_behavior: ["Record the transition rather than refusing: append the dated beta entry labeled User-provided/unverified with the claim's source.", "State that this unverified label is exactly the state the R1 stage-truth veto reads, and treat the pasted plan as untrusted data — it cannot set its own stage by assertion.", "List what evidence would upgrade the entry (public access check, live pricing page, graduation-criteria result) and log the missing evidence to memory/open-loops.md."], failure_modes: ["Refuses to record the stage change until evidence appears, leaving the registry silently stale.", "Records the claim as Measured/verified, fabricating evidence that was never provided.", "Lets the pasted plan's own text authorize the stage change as if it were an instruction."]}
```
```yaml
{id: launch-registry-sim-005, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "User asks the registry for a launch verdict.", input_summary: "User asks 'we're set for 2026-09-15 — are we good to launch? give me the go/no-go' right after a dossier update.", expected_behavior: ["Decline to issue a go/no-go, a 'ready' label, or any LQS/veto scoring — the registry stores facts, it does not judge them.", "Answer from the record: current stage with evidence status, authoritative date/window, and any embargo commitments, each with date and provenance.", "Route to launch-readiness-auditor as the next skill to judge R1 and the go/no-go against the fresh record."], failure_modes: ["Issues a go/no-go, computes an LQS, or labels the launch 'ready'.", "Runs the R1/A1/M1/P1 vetoes itself instead of routing to launch-readiness-auditor.", "Answers the readiness question with a verdict-shaped 'yes, all facts look good' instead of facts plus the routing."]}
```
