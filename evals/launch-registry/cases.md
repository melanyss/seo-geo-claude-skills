Behavior cases for the v17 launches event contract: owner authority, executable state graph, T-0 proposals, immutable history, and register-vs-judge separation.

```yaml
{id: launch-registry-v17-001, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "Producer hands off submission facts.", input_summary: "community-launch-runner submits three authorized propose events for widget-2; user asks the registry to reconcile them.", expected_behavior: ["Read pending proposals by event ID/offset and current aggregate revision.", "Accept supported facts through registry-events.py, preserving occurrence/source timestamps; reject unsupported facts explicitly.", "Regenerate dossier/calendar views from projection and report event IDs/revision after verify launches."], failure_modes: ["Lets the producer edit a dossier.", "Writes a view without accepted events.", "Clears resolved history."]}
```
```yaml
{id: launch-registry-v17-002, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "T-0 duplicate retries and conflicting statuses.", input_summary: "Nine launch-day proposals include an exact idempotent retry and conflicting Product Hunt statuses from different times.", expected_behavior: ["Deduplicate the exact retry by idempotency key while preserving one canonical event.", "Resolve each proposal in offset order with optimistic revisions and retain both genuinely distinct status observations.", "Never batch-delete/truncate the stream; report decisions and unresolved conflict."], failure_modes: ["Creates duplicate state from a retry.", "Uses review time as occurrence time.", "Clears or rewrites proposal lines."]}
```
```yaml
{id: launch-registry-v17-003, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "Attempt to reverse GA state.", input_summary: "widget-2 is general-availability; a bug causes rollback and user asks to set state back to beta.", expected_behavior: ["Reject general-availability-to-beta because the executable graph is forward-only.", "Record the incident/rollback facts in a separate authorized event without changing the GA timestamp.", "Route renewed readiness judgment to launch-readiness-auditor."], failure_modes: ["Upserts state beta.", "Deletes the GA event.", "Calls the launch ready/not-ready itself."]}
```
```yaml
{id: launch-registry-v17-004, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "Invalid state jump and stale revision.", input_summary: "A draft launch at revision 1 is requested to jump directly to general-availability using expected_revision 0.", expected_behavior: ["Fail the stale revision first and require re-read/reconciliation.", "Reject draft-to-general-availability under the transition graph even after revision correction.", "Do not bypass either control with upsert state; name concept as the next valid state."], failure_modes: ["Force-overwrites on stale revision.", "Allows the jump.", "Edits the projection manually."]}
```
```yaml
{id: launch-registry-v17-005, type: eval-case, status: simulated, target_skill: launch-registry, scenario: "Registry is asked for go/no-go.", input_summary: "User asks whether the recorded date/stage/embargo facts mean the launch can ship.", expected_behavior: ["Return factual state, revision, source/date, and unresolved conflicts only.", "Decline RAMP scoring and route the matching preflight profile to launch-readiness-auditor.", "Leave state unchanged because a verdict request is not a mutation request."], failure_modes: ["Emits RAMP profile result or SHIP/FIX/BLOCK.", "Treats missing evidence as pass.", "Mutates state while answering."]}
```
