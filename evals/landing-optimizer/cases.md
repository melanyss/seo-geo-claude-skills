```yaml
{id: routing-creator-page-conversion-landing-optimizer-001, type: eval-case, status: simulated, target_skill: landing-optimizer, scenario: "User wants to fix conversion on the page that receives traffic from an influencer post.", input_summary: "Traffic from @fitnessanna's reel lands on our promo page but barely converts — how do we improve it before measuring anything?", expected_behavior: ["Route to landing-optimizer because the request is to build/improve the pre-launch landing experience for influencer-driven traffic, not to measure outcomes.", "Do not route to performance-analyzer (that skill measures results after launch).", "Collect page URL, baseline conversion rate, the creator's key message, and promo code before producing the audit."], failure_modes: ["Routes to performance-analyzer for what is pre-launch optimization work.", "Treats this as a campaign-results / ROI request.", "Starts producing fixes without page URL, baseline CR, or creator message."]}
```
```yaml
{id: core-message-match-conversion-plan-landing-optimizer-001, type: eval-case, status: simulated, target_skill: landing-optimizer, scenario: "User supplies full context and wants the complete optimization deliverable.", input_summary: "Our landing page for @fitnessanna's protein campaign converts at 1.2%. Page is site.com/protein, code ANNA20, her hook is 'smoothest protein I've tried.' Optimize it.", expected_behavior: ["Produce a Message Match Analysis table with a Message Match Score (X/10) and named issues with fixes (influencer-says vs landing-page-shows).", "Deliver a prioritized conversion plan covering CTA, promo-code experience (auto-apply via URL param), friction, and mobile, each with expected impact.", "Write an A/B test roadmap with at least one hypothesis and a success metric (conversion rate).", "Save the plan to memory/influencer/landing-optimizer/ and promote campaign name, URL, baseline CR, promo code, and primary creator to hot-cache; recommend performance-analyzer as next skill."], failure_modes: ["Omits the Message Match Score or returns recommendations with no expected-impact estimates.", "Skips the A/B test roadmap or gives a test with no hypothesis/success metric.", "Forgets to feature the specific creator (@fitnessanna) in social-proof recommendations."]}
```
```yaml
{id: edge-missing-creator-message-needs-input-landing-optimizer-001, type: eval-case, status: simulated, target_skill: landing-optimizer, scenario: "User asks for optimization but the creator's actual messaging and baseline conversion are unknown.", input_summary: "Make our influencer landing page convert better. I don't know our current conversion rate and I'm not sure exactly what the creator said in her post.", expected_behavior: ["Stop and ask for the missing inputs the Skill Contract reads: baseline conversion rate, the influencer's key message/quote, page URL, and promo code.", "Declare NEEDS_INPUT because Message Match Analysis cannot be scored without the creator's actual messaging and a baseline CR.", "Offer the optional connector path (~~analytics for live CR, ~~social platform analytics to confirm creator messaging) while degrading gracefully to user-supplied inputs."], failure_modes: ["Fabricates a creator quote or a baseline conversion rate to fill the Message Match table.", "Proceeds to a full plan and claims DONE without the message-match score.", "Fails to declare NEEDS_INPUT or never asks for the blocking inputs."]}
```
```yaml
{id: quality-output-voice-measured-vs-estimated-landing-optimizer-001, type: eval-case, status: simulated, target_skill: landing-optimizer, scenario: "User pushes for guaranteed numbers and a clean handoff to measurement.", input_summary: "Just tell me the exact conversion rate we'll hit after these fixes, then hand it off so we can confirm the lift.", expected_behavior: ["Label expected-impact numbers as Estimated (e.g., +0.5% CR projected) and only mark figures sourced from analytics/A/B data as Measured; never present projected lift as a guarantee.", "Keep the boundary: projected impact is the deliverable here, while actual post-launch CR/AOV/attribution is measured by performance-analyzer.", "Emit a Handoff Summary in the standard shape and recommend performance-analyzer as the primary next skill to verify whether optimizations moved conversion."], failure_modes: ["States a guaranteed post-fix conversion rate as fact rather than an estimate.", "Conflates projected impact with measured results or claims the lift is already confirmed.", "Omits the Handoff Summary or fails to route to performance-analyzer for measurement."]}
```
