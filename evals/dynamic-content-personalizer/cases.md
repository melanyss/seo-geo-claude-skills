Simulated seed cases for merge-tag/fallback mapping, per-segment conditional blocks with a mandatory catch-all, the fallback-safety audit (empty-field render), the PII guard, and the NEEDS_INPUT path when fill-rates are unknown for a conditional-driving field.

```yaml
{id: dynamic-content-personalizer-sim-001, type: eval-case, status: simulated, target_skill: dynamic-content-personalizer, scenario: "Straightforward promo personalization with first_name + last_product.", input_summary: "Base creative + export columns first_name, city, last_product (fill-rates provided); promo mode.", expected_behavior: ["Map each merge tag to a real export column and give every tag a rendered fallback (first_name -> 'there').", "Note SEND E (Engagement/personalization) relevance and label fill-rates Measured.", "Recommend email-render-builder or email-quality-auditor without self-invoking the gate."], failure_modes: ["Leaves a tag with no fallback (renders 'Hi ,').", "Binds a tag to a column that doesn't exist.", "Scores an EQS or runs a veto (out of scope)."]}
```
```yaml
{id: dynamic-content-personalizer-sim-002, type: eval-case, status: simulated, target_skill: dynamic-content-personalizer, scenario: "Per-segment conditional offer blocks keyed to the segment map.", input_summary: "Segment map with champions/at-risk/new; user wants loyalty vs win-back vs welcome offer per segment.", expected_behavior: ["Write if/elseif/else blocks keyed to named segments from the segment map.", "Require a catch-all else that renders valid content for the un-bucketed remainder.", "Run the fallback-safety audit assuming every subscriber falls to else."], failure_modes: ["Omits the catch-all else (dead-content render for un-bucketed subscribers).", "Invents segments not in the map.", "Keys conditionals on raw fields instead of named segments."]}
```
```yaml
{id: dynamic-content-personalizer-sim-003, type: eval-case, status: simulated, target_skill: dynamic-content-personalizer, scenario: "PII guard blocks sensitive fields from rendering into the body.", input_summary: "Template merges full email address and order_id into visible body copy; user asks to audit before send.", expected_behavior: ["Flag email address / order_id as blocked from body/subject render under the PII guard.", "Propose coarser substitutes (category not SKU, city not street) and keep first_name/city allowed.", "Never emit example renders containing real PII from the export."], failure_modes: ["Allows raw email/order_id to render in the body.", "Echoes real subscriber PII in example output.", "Treats the PII guard as optional."]}
```
```yaml
{id: dynamic-content-personalizer-sim-004, type: eval-case, status: simulated, target_skill: dynamic-content-personalizer, scenario: "Fill-rates unknown for a field that drives a conditional offer.", input_summary: "last_product drives a product-recommendation block but its fill-rate is not provided; promo mode.", expected_behavior: ["Return NEEDS_INPUT and ask for the fill-rate, or default the whole segment to the catch-all else.", "Do not assume the field is populated or fabricate a fill-rate.", "Label any coverage figure Estimated, never Measured, if inferred."], failure_modes: ["Assumes the field is fully populated and sends most subscribers to the wrong branch.", "Presents an estimated fill-rate as Measured.", "Proceeds silently without flagging the coverage risk."]}
```
```yaml
{id: dynamic-content-personalizer-sim-005, type: eval-case, status: simulated, target_skill: dynamic-content-personalizer, scenario: "A per-segment variation makes an unregistered promotional claim.", input_summary: "The at-risk win-back block promises 'lowest price of the year' with no ledger row; claims-ledger present.", expected_behavior: ["Check the personalized claim against memory/claims/claims-ledger.md and flag it [needs source] when no row exists.", "Recommend offer-claims-registry to register lawful wording before that variation ships.", "Do not invent substantiation or run the D1 veto itself."], failure_modes: ["Uses the unapproved superlative verbatim without flagging.", "Fabricates a claims-ledger row.", "Runs or reports a D1 verdict (the auditor's job)."]}
```
