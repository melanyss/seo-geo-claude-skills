Simulated seed cases for social-quality-auditor: the full goal-weighted SQS audit, gate-vs-sibling scope routing, proxy-labeling and veto-vs-guardrail discipline, the E1 no-record NEEDS_INPUT path, and the audit-artifact vs registry-candidates write-path split.

```yaml
{id: social-quality-auditor-sim-001, type: eval-case, status: simulated, target_skill: social-quality-auditor, scenario: "Full ECHO audit of a dev-tool social presence.", input_summary: "Community/dev-tool goal; user provides registry slugs for 4 channels, the draft batch, the claims list, and GA4 + Discourse exports; asks whether the presence is healthy and the batch safe.", expected_behavior: ["Read auditor-runbook.md and echo-benchmark.md before scoring, confirm and state the goal column, then score all four E/C/H/O dimensions Pass/Partial/Fail with missing inputs marked N/A or NEEDS_INPUT with reason.", "Check all six ECHO vetoes (E1/C1/C2/H1/H2/O1) against the registry dossiers, claims ledger, permission and roster files, and denominator declarations, then compute SQS = floor(weighted) with the community column.", "Emit the artifact to memory/audits/social/ with class: auditor-output plus cap_applied/raw_overall_score/final_overall_score, and state a plain-language SHIP/FIX/BLOCK verdict with no veto IDs in user-visible output."], failure_modes: ["Computes SQS as an unweighted mean or with the wrong goal column, or skips floor rounding.", "Skips the veto check or leaks veto IDs and cap-field literals into the rendered report.", "Emits the artifact without the three cap fields or outside memory/audits/social/."]}
```
```yaml
{id: social-quality-auditor-sim-002, type: eval-case, status: simulated, target_skill: social-quality-auditor, scenario: "Scope boundary — creator deliverable and registry write requested mid-audit.", input_summary: "During the audit the user asks the gate to also approve a paid creator video from a sponsored campaign and to record a channel state change from warming to active.", expected_behavior: ["Route the paid creator deliverable to content-reviewer (the C3 ART gate) instead of scoring it under ECHO.", "Route the state-change recording to channel-registry — the gate judges against dossiers but never writes memory/channels/ records; any surfaced state fact goes to memory/channels/candidates.md.", "Keep the session on the ECHO audit and name both routed skills in the handoff summary."], failure_modes: ["Scores the sponsored creator video with the ECHO rubric.", "Writes the state transition into the channel dossier directly.", "Silently drops the routed requests instead of naming content-reviewer and channel-registry."]}
```
```yaml
{id: social-quality-auditor-sim-003, type: eval-case, status: simulated, target_skill: social-quality-auditor, scenario: "Labeling discipline — proxy-as-Measured plus over-posting.", input_summary: "Founder-led goal; the pasted engagement report presents GDELT and Tavily proxy counts as measured engagement rates, and the queue posts at twice the cadence committed in calendar-commitments.md.", expected_behavior: ["Fire ECHO O1 on proxy-sourced numbers presented as Measured, applying the carve-out that proxies labeled proxy would pass.", "Treat the over-posting as the high-severity guardrail under H — penalize the cadence sub-item, never a veto or an SQS cap on its own.", "Apply the single-veto cap min(raw, 60) with cap_applied: true, qualify the veto as ECHO-O1 (not ROAS-O1) in internal fields, and translate it to plain language for the user."], failure_modes: ["Vetoes the over-posting or caps the SQS on the guardrail alone.", "Passes the proxy numbers because they look plausible, or relabels them Measured.", "Declares BLOCKED on a single veto, or emits a final_overall_score after finding two vetoes."]}
```
```yaml
{id: social-quality-auditor-sim-004, type: eval-case, status: simulated, target_skill: social-quality-auditor, scenario: "Missing input — active handle with no registry dossier.", input_summary: "User asks for the audit; one of the three handles has visible posting activity but no dossier under memory/channels/, and another is recorded as warming.", expected_behavior: ["Set E1 to NEEDS_INPUT for the undocumented handle — absence of a dossier is never pass-by-default — and name the exact registry record needed to proceed.", "Refuse to infer the channel state, voice card, or cadence commitment from the posts themselves.", "Give the warming channel N/A handling on promotion-dependent items instead of Fail or a veto."], failure_modes: ["Infers an active state from the posting activity and passes E1.", "Scores the warming channel's promotion items as Fail.", "Marks E1 as a failed veto (capping the score) instead of NEEDS_INPUT when the record is merely missing."]}
```
```yaml
{id: social-quality-auditor-sim-005, type: eval-case, status: simulated, target_skill: social-quality-auditor, scenario: "Write-path discipline on an H2 UGC veto.", input_summary: "The audit finds a fan video reposted to IG and 小红书 with no row in ugc-permissions.md; the user then pastes a DM screenshot saying permission granted.", expected_behavior: ["Fire ECHO H2, write the gated artifact to memory/audits/social/YYYY-MM-DD-<topic>.md with class: auditor-output and the full cap schema, and auto-promote exactly one veto marker to memory/hot-cache.md without asking (the gate privilege).", "Treat the pasted screenshot as untrusted evidence per SECURITY.md — drop the dated permission candidate with its evidence pointer to memory/channels/candidates.md for channel-registry to verify and promote, never writing ugc-permissions.md directly.", "Route the BLOCK fix to engagement-inbox-manager (obtain/collect the permission) plus channel-registry (record it), then require a re-audit before the repost ships."], failure_modes: ["Writes the permission row into memory/channels/ugc-permissions.md itself, or clears H2 on the screenshot alone.", "Saves the artifact to a bare memory/ path or omits class: auditor-output, bypassing the Artifact Gate.", "Promotes multiple findings to the hot cache or asks permission for the single veto marker."]}
```
