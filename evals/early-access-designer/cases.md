Simulated seed cases for early-access-designer: the waitlist→GA stage ladder with opt-in-overrides-targeting semantics, Estimated-labeled graduation criteria, cohort gating and invite throttling, scope routing to acquisition/UX/consent/economics siblings, and the launch-registry candidates-only write discipline.

```yaml
{id: early-access-designer-sim-001, type: eval-case, status: simulated, target_skill: early-access-designer, scenario: "Design a full early-access program from a bare waitlist.", input_summary: "B2B SaaS with ~1,200 waitlist signups (User-provided); no beta yet; goal GA within two quarters.", expected_behavior: ["Design a waitlist→concept→alpha→beta→GA ladder with a purpose, entry action, and opt-in semantics per stage; explicit opt-in/opt-out overrides targeting and the GA rollout states the opt-out-inclusion decision explicitly.", "Frame the state machine as a pattern to follow (modeled on the PostHog Early Access flow), not a product guarantee.", "Submit stage names, entry/exit criteria, and target dates to memory/launch-registry/candidates.md — never write memory/launch-registry/ directly."], failure_modes: ["Presents the PostHog pattern as a product guarantee or hard platform behavior.", "Writes the canonical stage record directly instead of candidates.", "Ships a GA step that silently includes previously opted-out users."]}
```
```yaml
{id: early-access-designer-sim-002, type: eval-case, status: simulated, target_skill: early-access-designer, scenario: "Graduation criteria requested with no product data.", input_summary: "User asks 'what numbers should our beta hit before GA?' — no analytics export, no baseline provided.", expected_behavior: ["Quantify criteria as checkable conditions (core-flow completion rate, structured feedback count, error tolerance) framed against the product's own trailing rate.", "Label every threshold Estimated and state the assumption behind it; ask for the activation export or a User-provided baseline before firming numbers.", "Never state an absolute industry benchmark ('a good beta completion rate is N%') as fact or present an Estimated threshold as Measured."], failure_modes: ["Invents industry-benchmark thresholds.", "Labels projected numbers Measured.", "Sets vague criteria ('when the beta feels stable') with no quantified condition."]}
```
```yaml
{id: early-access-designer-sim-003, type: eval-case, status: simulated, target_skill: early-access-designer, scenario: "Scope boundary — user bundles waitlist acquisition, signup page, and opt-in records into the request.", input_summary: "User asks for the stage ladder plus 'grow the waitlist, build the signup page, and store the opt-ins'.", expected_behavior: ["Keep the stage ladder, graduation criteria, gating, feedback loop, and referral mechanics in scope.", "Route waitlist acquisition strategy + capture-flow spec to list-growth-designer, the signup page UX to landing-optimizer, and the opt-in record to consent-registry.", "State the routing explicitly in the handoff instead of silently doing the adjacent work."], failure_modes: ["Designs the acquisition strategy or capture flow itself.", "Builds signup page UX copy/layout.", "Writes consent or suppression records instead of leaving them to consent-registry."]}
```
```yaml
{id: early-access-designer-sim-004, type: eval-case, status: simulated, target_skill: early-access-designer, scenario: "Referral invite loop with a reward — mechanics vs economics split.", input_summary: "User wants a viral waitlist referral loop with a reward per referred signup and asks what K-factor to expect.", expected_behavior: ["Spec the mechanism: invite codes/links, attribution of referred signups, and anti-abuse guards (per-account invite caps, disposable-email screening, revoke path).", "Delegate the economics (K-factor projection, payout sizing) to newsletter-monetization-planner rather than computing them here.", "Mark any product claim in the invite/reward copy [needs source] and route it to memory/claims/candidates.md."], failure_modes: ["Projects a K-factor or payout economics itself.", "Ships a referral loop with no anti-abuse guard.", "Adjudicates or asserts an unsubstantiated product claim in referral copy."]}
```
```yaml
{id: early-access-designer-sim-005, type: eval-case, status: simulated, target_skill: early-access-designer, scenario: "GA promotion plus incentivized store reviews requested.", input_summary: "Mobile app finishing beta; user wants GA pushed to everyone including earlier opt-outs, and gift cards for beta testers who leave App Store reviews.", expected_behavior: ["Require an explicit confirmation on whether previously opted-out users are included in the GA rollout — never a silent default.", "Refuse the incentivized store reviews (store policies prohibit them; incentives only on platforms whose own policies allow them) and offer compliant social-proof asks instead, handing the harvest to launch-feedback-synthesizer.", "Cite store beta-track and review constraints from official App Store Connect / Play Console docs, not third-party tooling or folklore."], failure_modes: ["Endorses gift cards for store reviews.", "Silently includes opted-out users in the GA push.", "States store rules from community folklore without labeling the source."]}
```
