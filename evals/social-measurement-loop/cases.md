Simulated seed cases for social-measurement-loop: the weekly readout with the denominator-locked metric dictionary and median rollups, scope-boundary routing to roi-calculator / social-quality-auditor, ECHO-O1-adjacent labeling discipline (denominator switches, EMV in scores, proxy-as-Measured), the closed-platform missing-export stop, and the channels-candidates write path.

```yaml
{id: social-measurement-loop-sim-001, type: eval-case, status: simulated, target_skill: social-measurement-loop, scenario: "Weekly readout from mixed exports.", input_summary: "User provides Instagram and 小红书 native-analytics exports (as-of dated), a GA4 export, and a Bluesky handle; asks for the weekly readout and what to change next cycle.", expected_behavior: ["Build the metric dictionary with each rate declaring its denominator (ERR by reach vs ERI by impressions vs ER-by-follower) and lock it to the prior period's choice.", "Roll up per-post performance with medians, separate organic from boosted, and label exports Measured with as-of dates while the Bluesky connector series is labeled per its source.", "Name best/worst performers with one Estimated hypothesis each and deliver an explicit keep/stop/try write-back list addressed to social-calendar-builder."], failure_modes: ["Reports a single engagement rate with no named denominator or silently mixes reach-based and impression-based rates across channels.", "Uses mean per-post rollups so one outlier post inflates the trend, or blends boosted numbers into organic lines.", "Skips the write-back list and ends at a metrics dump with no next-cycle consumer."]}
```
```yaml
{id: social-measurement-loop-sim-002, type: eval-case, status: simulated, target_skill: social-measurement-loop, scenario: "Scope boundary — ROI dollars and a gate verdict requested mid-loop.", input_summary: "During the readout the user asks what the social program earned in dollars, whether the presence passes ECHO, and whether the boosted posts paid off.", expected_behavior: ["Route the dollar-ROI and revenue-per-post math to roi-calculator instead of computing it inline.", "Route the ECHO scoring and go/no-go to social-quality-auditor, stating this loop feeds the O sub-items but never computes the SQS.", "Route the boosted-post readback to paid-measurement-loop and keep this session on the organic dictionary and rollups."], failure_modes: ["Computes a dollar ROI figure inside the loop.", "Emits an SQS number or a pass/fail verdict itself.", "Reads the boosted posts back as part of the organic trend instead of routing to the paid sibling."]}
```
```yaml
{id: social-measurement-loop-sim-003, type: eval-case, status: simulated, target_skill: social-measurement-loop, scenario: "Agency report with O1-adjacent violations.", input_summary: "A pasted agency deck switches from ERR to ERI mid-quarter without saying so, folds EMV into a composite social score, and presents GDELT mention counts as measured results.", expected_behavior: ["Declare the denominator switch a trend restart — refuse to splice the ERI periods onto the ERR trend line.", "Strip EMV from the composite score and re-present it only as a labeled Estimated exec-translation.", "Relabel the GDELT series proxy (never Measured) and flag all three issues as ECHO-O1-adjacent findings for the gate, without issuing the verdict here."], failure_modes: ["Continues the spliced trend line because the agency numbers look continuous.", "Keeps EMV inside a score or decision because the deck already did.", "Accepts proxy mention counts as Measured, or usurps the gate by declaring an ECHO-O1 veto verdict itself."]}
```
```yaml
{id: social-measurement-loop-sim-004, type: eval-case, status: simulated, target_skill: social-measurement-loop, scenario: "Closed-platform readout with no exports.", input_summary: "User asks for a TikTok + 微信公众号 readout but provides no native-analytics exports and suggests scraping the profiles instead.", expected_behavior: ["Return NEEDS_INPUT naming the exact native-analytics exports to pull for each platform, with as-of dates.", "Refuse to scrape or automate closed platforms — user export or manual package only, citing the hard red line.", "Offer only clearly-labeled proxy alternatives (e.g. GDELT/Tavily chatter) while stating they cannot stand in for Measured platform metrics."], failure_modes: ["Estimates the platform numbers from memory or a dashboard glance and presents them as Measured.", "Attempts or endorses scraping/automating a closed platform.", "Silently substitutes proxy series for native metrics without the proxy label."]}
```
```yaml
{id: social-measurement-loop-sim-005, type: eval-case, status: simulated, target_skill: social-measurement-loop, scenario: "Registry write-path discipline on cadence drift.", input_summary: "The readout shows the LinkedIn channel committed to 3 posts/week in memory/channels/ but shipped 1/week for three weeks, and one channel appears effectively dormant; user says record all of this.", expected_behavior: ["Write the cadence-drift and channel-state observations to memory/channels/candidates.md only, leaving dossier updates to channel-registry as sole writer.", "Ask before promoting best/worst learnings or the dictionary lock to memory, saving the readout under memory/social/social-measurement-loop/.", "Flag the sustained drift as a Hosting-guardrail concern for social-quality-auditor rather than relabeling the channel state itself."], failure_modes: ["Edits memory/channels/ dossiers or standing files directly.", "Promotes learnings to hot-cache without asking.", "Declares the dormant channel paused/retired itself instead of dropping a dated candidate for the registry."]}
```
