Simulated seed cases for narrative-baseline-mapper: surface-by-surface current-state inventory with labeling discipline, gap reads against the intended message / existing canon, claim-flagging (never adjudicating) to candidates, drift-baseline freezing before a reposition, and scope-boundary routing to message-system-architect / narrative-quality-auditor.

```yaml
{id: narrative-baseline-mapper-sim-001, type: eval-case, status: simulated, target_skill: narrative-baseline-mapper, scenario: "Full surface inventory vs a stated intended message.", input_summary: "B2B SaaS; user pastes homepage, pricing, docs intro, deck one-liner, and two social bios, plus an intended one-liner; asks what each surface says today and where the gaps are.", expected_behavior: ["Produce a surface-by-surface current-state line, each labeled Measured / User-provided / Estimated with an as-of date.", "Classify each surface aligned / drifted / contradictory / silent vs the intended one-liner and quote the exact line earning the classification.", "Freeze the drift baseline and hand off to category-narrative-mapper."], failure_modes: ["Presents an inferred or improved headline as the current state instead of the actual line.", "Gives a gap read with no quoted line to back it.", "Rewrites the surfaces into on-message copy instead of inventorying them."]}
```
```yaml
{id: narrative-baseline-mapper-sim-002, type: eval-case, status: simulated, target_skill: narrative-baseline-mapper, scenario: "No canon and no intended message yet.", input_summary: "Pre-messaging startup; user supplies surfaces but has no canon in memory/narrative/ and no stated intended message.", expected_behavior: ["Record that no canon exists and mark intent as User-provided-only rather than inventing an intended message to score against.", "Still deliver the full current-state inventory with labels and as-of dates so the baseline is usable once a canon exists.", "Route canon authoring to message-system-architect instead of drafting positioning or a message house inline."], failure_modes: ["Invents an intended message and reads gaps against it as if it were canon.", "Writes a canon or message house itself.", "Refuses to inventory anything because no canon exists."]}
```
```yaml
{id: narrative-baseline-mapper-sim-003, type: eval-case, status: simulated, target_skill: narrative-baseline-mapper, scenario: "Unverifiable claim found on a live surface.", input_summary: "Homepage scraped via firecrawl.py reads 'the #1 fastest platform'; no dated basis and not present in memory/claims/claims-ledger.md.", expected_behavior: ["Record the claim as the current state of the surface, labeled Measured with the URL and as-of date, exactly as it reads.", "Mark the superlative [needs source] and submit it to memory/claims/candidates.md without adjudicating it.", "State that offer-claims-registry owns substantiation and this skill only records where the claim lives."], failure_modes: ["Adjudicates the '#1' claim as true or false itself.", "Silently drops the claim instead of routing it to candidates.", "Treats the marketing superlative as a Measured performance fact."]}
```
```yaml
{id: narrative-baseline-mapper-sim-004, type: eval-case, status: simulated, target_skill: narrative-baseline-mapper, scenario: "Freeze a drift baseline before a reposition.", input_summary: "Team is about to reposition; user wants an immutable 'before' snapshot of every owned surface and the drift already visible from last year's copy.", expected_behavior: ["Freeze each surface's current line with source and as-of date as the immutable baseline that narrative-drift-monitor later measures against.", "Pull prior copy via wayback.py to show the drift-in-progress, labeling historical lines with their capture dates.", "Hand off the frozen baseline and name narrative-drift-monitor as the reader of it."], failure_modes: ["Freezes an edited/idealized version instead of the literal current copy.", "Labels wayback historical copy as current or as Measured-today.", "Scores or ranks the surfaces instead of just snapshotting them."]}
```
```yaml
{id: narrative-baseline-mapper-sim-005, type: eval-case, status: simulated, target_skill: narrative-baseline-mapper, scenario: "Scope boundary — user asks for the score and new copy.", input_summary: "Mid-inventory the user asks for the NQS on these surfaces and a rewritten on-message homepage headline.", expected_behavior: ["Route NQS scoring and the vetoes to narrative-quality-auditor rather than computing a score.", "Route the rewritten headline to message-system-architect (canon) and the surface creative builder, keeping this session on the inventory.", "Deliver the baseline as the only artifact and name the routed skills in the handoff summary."], failure_modes: ["Emits an NQS or runs a veto itself.", "Writes the new headline instead of stopping at the inventory.", "Silently drops the routed requests instead of naming the sibling skills."]}
```
