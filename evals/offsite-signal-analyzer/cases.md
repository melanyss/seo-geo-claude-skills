Simulated seed cases for offsite-signal-analyzer covering the two absorbed modes (backlinks link-profile/toxicity/gap and ai-referrals GA4/GSC/log AI-assistant channel), the mode selector and seam, plus NEEDS_INPUT and mode-ambiguity boundaries.

```yaml
{id: offsite-signal-analyzer-sim-001, type: eval-case, status: simulated, target_skill: offsite-signal-analyzer, scenario: "backlinks mode — full profile with toxic links and competitor gap.", input_summary: "Ahrefs referring-domain export + 3 competitor domains; 18% toxic-anchor share observed.", expected_behavior: ["Run mode=backlinks and state it in the first output line.", "Report referring domains, anchor mix, and a computed toxic ratio, each figure source-tagged.", "Name >=3 link-building or disavow actions and recommend domain-authority-auditor because toxic ratio > 15%."], failure_modes: ["Estimates referring-domain volume from the domain alone.", "Presents an estimated toxic ratio as Measured.", "Blends backlink and AI data into one score or skips the auditor handoff."]}
```
```yaml
{id: offsite-signal-analyzer-sim-002, type: eval-case, status: simulated, target_skill: offsite-signal-analyzer, scenario: "ai-referrals mode — 90-day AI channel with conversion comparison.", input_summary: "GA4 source/medium export + conversion event; user asks if ChatGPT/Perplexity convert better than organic.", expected_behavior: ["Run mode=ai-referrals; define the AI channel with the starter regex and record the final source list as evidence.", "Report AI sessions trend, top AI landing pages, and AI-vs-organic conversion as a ratio for the same window.", "Apply measurement-protocol against a control and label the movement Promote/Keep-testing/Rollback/Unproven; recommend performance-reporter."], failure_modes: ["Places AI Assistants below Referral so it never matches.", "Credits a shift to AI without a control readback.", "Reports the raw roas/conversion delta by eye instead of the ledger."]}
```
```yaml
{id: offsite-signal-analyzer-sim-003, type: eval-case, status: simulated, target_skill: offsite-signal-analyzer, scenario: "ai-referrals with no conversion event named — continue silently.", input_summary: "GA4 source/medium export provided but no goal/conversion event specified.", expected_behavior: ["Do not stop; report AI sessions and engagement only and note the missing-conversion gap.", "Still define the AI channel and build the trend against the same window.", "Emit DONE_WITH_CONCERNS listing the conversion gap as an open loop."], failure_modes: ["Stops and asks for a conversion when sessions could be reported.", "Fabricates a conversion rate for the AI channel.", "Emits plain DONE without flagging the gap."]}
```
```yaml
{id: offsite-signal-analyzer-sim-004, type: eval-case, status: simulated, target_skill: offsite-signal-analyzer, scenario: "backlinks mode — no data and no link database connected (NEEDS_INPUT boundary).", input_summary: "User asks to analyze backlinks for a domain but provides no export and no ~~link database is connected.", expected_behavior: ["Return status NEEDS_INPUT and hit the backlinks decision gate.", "Offer the three options: paste an export, connect a tool, or cancel.", "Do not estimate referring-domain volume or toxic ratio from the domain alone."], failure_modes: ["Invents a referring-domain count or health score.", "Silently switches to ai-referrals to produce output.", "Emits DONE despite having no measurable data."]}
```
```yaml
{id: offsite-signal-analyzer-sim-005, type: eval-case, status: simulated, target_skill: offsite-signal-analyzer, scenario: "Mode ambiguous — request names both link and AI-traffic intent.", input_summary: "User: 'give me my off-site picture — backlinks and how much AI traffic I get' with a backlink CSV and a GA4 export.", expected_behavior: ["Detect the ambiguity and stop per the decision gate rather than auto-running both modes.", "Present the two modes (backlinks, ai-referrals) and ask which to run first.", "Keep the datasets separate and note the seam once a mode is chosen."], failure_modes: ["Auto-runs both modes and merges them into one number.", "Guesses a single mode without stating the choice.", "Chains into an auditor before either mode completes."]}
```
