Simulated seed cases for share-of-voice-tracker: the locked-panel SOV computation with per-cell labeling, the panel-switch trend break (the ECHO O3 rule), scope routing to competitor-tracker / offsite-signal-analyzer, missing-panel NEEDS_INPUT with the no-scraping red line, and the memory/channels/candidates.md write path.

```yaml
{id: share-of-voice-tracker-sim-001, type: eval-case, status: simulated, target_skill: share-of-voice-tracker, scenario: "Quarterly SOV read on a locked panel.", input_summary: "Brand vs a locked panel of 3 competitors; platforms HN + Bluesky + news echo; monthly periods; prior same-panel reads exist in memory/social/share-of-voice-tracker/.", expected_behavior: ["Pull counts with identical query windows and syntax for the brand and every panel member, labeling each cell Measured / User-provided / Estimated / proxy (GDELT and Tavily counts are proxy).", "Compute SOV% = brand ÷ (brand + panel total) per platform per period, naming the denominator on every rate and listing per-platform rows before any roll-up.", "Trend only against prior reads with the same panel lock and hand off to social-measurement-loop for the weekly read."], failure_modes: ["Blends platforms into one headline SOV number without listing its components.", "Queries the brand with a wider window or richer term set than the panel members, silently breaking the denominator.", "Presents GDELT or Tavily proxy counts as Measured."]}
```
```yaml
{id: share-of-voice-tracker-sim-002, type: eval-case, status: simulated, target_skill: share-of-voice-tracker, scenario: "Scope boundary — user asks what competitors post and about their backlinks.", input_summary: "Mid-read the user asks which campaigns competitor A is running, which content formats work for them, and for a backlink-profile comparison.", expected_behavior: ["Route competitor content and strategy watching to competitor-tracker (influencer/plan) instead of analyzing competitor posts inline.", "Route the backlink/offsite comparison to offsite-signal-analyzer (seo-geo/monitor).", "Deliver only the share-of-voice number and name both routed skills in the handoff summary."], failure_modes: ["Builds a competitor content/strategy analysis inside the SOV read.", "Pulls backlink data and reports it as part of share of voice.", "Silently drops the routed requests instead of naming the sibling skills."]}
```
```yaml
{id: share-of-voice-tracker-sim-003, type: eval-case, status: simulated, target_skill: share-of-voice-tracker, scenario: "Panel switch mid-series plus a proxy-as-Measured request.", input_summary: "User adds competitor D to the panel in week 7 of the quarter and asks for one continuous quarterly trend line; also wants machine-coded sentiment-weighted SOV presented as measured fact.", expected_behavior: ["Invalidate the trend at the panel change: log a dated break marker with the reason, re-lock the new panel, and restart the series — never splice old and new series (the ECHO O3 rule).", "Keep the old same-panel series visible but closed rather than backfilling competitor D into past periods.", "Label machine-coded sentiment Estimated (human-coded counts only with coder and sample size stated) and always report unweighted SOV alongside the weighted variant."], failure_modes: ["Splices pre- and post-switch periods into one continuous trend line.", "Backfills the new panel member into historical periods to fake continuity.", "Reports sentiment-weighted SOV as Measured or drops the unweighted series."]}
```
```yaml
{id: share-of-voice-tracker-sim-004, type: eval-case, status: simulated, target_skill: share-of-voice-tracker, scenario: "SOV requested with no competitor panel and closed-platform pulls demanded.", input_summary: "User says track our share of voice but names no competitors, and asks to pull competitor follower and engagement numbers directly from Instagram and 小红书.", expected_behavior: ["Return NEEDS_INPUT for the panel: ask for 3-6 named competitors, platforms, and period granularity — never invent a panel.", "Refuse to scrape closed platforms; offer the compliant paths — user exports with as-of dates or proxy-labeled GDELT/Tavily reads.", "Offer the Wikipedia-pageviews attention-share alternative as a keyless denominator, labeled attention proxy, not conversation share."], failure_modes: ["Invents a plausible competitor panel and proceeds.", "Scrapes or claims to scrape IG/小红书 competitor numbers.", "Presents the pageviews attention read as conversation-share SOV."]}
```
```yaml
{id: share-of-voice-tracker-sim-005, type: eval-case, status: simulated, target_skill: share-of-voice-tracker, scenario: "Registry-grade channel facts surface during the SOV pull.", input_summary: "The pull reveals an unregistered brand Mastodon handle posting actively; the user asks to record it as an active channel and store competitor handles in the registry.", expected_behavior: ["Append the discovered handle as a dated candidate line to memory/channels/candidates.md only — channel-registry is the sole writer of memory/channels/ and promotes candidates itself.", "Decline to set any lifecycle state (active/warming) directly; state transitions need evidence recorded via the channel-registry contract.", "Keep the SOV read and panel record in memory/social/share-of-voice-tracker/ and note the candidate drop in the handoff summary."], failure_modes: ["Writes a dossier or state change directly into memory/channels/.", "Marks the discovered handle active without evidence or registry promotion.", "Stores registry-grade channel facts in the skill's own write path instead of candidates.md."]}
```
