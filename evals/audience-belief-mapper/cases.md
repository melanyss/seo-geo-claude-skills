Simulated seed cases for audience-belief-mapper: belief/objection extraction sourced to interviews, the JTBD four-forces mapping, claim-verifiability routing to candidates, the persona-base dependency on audience-mapper, and scope-boundary routing away from persona profiling and the positioning canvas.

```yaml
{id: audience-belief-mapper-sim-001, type: eval-case, status: simulated, target_skill: audience-belief-mapper, scenario: "Full belief map from win-loss notes.", input_summary: "B2B SaaS beachhead; user pastes 15 interview and win-loss notes and asks for beliefs, objections with reframes, and the four forces.", expected_behavior: ["Extract held beliefs and objections traced to specific User-provided evidence lines, each labeled Measured / User-provided / Estimated.", "Map the JTBD four forces (push / pull / anxiety / habit) with the evidence line each came from, dropping or labeling Estimated any force with no evidence.", "Give every recurring objection at least one reframe candidate and hand off to strategic-narrative-designer."], failure_modes: ["Invents beliefs or objections not present in the notes and presents them as User-provided.", "Collapses the four forces into a single pros/cons list.", "Leaves objections with no reframe candidate."]}
```
```yaml
{id: audience-belief-mapper-sim-002, type: eval-case, status: simulated, target_skill: audience-belief-mapper, scenario: "Scope boundary — user asks for a persona profile and a positioning canvas.", input_summary: "Mid-session the user asks for a detailed firmographic buyer persona and to redo the positioning canvas.", expected_behavior: ["Route demographic/firmographic persona profiling to audience-mapper and read its output as the persona base rather than rebuilding it.", "Route positioning-canvas reconciliation to positioning-truth-tracer instead of doing it inline.", "Deliver only the belief map and name the routed sibling skills in the handoff summary."], failure_modes: ["Builds a full persona profile inside the belief map.", "Rebuilds or reconciles the positioning canvas itself.", "Silently drops the routed requests instead of naming the sibling skills."]}
```
```yaml
{id: audience-belief-mapper-sim-003, type: eval-case, status: simulated, target_skill: audience-belief-mapper, scenario: "Objection reframe leans on an unverified comparative claim.", input_summary: "A recurring objection is 'competitor X is cheaper'; the user wants the reframe to assert the product is cheaper at scale, with no pricing evidence provided.", expected_behavior: ["Preserve the buyer's verbatim objection as User-provided, sourced to the notes it appears in.", "Mark the comparative reframe claim [needs source] and submit it to memory/events/claims.ndjson via an authorized operation: propose request to registry-events.py instead of asserting it as fact.", "State that this skill records who said it and never adjudicates whether it is true — the claims ledger owns substantiation."], failure_modes: ["Writes the 'cheaper at scale' claim into the reframe as established fact.", "Adjudicates the claim itself instead of routing it to candidates.", "Drops the objection or its claim silently without recording it."]}
```
```yaml
{id: audience-belief-mapper-sim-004, type: eval-case, status: simulated, target_skill: audience-belief-mapper, scenario: "Belief map requested with no persona base or evidence.", input_summary: "User wants the beliefs and forces for the beachhead but provides no interviews, win-loss notes, or prior audience-mapper output.", expected_behavior: ["Stop and route to audience-mapper to build the segment/persona base before mapping beliefs against it.", "Refuse to map beliefs for 'everyone' and flag that an unanchored map is a T beachhead-truth failure.", "Label any provisional belief Estimated with its assumption stated rather than presenting it as User-provided."], failure_modes: ["Maps beliefs for a generic 'everyone' audience with no beachhead anchor.", "Fabricates interview quotes to fill the empty evidence base.", "Presents Estimated inferences as Measured or User-provided facts."]}
```
```yaml
{id: audience-belief-mapper-sim-005, type: eval-case, status: simulated, target_skill: audience-belief-mapper, scenario: "Canon-grade belief and memory-write discipline.", input_summary: "A durable, cross-deal belief emerges ('buyers distrust auto-migration tools'); user asks to save it as the brand's official belief.", expected_behavior: ["Route the durable canon-grade belief to memory/events/narrative.ndjson via an authorized operation: propose request to registry-events.py only, never writing memory/narrative/ canon files directly.", "Ask before writing any memory and save the working map to memory/narrative/audience-belief-mapper/YYYY-MM-DD-<topic>.md on confirmation.", "Note that narrative-registry is the sole writer of the canon and this skill only proposes candidates."], failure_modes: ["Writes the belief into a memory/narrative/ canon file directly.", "Saves memory without asking the user first.", "Treats the candidate belief as adjudicated canon rather than a proposal."]}
```
