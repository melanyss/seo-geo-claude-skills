Simulated seed cases for preference-center design, the frequency/topic opt-down ladder as N1 mitigation, the preference-choice → suppression/frequency mapping, the SEND N sub-item note, scope boundaries against email-sequence-designer / consent-registry / email-quality-auditor, and the NEEDS_INPUT no-topic-set path.

```yaml
{id: preference-frequency-manager-sim-001, type: eval-case, status: simulated, target_skill: preference-frequency-manager, scenario: "Rising opt-out rate; build a preference center with a step-down ladder instead of a hard unsubscribe.", input_summary: "Retention/newsletter program on Klaviyo; topics product-updates/weekly-digest/promotions; opt-out rate climbing on the unengaged segment.", expected_behavior: ["Define topic groups plus at least two cadence tiers and a pause option.", "Design an ordered opt-down ladder (reduce frequency -> pick topics -> pause) with the hard unsubscribe still one click away.", "Emit the N preference-center/frequency-options sub-item note as Pass with rationale and recommend email-sequence-designer."], failure_modes: ["Obstructs or hides the one-click hard unsubscribe behind the ladder.", "Computes the full N dimension score or the EQS.", "Rules the N1 veto instead of handing it to the auditor."]}
```
```yaml
{id: preference-frequency-manager-sim-002, type: eval-case, status: simulated, target_skill: preference-frequency-manager, scenario: "Every preference choice must map to a suppression/frequency rule the ESP and consent-registry honor.", input_summary: "User wants topic toggles (mute promotions), a monthly cadence tier, and a 90-day pause; asks how each is enforced.", expected_behavior: ["Map muting a topic to suppressing only that stream, not the whole list.", "Map the monthly tier to a 1/month send cap and the pause to suppress-until-date then resume prior tier.", "Route the mapping to consent-registry as the record and note the auditor's N1 check reads against it."], failure_modes: ["Maps a topic mute to a full-list suppression.", "Writes the consent/suppression record itself instead of routing to consent-registry.", "Offers a monthly tier the flows cannot actually throttle to."]}
```
```yaml
{id: preference-frequency-manager-sim-003, type: eval-case, status: simulated, target_skill: preference-frequency-manager, scenario: "No topic set stated and no ESP export to read streams from — NEEDS_INPUT.", input_summary: "User asks to build a preference center for a program with no described content streams and no preference-config export.", expected_behavior: ["Stop and present numbered options for topic groups and cadence tiers with their outcomes.", "Return status NEEDS_INPUT naming the missing topic set / ESP export.", "Do not invent subscription categories the program does not send."], failure_modes: ["Fabricates topic groups with no basis.", "Emits DONE despite unknowable topics.", "Designs a ladder against invented streams."]}
```
```yaml
{id: preference-frequency-manager-sim-004, type: eval-case, status: simulated, target_skill: preference-frequency-manager, scenario: "User also asks for global send caps and quiet hours — scope boundary to email-sequence-designer.", input_summary: "Request bundles the preference page plus a program-wide send cap and quiet-hours window across all flows.", expected_behavior: ["Design the per-subject preference/ladder rules and the choice-to-rule mapping.", "Decline the global send-cap / quiet-hours governance as email-sequence-designer's scope and hand it off.", "Match the ladder's cadence tiers to the designer's cadence plan when present."], failure_modes: ["Designs the global send cap and quiet-hours governance itself.", "Offers cadence tiers that contradict the flow program's frequencies.", "Drops the handoff to email-sequence-designer."]}
```
```yaml
{id: preference-frequency-manager-sim-005, type: eval-case, status: simulated, target_skill: preference-frequency-manager, scenario: "Partial ladder — pause option missing and a topic mute suppresses the whole list.", input_summary: "Existing preference center offers topics and a monthly tier but no pause; muting any topic unsubscribes the subject from everything.", expected_behavior: ["Flag the whole-list suppression as a mapping defect and specify per-topic suppression.", "Add the missing pause tier and define the sunset terminus the ladder ends into.", "Emit the N sub-item note as Partial with the specific gaps, not Pass."], failure_modes: ["Scores the sub-item Pass despite the whole-list-suppression defect.", "Adds a pause tier with no sunset terminus.", "Treats over-frequency as an N1 veto rather than a mitigation gap."]}
```
