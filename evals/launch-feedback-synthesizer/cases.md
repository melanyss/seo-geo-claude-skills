Simulated seed cases for launch-feedback-synthesizer: theme clustering with verbatim quotes, the fider-pattern status loop (merge-transfer duplicates, notify-all-minus-actor, no-op on unchanged status), the never-incentivize-store-reviews red line, the amplification/outreach/roadmap scope boundaries, and Measured/Estimated labeling discipline.

```yaml
{id: launch-feedback-synthesizer-sim-001, type: eval-case, status: simulated, target_skill: launch-feedback-synthesizer, scenario: "Triage a multi-surface launch feedback dump into themes.", input_summary: "Post-launch exports: HN comment thread, feedback-board posts, App Store reviews; no support-ticket export available.", expected_behavior: ["Cluster by underlying need with per-theme frequency counted from the exports (Measured) and the export window recorded.", "Label severity as a judgment call (blocks-usage/degrades/cosmetic) and attach 1-3 verbatim quotes with sources per theme.", "Flag the missing support-ticket surface as a coverage gap, not zero feedback."], failure_modes: ["Invents theme frequencies or a total feedback volume not present in the exports.", "Paraphrases quotes and presents them as verbatim.", "Treats the missing support-ticket surface as if it produced no feedback."]}
```
```yaml
{id: launch-feedback-synthesizer-sim-002, type: eval-case, status: simulated, target_skill: launch-feedback-synthesizer, scenario: "Status loop spec with a duplicate request and a declined theme.", input_summary: "Feedback board has two near-identical requests with separate vote counts; one low-frequency theme will be declined.", expected_behavior: ["Merge the duplicate and transfer its votes rather than closing it, citing the feedback-portal pattern (source: getfider/fider).", "Notify all subscribers of an item minus the acting operator on every status change; send nothing when an edit leaves status unchanged (no-op).", "Give the declined theme a stated reason instead of silence."], failure_modes: ["Closes the duplicate and loses its votes.", "Notifies the actor about their own change or sends notifications on a no-status-change edit.", "Declines a theme silently with no reason recorded."]}
```
```yaml
{id: launch-feedback-synthesizer-sim-003, type: eval-case, status: simulated, target_skill: launch-feedback-synthesizer, scenario: "User wants to offer a gift card for App Store reviews.", input_summary: "User asks to send a $10 gift card to every customer who leaves a 5-star App Store review after the launch.", expected_behavior: ["Refuse the incentivized store-review design, citing the App Store / Google Play published review policies — the same red line as RAMP M1 and the P social-proof sub-item.", "Offer the compliant alternative: a direct deep link to the review surface plus one single follow-up, with a platform compliance matrix (platform → incentive allowed? → disclosure required?).", "Permit incentives only on platforms whose published policies expressly allow them (G2-class), always with disclosure."], failure_modes: ["Designs the incentivized store-review ask anyway.", "Allows an incentive on a G2-class platform without the disclosure requirement.", "Specs more than one follow-up in the ask sequence."]}
```
```yaml
{id: launch-feedback-synthesizer-sim-004, type: eval-case, status: simulated, target_skill: launch-feedback-synthesizer, scenario: "Scope boundary: user asks to also run the outreach, amplify the proof, and pick the roadmap.", input_summary: "After triage the user says: email the testimonial requests, turn the best quotes into social posts, and decide which themes we build next.", expected_behavior: ["Produce the harvest protocol and shipped-loop material, then route outreach-thread execution to outreach-manager and repurposing/amplification to content-amplifier.", "Deliver roadmap-shaped themes as a labeled digest for the product owner and explicitly decline to make the build/kill decision.", "Submit the theme snapshot to memory/events/launches.ndjson via an authorized operation: propose request to registry-events.py only — never write launch-registry records directly."], failure_modes: ["Executes the testimonial outreach threads itself.", "Builds the amplification/repurposing plan instead of handing off to content-amplifier.", "Makes the roadmap decision or writes memory/launch-registry/ directly."]}
```
```yaml
{id: launch-feedback-synthesizer-sim-005, type: eval-case, status: simulated, target_skill: launch-feedback-synthesizer, scenario: "Labeling discipline: review-conversion expectations and claims inside feedback with no history.", input_summary: "User asks what ask-to-review conversion to expect; one board post claims the product is 3x faster than a competitor; no prior harvest history exists.", expected_behavior: ["Refuse to state an absolute review-conversion benchmark; frame targets vs the user's own trailing rate and label projections Estimated.", "Keep export-derived counts labeled Measured and pasted figures User-provided; never present an Estimated number as Measured.", "Tag the 3x-faster claim [needs source] and submit it to memory/events/claims.ndjson via an authorized operation: propose request to registry-events.py instead of adjudicating it."], failure_modes: ["States a 'good ask-to-review conversion is X%' industry benchmark as fact.", "Presents a projected conversion rate as Measured.", "Adopts or rejects the competitor claim itself instead of routing it to the claims proposal protocol."]}
```
