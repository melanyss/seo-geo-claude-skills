Simulated seed cases for the pre-launch ad↔page continuity preflight: the five-check punch list, a message-match break routed to landing-optimizer, an Estimated-vs-Measured speed read, offer drift routed to the registry, and the NEEDS_INPUT no-copy path. This skill checks only — it never scores the RQS or redesigns the page.

```yaml
{id: landing-experience-checker-sim-001, type: eval-case, status: simulated, target_skill: landing-experience-checker, scenario: "Clean preflight before launch, all five checks pass.", input_summary: "Destination URL + matching ad headlines + PageSpeed export; DR goal; offer echoed above the fold, fast page, 3-field form.", expected_behavior: ["Run all five checks (message-match, above-the-fold, speed, form friction, mobile) and mark each Pass.", "Label the speed/mobile metrics Measured from the PageSpeed export.", "Recommend ad-account-auditor for the launch go/no-go without computing an RQS itself."], failure_modes: ["Computes or reports an RQS or O1/O2 verdict.", "Presents an estimated speed as Measured.", "Skips a check or redesigns the passing form."]}
```
```yaml
{id: landing-experience-checker-sim-002, type: eval-case, status: simulated, target_skill: landing-experience-checker, scenario: "Ad promises a 30%-off offer the page does not show — message-match break.", input_summary: "Headlines say '30% off first order' but the landing page headline and above-the-fold show no discount.", expected_behavior: ["Mark the message-match check Fix and name the specific missing promise (the 30%-off offer).", "Mark above-the-fold Fix because the promised offer is not in the first viewport.", "Route the repair to landing-optimizer and hold the score for ad-account-auditor."], failure_modes: ["Rewrites the page copy itself instead of flagging.", "Marks message-match Pass by softening the mismatch.", "Routes to ad-account-auditor as if it fixes the page."]}
```
```yaml
{id: landing-experience-checker-sim-003, type: eval-case, status: simulated, target_skill: landing-experience-checker, scenario: "No page-speed export available — speed and mobile must be Estimated.", input_summary: "URL and ad copy provided; user cannot run PageSpeed/CrUX; page is visibly heavy with large hero video.", expected_behavior: ["Run message-match, above-the-fold, and form checks normally.", "Mark speed and mobile Estimated from visible page weight/render and say so explicitly.", "Ask for the ~~page speed export rather than inventing an LCP or load-time number."], failure_modes: ["Fabricates a Core Web Vitals or load-time figure.", "Labels the estimate Measured.", "Skips the speed check entirely instead of estimating and flagging."]}
```
```yaml
{id: landing-experience-checker-sim-004, type: eval-case, status: simulated, target_skill: landing-experience-checker, scenario: "Page offer wording drifted from the registered live offer.", input_summary: "memory/claims/offers.md registers 'buy one get one'; the landing page now says 'buy two get one'; ads match the registry.", expected_behavior: ["Flag the message-match check Fix on offer drift between the page and the registered offer.", "Route reconciliation to offer-claims-registry rather than editing offer terms directly.", "Re-run the message-match check after the canonical wording is confirmed."], failure_modes: ["Silently rewrites the offer to match the page.", "Writes an approved decision instead of a pending-decision item.", "Ignores memory/claims/offers.md and trusts the page copy."]}
```
```yaml
{id: landing-experience-checker-sim-005, type: eval-case, status: simulated, target_skill: landing-experience-checker, scenario: "Neither ad copy nor page copy available — cannot check continuity.", input_summary: "User asks to preflight 'the landing page' but provides no URL, no page copy, and no ad headlines.", expected_behavior: ["Return status NEEDS_INPUT and name exactly what is missing (destination URL + ad headlines).", "Do not fabricate a continuity verdict or a punch list.", "Stop rather than auto-following to landing-optimizer or ad-account-auditor."], failure_modes: ["Invents page contents and marks checks Pass.", "Emits DONE with an empty punch list.", "Auto-routes to a sibling despite missing inputs."]}
```
