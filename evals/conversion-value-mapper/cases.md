Simulated seed cases for the conversion value-model builder — net-value/margin adjustment, proxy values for non-revenue actions, static-vs-dynamic rule choice, the value-vs-count reconciliation, and the missing-margin NEEDS_INPUT path.

```yaml
{id: conversion-value-mapper-sim-001, type: eval-case, status: simulated, target_skill: conversion-value-mapper, scenario: "Ecommerce account bids tROAS to gross revenue but margins vary by category.", input_summary: "GA4 purchase-value export + margin-by-category COGS export; tROAS goal; wants profit-based bidding.", expected_behavior: ["Set dynamic transaction value as the revenue basis and mark it Measured.", "Map net_value = revenue x per-category margin so tROAS bids to contribution not gross.", "Run the value-vs-count reconciliation and hand off to ad-account-auditor to score R1/R2."], failure_modes: ["Leaves values at gross revenue.", "Applies one blended margin without labeling it Estimated.", "Tries to score R1/R2 itself instead of handing off."]}
```
```yaml
{id: conversion-value-mapper-sim-002, type: eval-case, status: simulated, target_skill: conversion-value-mapper, scenario: "Lead-gen account with no transaction value needs proxy values for max-conversion-value.", input_summary: "Non-revenue conversions (lead, phone-call, signup); user provides AOV and lead-to-sale close rate.", expected_behavior: ["Derive proxy_value = avg_order_net_value x close_rate and show the derivation.", "Label each proxy Estimated and name its inputs.", "State the value model is launch-ready and recommend ad-account-auditor."], failure_modes: ["Assigns a round number with no derivation.", "Presents the proxy as a Measured value.", "Skips the close-rate and guesses."]}
```
```yaml
{id: conversion-value-mapper-sim-003, type: eval-case, status: simulated, target_skill: conversion-value-mapper, scenario: "No margin or COGS data provided at all.", input_summary: "GA4 purchase-value export present, but no COGS/margin breakdown and no blended rate.", expected_behavior: ["Mark net-value rows needs-input rather than guessing a 50% margin.", "Return status NEEDS_INPUT naming the missing COGS/margin export.", "Do not present a fabricated net value or a launch-ready verdict."], failure_modes: ["Defaults to an invented 50% margin.", "Emits DONE despite missing margin data.", "Passes gross revenue off as net value silently."]}
```
```yaml
{id: conversion-value-mapper-sim-004, type: eval-case, status: simulated, target_skill: conversion-value-mapper, scenario: "Platform-summed conversion value is ~3x the real booked contribution.", input_summary: "Value model built, but the value-vs-count reconciliation shows platform total value far exceeds actual net profit for the period.", expected_behavior: ["Flag the value-vs-count mismatch as a value-integrity blocker.", "Distinguish a value-model inflation (fix here) from a cross-platform double-count (route to attribution-reconciler).", "Promote the blocker to open-loops and return DONE_WITH_CONCERNS, not a launch-ready pass."], failure_modes: ["Reports the model as launch-ready despite the 3x gap.", "Runs cross-platform order-ID de-dup itself instead of routing to attribution-reconciler.", "Silently passes the inflated value."]}
```
```yaml
{id: conversion-value-mapper-sim-005, type: eval-case, status: simulated, target_skill: conversion-value-mapper, scenario: "Account bids to max-conversions (count), not a value goal, and a value rule double-adjusts an already-netted value.", input_summary: "User asks for a value model, but the campaign uses max-conversions with no value target; a location value rule sits on top of a margin-netted value.", expected_behavior: ["Note a value model is optional under a count goal and route back rather than over-building.", "If a value model is still wanted, flag the value-rule-on-netted-value double-adjust collision.", "Recommend the simplest single dynamic value with no redundant rule."], failure_modes: ["Builds a full value model for a count-only account with no caveat.", "Misses the double-adjustment where the value rule re-margins an already-net value.", "Adds speculative device/audience value rules with no supporting margin split."]}
```
