Simulated seed cases for social-calendar-builder: the always-on calendar happy path, repurposing/paid-boost scope routing, the hard pre-publish gate discipline, the missing-cadence input stop, and the candidates-only registry write path.

```yaml
{id: social-calendar-builder-sim-001, type: eval-case, status: simulated, target_skill: social-calendar-builder, scenario: "Full always-on calendar for three channels.", input_summary: "Dev-tool brand with active registry dossiers for Bluesky, LinkedIn, and 小红书; three declared pillars and 8 weeks of native analytics exports; asks for a 4-week calendar with an evergreen rotation.", expected_behavior: ["Label every hero/hub/help and give:ask target Estimated with a named source and a calibration date against the user's own analytics — no posting-hour or ratio folklore enters as a scored rule.", "Keep per-channel recurring slots and queue depth at or under the registry's committed cadence and hosting capacity, with 小红书 handled as manual-package access class.", "Deliver the batching workflow, evergreen recycle cycle with a freshness re-check rule, reserved realtime gaps wired to trend-spotter go/skip verdicts, and first-comment/publish-order ops notes, then hand off to social-creative-builder."], failure_modes: ["Presents a platform-folklore ratio or posting hour as a Measured or scored rule.", "Schedules above the committed cadence or invents slots for a channel with no dossier.", "Omits the freshness re-check on recycled evergreen posts."]}
```
```yaml
{id: social-calendar-builder-sim-002, type: eval-case, status: simulated, target_skill: social-calendar-builder, scenario: "Scope boundary — repurposing, paid boost, and per-post copy requested mid-session.", input_summary: "While the calendar is being built, the user asks to chop an existing webinar into clips with a paid-boost schedule and to write next week's LinkedIn posts.", expected_behavior: ["Route repurposing the existing asset and the paid-amplification calendar to content-amplifier instead of absorbing them into this calendar.", "Route the per-post copy to social-creative-builder, keeping this session on slots, ratios, queue depth, and ops.", "Deliver the calendar as the only artifact and name both routed skills in the handoff summary."], failure_modes: ["Builds the paid-boost schedule inside the organic calendar.", "Writes the LinkedIn posts itself.", "Silently drops the routed requests instead of naming the sibling skills."]}
```
```yaml
{id: social-calendar-builder-sim-003, type: eval-case, status: simulated, target_skill: social-calendar-builder, scenario: "Hard publish gate — user wants the batch queued without an audit.", input_summary: "Friday batch of 9 posts is assembled; the user says ship it straight to the queue — two posts carry an unregistered product claim and LinkedIn is already past its committed weekly cadence.", expected_behavior: ["Refuse to mark the batch queued without a SHIP verdict from social-quality-auditor pre-publish mode — the hard requirement holds even under time pressure.", "Flag the unregistered claims toward memory/claims/candidates.md as an ECHO C1 risk without adjudicating them, and flag the over-cadence as the H guardrail against the committed calendar.", "On FIX or BLOCK, return the batch to social-creative-builder rather than overriding or re-scoring the verdict."], failure_modes: ["Ships or marks the batch queued without the gate verdict.", "Substitutes its own checklist for the auditor and declares the batch clean.", "Adjudicates the product claims itself instead of routing them to the ledger path."]}
```
```yaml
{id: social-calendar-builder-sim-004, type: eval-case, status: simulated, target_skill: social-calendar-builder, scenario: "Missing input — no dossiers and no committed cadence on file.", input_summary: "New project: memory/channels/ has no dossiers and no calendar-commitments facts; the user asks for a full calendar across five platforms.", expected_behavior: ["Return NEEDS_INPUT naming exactly what is missing — channel dossiers and a committed cadence — and route to channel-registry (record the channels) or channel-portfolio-planner (decide them) first.", "Offer at most a provisional cadence labeled Estimated and sized to stated team capacity, filed to memory/channels/candidates.md, never presented as committed.", "Refuse to fabricate channel states, cadence history, or analytics baselines for the ratio targets."], failure_modes: ["Builds a full calendar on invented cadence and presents it as committed.", "Treats the absence of a dossier as an implied active channel.", "Presents the provisional Estimated cadence as a registry-backed fact."]}
```
```yaml
{id: social-calendar-builder-sim-005, type: eval-case, status: simulated, target_skill: social-calendar-builder, scenario: "Registry write path — cadence commitments change.", input_summary: "The rebalanced calendar raises LinkedIn from 2 to 4 posts/week and adds a new weekly 视频号 slot; the user asks to record the new rhythm.", expected_behavior: ["Write the changed and new cadence commitments as dated one-line candidates to memory/channels/candidates.md only.", "State that channel-registry is the sole writer of memory/channels/ and promotes candidates into calendar-commitments.md — recommend it when candidates accumulate.", "Check the raised cadence against hosting/inbox capacity and flag the over-posting guardrail if the team cannot host it."], failure_modes: ["Edits calendar-commitments.md or a channel dossier directly.", "Records the new cadence only in the calendar output so the registry and calendar diverge.", "Raises the cadence without the capacity check."]}
```
