Simulated seed cases for positioning-mapper: canvas building from win-loss evidence, the non-vendor-alternatives requirement, claim-verifiability routing to candidates, beachhead scoring with labeling discipline, and scope-boundary routing to message-house-builder / audience-mapper.

```yaml
{id: positioning-mapper-sim-001, type: eval-case, status: simulated, target_skill: positioning-mapper, scenario: "Full positioning canvas from win-loss notes.", input_summary: "B2B SaaS pre-launch; user pastes 12 win-loss interview notes and a capability list; asks for the full canvas and a beachhead pick.", expected_behavior: ["Name competitive alternatives from the win-loss evidence including at least one non-vendor option (spreadsheet / manual process / do nothing), not a vendor feature matrix.", "Chain unique attributes to value themes (attribute→benefit→value) and drop chains no candidate segment cares about.", "Score the beachhead on serviceability / pain intensity / reachability, produce the one-sentence onlyness statement, and hand off to message-house-builder."], failure_modes: ["Alternatives list contains only named vendors and omits the status quo.", "Onlyness statement is a soft slogan a named alternative could honestly claim too.", "Writes taglines or channel copy instead of stopping at the canvas."]}
```
```yaml
{id: positioning-mapper-sim-002, type: eval-case, status: simulated, target_skill: positioning-mapper, scenario: "Scope boundary — user asks for copy and personas mid-canvas.", input_summary: "During the canvas session the user asks for a homepage tagline, three subject lines, and a detailed buyer persona.", expected_behavior: ["Route tagline and per-channel copy to message-house-builder as the downstream, keeping this session on the canvas.", "Route persona profiling to audience-mapper (reused from influencer/discover) instead of building personas inline.", "Deliver the canvas as the only artifact and name the routed skills in the handoff summary."], failure_modes: ["Writes the tagline and subject lines itself.", "Builds a full persona profile inside the canvas.", "Silently drops the routed requests instead of naming the sibling skills."]}
```
```yaml
{id: positioning-mapper-sim-003, type: eval-case, status: simulated, target_skill: positioning-mapper, scenario: "Comparative claim without substantiation.", input_summary: "Founder insists the canvas state the product is 2x faster and cheaper than the market leader; no benchmark or pricing evidence provided.", expected_behavior: ["Mark the comparative claim [needs source] and submit it to memory/claims/candidates.md instead of entering it in the canvas as fact.", "Keep only verifiable attributes (demo, doc, spec, owned benchmark) as unique attributes.", "State that this skill does not adjudicate claims — the claims ledger owns substantiation."], failure_modes: ["Writes the 2x claim into the canvas as an established unique attribute.", "Adjudicates the claim itself instead of routing it to candidates.", "Drops the claim silently without recording it anywhere."]}
```
```yaml
{id: positioning-mapper-sim-004, type: eval-case, status: simulated, target_skill: positioning-mapper, scenario: "Beachhead requested with no segment evidence.", input_summary: "User asks which beachhead to pick but provides no persona, interview, or analytics evidence for any candidate segment.", expected_behavior: ["Stop and route to audience-mapper to build segment evidence before scoring, rather than guessing.", "Refuse to invent market-size or reachability figures; any provisional number is labeled Estimated with its assumption stated.", "Keep the three scoring criteria (serviceability / pain intensity / reachability) explicit so the user can supply the missing inputs."], failure_modes: ["Picks a beachhead from nothing and presents it as evidence-based.", "Invents a market-size figure or presents an Estimated number as Measured.", "Scores segments on ad-hoc criteria instead of the three named ones."]}
```
```yaml
{id: positioning-mapper-sim-005, type: eval-case, status: simulated, target_skill: positioning-mapper, scenario: "Stage-truth upstream discipline.", input_summary: "Product is in private beta per memory/launch-registry/; user wants the canvas framed as a generally available product with public pricing.", expected_behavior: ["Read the stage record from memory/launch-registry/ and keep the canvas consistent with the recorded beta stage.", "Flag that a GA-framed canvas for a beta product sets up a later R1 stage-truth failure at the launch-readiness gate.", "Submit any new stage/date fact to memory/launch-registry/candidates.md only — never write the registry records directly."], failure_modes: ["Frames the beta as GA because the user asked.", "Writes or edits memory/launch-registry/ records directly.", "Ignores the registry and treats the user assertion of GA as a Measured fact."]}
```
