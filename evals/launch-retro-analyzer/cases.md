Simulated seed cases for launch-retro-analyzer: the UTM-truth vs platform-reference table discipline, the single-largest-miss 5-Whys constraint, no post-hoc target back-filling, scope routing to roi-calculator / report-generator / performance-analyzer, and the candidates-only launch-registry write path.

```yaml
{id: launch-retro-analyzer-sim-001, type: eval-case, status: simulated, target_skill: launch-retro-analyzer, scenario: "Standard W1 retro with targets on file.", input_summary: "Dev-tool launch; pre-declared D0/W1 targets from launch-tier-planner + registry goal column; GA4 UTM export + HN/PH dashboards provided.", expected_behavior: ["Build the per-channel actual-vs-target table with the UTM-attributed export as the truth column (Measured) and platform numbers in a separate reference column.", "Run exactly one 5-Whys chain on the single largest miss; give every channel a keep/kill/change call with a one-line reason.", "Draft 3-5 actionable learnings and submit the outcome snapshot to memory/launch-registry/candidates.md; recommend momentum-planner."], failure_modes: ["Merges platform self-reported numbers into the truth column.", "Runs a 5-Whys on every missed row (retro paralysis).", "Writes memory/launch-registry/ records directly instead of candidates."]}
```
```yaml
{id: launch-retro-analyzer-sim-002, type: eval-case, status: simulated, target_skill: launch-retro-analyzer, scenario: "Measured/Estimated labeling discipline on conflicting numbers.", input_summary: "Platform dashboard claims 12k launch-day visits; the GA4 UTM export shows 8k attributed sessions; user asks which number goes in the retro.", expected_behavior: ["Use the UTM-attributed 8k as the truth figure labeled Measured; keep the platform 12k as a reference figure labeled as platform self-reported.", "Flag the discrepancy as a finding without averaging the two into one number presented as measured.", "Route a deep attribution reconciliation to performance-analyzer instead of adjudicating it in the retro."], failure_modes: ["Presents the platform self-reported number as the Measured truth.", "Averages or blends the two figures and labels the result Measured.", "Performs the full attribution deep-dive itself."]}
```
```yaml
{id: launch-retro-analyzer-sim-003, type: eval-case, status: simulated, target_skill: launch-retro-analyzer, scenario: "Scope boundary: ROI math and board report requested inside the retro.", input_summary: "User asks for the channel table plus launch CPA/ROI and a board-ready report document.", expected_behavior: ["Deliver the retro scope: actual-vs-target table, 5-Whys on the largest miss, keep/kill/change calls, learnings.", "Route the CPA/ROI return math to roi-calculator rather than computing it inline.", "Route the board-ready writeup to report-generator as a follow-up, keeping the retro output as its input."], failure_modes: ["Computes CPA/ROI/payback itself.", "Drafts the stakeholder-facing report document itself.", "Drops the retro deliverables to chase the adjacent requests."]}
```
```yaml
{id: launch-retro-analyzer-sim-004, type: eval-case, status: simulated, target_skill: launch-retro-analyzer, scenario: "No pre-declared targets on file.", input_summary: "Launch shipped without KPI targets; no launch-tier-planner output and no goal column in the registry record; user asks how the launch did.", expected_behavior: ["Ask for targets and label anything reconstructed as User-provided (post-hoc); do not back-fill targets as if they were declared pre-launch.", "Compare channels against the user's own trailing rates from prior launches when available, never an invented industry benchmark.", "Mark the retro NEEDS_INPUT (or DONE_WITH_CONCERNS with the gap named) when the baseline cannot be established."], failure_modes: ["Invents targets or states an absolute benchmark like a good launch gets X signups.", "Presents reconstructed post-hoc targets as pre-declared.", "Scores the launch as pass/fail with no baseline at all."]}
```
```yaml
{id: launch-retro-analyzer-sim-005, type: eval-case, status: simulated, target_skill: launch-retro-analyzer, scenario: "5-Whys with platform folklore in the chain.", input_summary: "Biggest miss was the HN submission; user asserts the flop happened because we posted at the wrong hour and wants that logged as the root cause.", expected_behavior: ["Keep posting-hour/vote-velocity mechanics as an Estimated hypothesis inside the chain with a named source (community folklore, minimaxir/hacker-news-undocumented), never the confirmed root cause.", "Continue the 5-Whys past the folklore level toward a changeable cause the team controls.", "Submit the outcome snapshot to memory/launch-registry/candidates.md only, and mark any product/comparative claim in the narrative [needs source] for memory/claims/candidates.md."], failure_modes: ["Logs the posting-hour lore as a confirmed root cause or hard rule.", "Stops the chain at the folklore level with no changeable action.", "Adjudicates a comparative product claim inside the retro."]}
```
