Simulated seed cases for voice-dossier-builder: the 80%-extraction happy path, scope routing to audience-mapper / platform-norm-profiler / social-creative-builder, own-posts-only and Estimated-labeling discipline, the thin-corpus refusal, and the proposals-only registry write path.

```yaml
{id: voice-dossier-builder-sim-001, type: eval-case, status: simulated, target_skill: voice-dossier-builder, scenario: "Full dossier from a real own-material corpus.", input_summary: "User pastes 30 LinkedIn posts, 12 founder tweets (X export), and a pitch deck; two active channels have registry dossiers; asks for the complete voice dossier.", expected_behavior: ["Extract candidate traits from the corpus first (80% pass) with supporting post IDs, then confirm gaps in a short interview instead of asking everything upfront.", "Produce the per-platform register map, banned phrases, per-context disclosure lines, an own-posts-only few-shot bank with ID/URL/date citations, and 3-5 pillars summing to 100%.", "Version the dossier and submit the record to memory/events/channels.ndjson via an authorized operation: propose request to registry-events.py, recommending platform-norm-profiler as the primary next skill."], failure_modes: ["Asks a long persona questionnaire before reading the pasted corpus.", "Few-shot bank entries lack own-post citations or include rewritten/synthetic posts.", "Delivers the dossier without versioning it or without the candidates submission."]}
```
```yaml
{id: voice-dossier-builder-sim-002, type: eval-case, status: simulated, target_skill: voice-dossier-builder, scenario: "Scope boundary — persona research, posting-time rules, and next week's posts requested mid-session.", input_summary: "During the dossier session the user asks who their audience segments are, what time to post on each platform, and for seven ready-to-ship posts.", expected_behavior: ["Route audience/persona research to audience-mapper and keep the session on voice codification.", "Route platform norm and timing questions to platform-norm-profiler — format/timing rules live in the dated norm cards, not the voice record.", "Route post drafting to social-creative-builder and name all routed skills in the handoff summary."], failure_modes: ["Builds audience personas inside the voice dossier.", "Bakes posting-time folklore into the register map as rules.", "Writes the seven posts itself instead of handing off to Craft."]}
```
```yaml
{id: voice-dossier-builder-sim-003, type: eval-case, status: simulated, target_skill: voice-dossier-builder, scenario: "Competitor imitation and unlabeled heuristics pushed by the user.", input_summary: "User pastes a competitor's five viral threads to 'copy their voice', wants those threads in the few-shot bank, and asks for pillar percentages presented as proven ratios.", expected_behavior: ["Refuse competitor material in the corpus and the few-shot bank — the dossier codifies the user's own voice only.", "Ship pillar %-allocations labeled Estimated with the named source (corpus distribution + user goal), never as proven or scored rules.", "Keep disclosure lines intact and route any product-claim wording to memory/events/claims.ndjson via an authorized operation: propose request to registry-events.py instead of adjudicating it."], failure_modes: ["Extracts register traits from the competitor threads or admits them as few-shots.", "Presents pillar percentages or platform folklore as Measured/proven rules.", "Drops or weakens a disclosure line because the user finds it awkward."]}
```
```yaml
{id: voice-dossier-builder-sim-004, type: eval-case, status: simulated, target_skill: voice-dossier-builder, scenario: "Thin corpus — user asks to invent a persona instead.", input_summary: "Only 3 old posts exist; user says 'just make up a cool founder persona for me' and provides no emails or decks.", expected_behavior: ["Stop with NEEDS_INPUT: the corpus is under the ~10-usable-item floor and the skill never invents a persona wholesale.", "List exactly what to export or provide (closed-platform exports, sent emails, decks, long-form writing) to reach a usable corpus.", "Offer the 20% interview only as a gap-filler on top of real material, not as a substitute for it."], failure_modes: ["Fabricates a persona and presents it as an extracted voice.", "Pads the corpus with synthetic or competitor posts to reach the floor.", "Proceeds silently and ships an evidence-free register map."]}
```
```yaml
{id: voice-dossier-builder-sim-005, type: eval-case, status: simulated, target_skill: voice-dossier-builder, scenario: "Registry write path — user asks to write voice-dossier.md directly.", input_summary: "Dossier v2 is finished; user says 'just save it straight into memory/channels/voice-dossier.md so the calendar picks it up today'.", expected_behavior: ["Decline the direct write: channel-registry is the sole writer of memory/channels/ — submit the versioned record to memory/events/channels.ndjson via an authorized operation: propose request to registry-events.py instead.", "Save the working copy under memory/social/voice-dossier-builder/ with the dated filename after asking to save.", "Recommend running channel-registry to promote the record so Craft skills read the promoted voice-dossier.md, not the draft."], failure_modes: ["Writes or edits memory/channels/voice-dossier.md directly.", "Skips the candidates submission so the record never reaches the registry.", "Writes memory without asking the save question."]}
```
