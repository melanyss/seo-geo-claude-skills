Simulated seed cases for launch-readiness-auditor: the R1 NEEDS_INPUT-not-pass-by-default rule when no launch-registry stage record exists, single-veto min(raw,60) capping with veto-ID-free user-facing output, the 2+ veto BLOCKED path that omits final_overall_score, goal-column confirmation across the three RAMP weight columns, and the T-1 go/no-go mode with the folklore-is-never-a-veto guardrail.

```yaml
{id: launch-readiness-auditor-sim-001, type: eval-case, status: simulated, target_skill: launch-readiness-auditor, scenario: "Full RAMP audit with no launch-registry stage record on file.", input_summary: "User pastes a launch plan + asset manifest announcing 'GA next week'; memory/launch-registry/ has no record for the slug; devtool goal.", expected_behavior: ["Mark R1 and the stage-truth sub-item NEEDS_INPUT — never pass-by-default and never infer the stage from the announcement copy itself.", "Ask for the registry record (or a stage claim + verifiable evidence: public access, live pricing page) before judging R1.", "Score the remaining dimensions with whatever is present, mark missing inputs N/A/NEEDS_INPUT with reason, and route the stage fix to launch-registry."], failure_modes: ["Passes R1 by default because nothing contradicts the GA claim.", "Infers the launch stage from the announcement wording instead of the registry record.", "Records a stage in launch-registry itself instead of routing to the owning skill."]}
```
```yaml
{id: launch-readiness-auditor-sim-002, type: eval-case, status: simulated, target_skill: launch-readiness-auditor, scenario: "Single veto fires — tracking missing on a launch surface (P1).", input_summary: "B2B SaaS launch scoring raw LQS 76; the announcement blog and PH listing carry no UTMs and the signup conversion event is unverified.", expected_behavior: ["Cap the overall at min(76, 60) = 60 with cap_applied: true and floor rounding; verdict FIX, not BLOCK, for a single veto.", "Keep the handoff YAML fields (cap_applied, raw_overall_score: 76, final_overall_score: 60) in the artifact only — the user-visible report shows plain findings, one score, and the verdict.", "Translate the veto per the RAMP rows ('Launch traffic will not be measurable — tracking is missing on one or more launch surfaces') and route the fix to launch-monitor's instrumentation spec, then re-audit."], failure_modes: ["Prints 'P1 failed', cap_applied, or the raw→capped delta in the user-facing report.", "Treats the cap as a floor or averages raw and capped instead of min(raw, 60).", "Escalates one veto to BLOCKED or leaves the score uncapped."]}
```
```yaml
{id: launch-readiness-auditor-sim-003, type: eval-case, status: simulated, target_skill: launch-readiness-auditor, scenario: "Two vetoes fire — unsubstantiated comparative claim (A1) plus a planned vote-solicitation DM blast (M1).", input_summary: "Launch copy claims '3x faster than [competitor]' with no entry in memory/claims/claims-ledger.md; the runbook schedules DMs asking the community to upvote at launch hour.", expected_behavior: ["Set status: BLOCKED for 2+ vetoes; retain raw_overall_score, omit final_overall_score entirely, cap_applied: false.", "State the BLOCK verdict in plain language at the top with the two red-line findings translated — no veto IDs anywhere user-visible.", "Route each fix to its owner (A1 → offer-claims-registry, M1 → community-launch-runner) and require the vetoes cleared + a re-audit before the date is committed."], failure_modes: ["Emits a final_overall_score on a BLOCKED audit or 'caps' it to 60.", "Adjudicates the claim itself instead of routing to offer-claims-registry.", "Downgrades vote solicitation to a guardrail because the DMs are 'just to friends'."]}
```
```yaml
{id: launch-readiness-auditor-sim-004, type: eval-case, status: simulated, target_skill: launch-readiness-auditor, scenario: "Goal column ambiguous — same dimension scores, different LQS per weight column.", input_summary: "User says 'audit our launch' with no goal stated; dimensions come out R=80 A=75 M=70 P=78; product could plausibly be read as B2B SaaS or dev-tool.", expected_behavior: ["Confirm the goal column with the user up front (B2B SaaS / sales-led vs Dev-tool / community vs Mobile / app-store) instead of silently picking one.", "Compute LQS = floor(weighted) with the confirmed column and state the column used in the report — the fixture vector yields 76 (B2B), 75 (dev-tool), 76 (mobile), so the choice is load-bearing.", "Use math.floor throughout; never present the LQS as a single launch KPI like upvotes or signups."], failure_modes: ["Silently assumes a goal column without confirming or stating it.", "Averages the four dimensions unweighted or rounds instead of flooring.", "Reports different-column scores interchangeably as if the weights don't matter."]}
```
```yaml
{id: launch-readiness-auditor-sim-005, type: eval-case, status: simulated, target_skill: launch-readiness-auditor, scenario: "T-1 launch-eve go/no-go with one unchecked item plus community-folklore pressure.", input_summary: "Date is tomorrow; runbook, kit, and registry record all clean except embargo lift times are unconfirmed with one outlet; user also worries their account 'lacks the karma threshold' folklore says is needed to rank.", expected_behavior: ["Run the fast go/no-go checklist mode (not the full LQS) and return no-go because a checklist item — embargo lift confirmation — is unchecked; any single unchecked item is a no-go.", "Treat the karma-threshold lore as Estimated context with a named source, never as a veto or no-go basis — M1 fires only on published platform rules, manipulation, or embargo breach.", "State that launch-day-conductor hard-requires a SHIP from this gate before T-0, and name the exact unchecked item to fix."], failure_modes: ["Issues a go with an unchecked checklist item because 'everything else passes'.", "Fails M1 or blocks the launch over an undocumented community norm (karma ladder, posting hour).", "Runs the full four-dimension LQS audit when the T-1 fast mode was asked for."]}
```
