Simulated seed cases for conversion-signal-qa — the pre-flight checklist + UTM/event-spec builder. Covers a clean launch-ready pass, a fail (events not firing), the dedup/attribution-window double-count fix, a NEEDS_INPUT missing-export case, and a BLOCKED/guardrail case where the skill is asked to score `R1`/`R2` (out of scope — that is the auditor's job).

```yaml
{id: conversion-signal-qa-sim-001, type: eval-case, status: simulated, target_skill: conversion-signal-qa, scenario: "Clean account, plumbing is launch-ready.", input_summary: "GA4 Conversions + Traffic-acquisition exports provided; a manual test conversion was run and appears with correct event/value/source; UTMs consistent; one platform.", expected_behavior: ["Mark every pre-flight item pass with evidence.", "Emit the filled UTM scheme + conversion-event spec.", "State launch-ready and hand off to ad-account-auditor to score R1/R2."], failure_modes: ["Passes items with no test-conversion evidence.", "Claims to have scored R1/R2 itself.", "Skips writing the UTM/event spec."]}
```
```yaml
{id: conversion-signal-qa-sim-002, type: eval-case, status: simulated, target_skill: conversion-signal-qa, scenario: "Conversion events are not firing.", input_summary: "Campaign spend present but the GA4 Conversions export shows zero conversions and the user ran a test purchase that never appeared.", expected_behavior: ["Mark the event-firing item FAIL (test conversion run but absent).", "Report fix-first, naming the broken event as the blocker.", "Promote the blocker to open-loops and recommend re-running QA, then the auditor."], failure_modes: ["Treats a run-but-absent test as iOS-ATT modeled and only flags.", "Reports launch-ready anyway.", "Tries to assign an R1 veto score."]}
```
```yaml
{id: conversion-signal-qa-sim-003, type: eval-case, status: simulated, target_skill: conversion-signal-qa, scenario: "Same orders double-counted across Meta and Google; mismatched windows.", input_summary: "GA4/ecommerce order IDs reconcile to fewer unique orders than Meta + Google self-report combined; platforms use different attribution windows.", expected_behavior: ["Name GA4/ecommerce order IDs as the single source of truth and quantify the inflation.", "State the dedup rule and align attribution windows/currency/timezone before matching.", "Report fix-first and hand the cleaned spec to ad-account-auditor to score R2."], failure_modes: ["Trusts platform self-reported counts.", "Compares 7-day-click to 1-day-view as equal.", "Declares the R2 veto pass/fail instead of just fixing the signal."]}
```
```yaml
{id: conversion-signal-qa-sim-004, type: eval-case, status: simulated, target_skill: conversion-signal-qa, scenario: "Missing GA4 Traffic-acquisition export — UTM hygiene cannot be verified.", input_summary: "User provides the GA4 Conversions export but not the Traffic-acquisition (source/medium) export and ran no test conversion.", expected_behavior: ["Set the UTM-hygiene and test-conversion items to NEEDS_INPUT, not pass.", "Return status NEEDS_INPUT and name the missing export + the test conversion to run.", "Do not fabricate UTM findings or a launch-ready verdict."], failure_modes: ["Marks UTM items pass with no source/medium data.", "Emits DONE despite missing export.", "Invents source/medium rows."]}
```
```yaml
{id: conversion-signal-qa-sim-005, type: eval-case, status: simulated, target_skill: conversion-signal-qa, scenario: "User asks this skill to score the R1/R2 vetoes and produce the RQS verdict.", input_summary: "User says 'just score R1 and R2 and tell me SHIP or BLOCK' instead of asking to build/fix the signal.", expected_behavior: ["Hold the scope boundary: this skill builds/fixes the pre-flight, it does not score R1/R2 or emit RQS.", "Run the pre-flight + spec, then route scoring to ad-account-auditor.", "Do not emit a SHIP/FIX/BLOCK verdict or a final_overall_score."], failure_modes: ["Produces an RQS or SHIP/FIX/BLOCK verdict.", "Assigns veto pass/fail as if it were the auditor.", "Refuses to do the in-scope pre-flight work."]}
```
