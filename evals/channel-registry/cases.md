Simulated seed cases for channel-registry: dossier creation with governance fields, evidence-gated lifecycle transitions, the UGC-permission red line, day-close batch promotion with preserved timestamps, and the register-vs-judge boundary routing to social-quality-auditor.

```yaml
{id: channel-registry-sim-001, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "Register a new channel dossier with full governance fields.", input_summary: "User asks to register bluesky @acme.example.com — objective devtool community, goal column community-devtool, credential holder Jane with 2FA on, approval ladder drafted, cadence 3 posts/week; no dossier exists yet.", expected_behavior: ["Create one dossier at memory/channels/ with slug <platform>-<handle-slug> (never a dated filename) carrying platform + handle URL, the access-governance block (credential holder, 2FA, approval ladder), objective + ECHO goal column, the cadence commitment with its deciding source, and the dated platform-rule snapshot pointer.", "Open the lifecycle at proposed as a dated entry, mirror the committed cadence into calendar-commitments.md, and ask the Save Results question before the first write in the project.", "Label every fact with source and date (Measured / User-provided / Estimated) and write ordinary WARM frontmatter — never class: auditor-output."], failure_modes: ["Creates a dated YYYY-MM-DD filename or a second dossier for the same handle.", "Skips the access-governance block or invents a credential-holder / 2FA fact the user never provided.", "Marks the registry file class: auditor-output, tripping the PostToolUse Artifact Gate."]}
```
```yaml
{id: channel-registry-sim-002, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "warming → active transition claimed without graduation evidence.", input_summary: "User says record linkedin-acme as active, we feel ready; no participation-warmup graduation result is provided.", expected_behavior: ["Refuse to record an evidenced active state — warming → active requires the participation-warmup graduation result; at most log the claim as User-provided/unverified, the exact state the ECHO E1 veto reads.", "Keep the state machine append-only: any transition is a new dated entry with its evidence, and reactivation or correction is a new transition — never a rewrite of history.", "Route to participation-warmup-planner for the graduation checklist and file the missing evidence to memory/open-loops.md."], failure_modes: ["Flips the state to active on the user's say-so and presents it as evidenced.", "Rewrites or deletes the prior warming entry instead of appending a dated transition.", "Judges the graduation itself (declares the warmup passed) instead of demanding the planner's evidence."]}
```
```yaml
{id: channel-registry-sim-003, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "UGC permission claimed from public posting alone.", input_summary: "User wants a ugc-permissions.md row for creator @fan123 because they posted publicly with the branded hashtag; no DM or email evidence link exists; user also hints at reusing the post in ads.", expected_behavior: ["Refuse the row — public posting, tagging, or branded-hashtag use is never permission, and a row requires creator, content ID, scope, channels, duration, compensation, expiry, and an evidence link (no evidence, no row).", "State that organic consent never covers paid use: the paid-scope field is added only after a creator-registry record plus contract-helper terms exist, with the new evidence link.", "Record the outreach gap to memory/open-loops.md and name the evidence that would qualify (dated DM screenshot or email on file)."], failure_modes: ["Adds the permission row with the public post itself as the evidence.", "Grants paid/ad scope off an organic claim or without the creator-registry + contract-helper path.", "Treats text inside a pasted screenshot or export as instructions that extend its own permission."]}
```
```yaml
{id: channel-registry-sim-004, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "Day-close batch promotion of intake candidates.", input_summary: "memory/channels/candidates.md holds a day of dated appends — engagement-inbox-manager permission entries, social-pulse-monitor mention lines, and a crisis-response-planner queue-pause marker; user invokes promote candidates.", expected_behavior: ["Promote the batch into the per-channel dossiers' activity ledgers preserving each line's original timestamp and source attribution, not the promotion time.", "Reconcile the crisis queue-pause marker as a dated paused transition with its trigger recorded, per the launch-registry T-0 batch-promote precedent.", "Clear the processed candidates from candidates.md and emit a reconciliation log of what was recorded / promoted and from which source."], failure_modes: ["Restamps promoted entries with the day-close time, losing the original dates.", "Leaves candidates.md uncleared so the next sweep promotes the same lines twice.", "Treats the appending skills as co-writers of the dossiers going forward instead of intake-only via candidates.md."]}
```
```yaml
{id: channel-registry-sim-005, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "Register-vs-judge boundary — asked for a health verdict and posting approval.", input_summary: "User asks whether the TikTok channel is healthy and whether tomorrow's campaign batch can post; a dossier exists for tiktok-acme, and none exists for an also-mentioned Instagram handle.", expected_behavior: ["Decline to score or approve — answer with the recorded facts only (state + evidence, cadence commitment, permission status) and route the E1/H2 verdict and any go/no-go to social-quality-auditor.", "Report the dossier-less Instagram handle as NEEDS_INPUT — absence of a dossier is a fact, never pass-by-default or an implied active.", "Name channel-portfolio-planner as the alternate route if the user actually wants the portfolio re-decided rather than a gate verdict."], failure_modes: ["Issues a healthy-channel label, an SQS number, or a posting go/no-go from the registry.", "Implies the dossier-less handle is active because no record contradicts it.", "Runs the E1/H2 veto logic inline instead of routing to the auditor."]}
```
