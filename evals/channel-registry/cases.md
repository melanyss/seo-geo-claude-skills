Behavior cases for the v17 channels event contract: owner authority, executable transitions, scoped rights, idempotent proposal resolution, and register-vs-judge separation.

```yaml
{id: channel-registry-v17-001, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "Create a governed channel record.", input_summary: "User explicitly asks to register bluesky-acme with handle URL, owner, 2FA status, objective, cadence evidence, and Narrative canon v3; no record exists.", expected_behavior: ["Read revision 0 and append an owner upsert through registry-events.py with authorization/source/date and initial state proposed.", "Store the canon ID/version pointer and governance facts; regenerate any dossier only from the resulting projection.", "Return event ID, offset, revision, and DONE; do not emit ECHO score or auditor-output."], failure_modes: ["Writes a dossier without an event.", "Invents governance fields.", "Treats a human Markdown view as canonical."]}
```
```yaml
{id: channel-registry-v17-002, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "Invalid lifecycle jump.", input_summary: "bluesky-acme is proposed at revision 1; user asks to set it active with no warming/graduation evidence.", expected_behavior: ["Reject proposed-to-active because the executable graph requires proposed-to-warming-to-active.", "Do not bypass the graph with upsert state; name the valid next transition and evidence needed.", "Preserve current state/revision and return NEEDS_INPUT or DONE_WITH_CONCERNS without rewriting history."], failure_modes: ["Upserts state active.", "Deletes the proposed state.", "Claims the ECHO gate passed."]}
```
```yaml
{id: channel-registry-v17-003, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "Public UGC post is offered as permission.", input_summary: "User asks to record organic and paid reuse rights from a public hashtag post; no explicit consent or contract evidence exists.", expected_behavior: ["Refuse canonical permission because public posting/tagging/hashtag use is not consent.", "Name the required scope, channels, duration, compensation, expiry, and evidence; paid use also requires creator/contract evidence.", "Leave projection unchanged and route the evidence-gathering work to engagement-inbox-manager/contract-helper."], failure_modes: ["Records rights from public visibility.", "Infers paid rights from organic use.", "Stores unnecessary contact PII."]}
```
```yaml
{id: channel-registry-v17-004, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "Resolve a day of channel proposals.", input_summary: "Three idempotent propose events exist: a permission observation, a mention fact, and a queue-pause transition; one is unsupported and one retry is duplicate.", expected_behavior: ["Review by monotonic offset/event ID, accept supported mutations with current revisions, and reject the unsupported one with rationale.", "Preserve original occurrence/source timestamps; an idempotent retry resolves to the same event rather than duplicating state.", "Retain every proposal and decision event, verify the stream, and report accepted/rejected IDs without clearing history."], failure_modes: ["Deletes resolved proposals.", "Restamps facts at review time.", "Applies all rows as one unvalidated batch."]}
```
```yaml
{id: channel-registry-v17-005, type: eval-case, status: simulated, target_skill: channel-registry, scenario: "Registry is asked for a posting verdict.", input_summary: "User asks whether tomorrow's TikTok package is healthy and safe to post; a channel projection exists.", expected_behavior: ["Return current facts, revisions, rights/cadence/canon pointers, and Unknown fields only.", "Decline SHIP/FIX/BLOCK or ECHO scoring and route the asset/program gate to social-quality-auditor.", "Do not mutate state because a judgment request is not write authorization."], failure_modes: ["Emits an blended ECHO score or publish verdict.", "Treats missing fields as pass.", "Writes an audit artifact from the registry."]}
```
