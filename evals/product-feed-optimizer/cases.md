Simulated seed cases for product-feed-optimizer: disapproval triage, title/attribute rewriting, price/availability hygiene against the landing page, claim/policy flagging (O1/O2 handoff, not scoring), and the NEEDS_INPUT missing-feed path.

```yaml
{id: product-feed-optimizer-sim-001, type: eval-case, status: simulated, target_skill: product-feed-optimizer, scenario: "Feed has disapprovals plus missing required attributes.", input_summary: "Merchant Center TSV export + diagnostics list; several items disapproved for missing GTIN and one for price mismatch; DR goal.", expected_behavior: ["Triage each disapproved item to a named cause and fix before rewriting titles.", "Produce a per-item attribute-completeness map naming the missing required fields (gtin, brand, condition).", "Hand off to ad-account-auditor for the O1/O2 veto scoring; do not compute an RQS."], failure_modes: ["Rewrites titles on still-disapproved items and calls it done.", "Fabricates a GTIN or google_product_category to fill a gap.", "Computes or reports an RQS score itself."]}
```
```yaml
{id: product-feed-optimizer-sim-002, type: eval-case, status: simulated, target_skill: product-feed-optimizer, scenario: "Feed price/availability disagrees with the landing page.", input_summary: "Feed lists price 49.00 and availability in_stock, but the destination landing page shows 59.00 and out of stock.", expected_behavior: ["Flag the price and availability mismatch against the landing-page truth set.", "Do NOT silently rewrite the feed price to match; report both values and the disapproval risk.", "Route the landing-page reconciliation to landing-optimizer if the page is the wrong source."], failure_modes: ["Overwrites feed price to 59.00 without flagging.", "Ignores the availability mismatch.", "Treats the mismatch as a policy veto and BLOCKs instead of flagging + handing off."]}
```
```yaml
{id: product-feed-optimizer-sim-003, type: eval-case, status: simulated, target_skill: product-feed-optimizer, scenario: "Titles are keyword-stuffed and a description carries an unsubstantiated claim.", input_summary: "Long stuffed titles over the platform character limit; one description says #1 rated and clinically proven with no source; prospecting goal.", expected_behavior: ["Rewrite titles to a front-loaded pattern within the platform character limit.", "Flag the superlative/health claims as O1 and check the claims projection for accepted wording before shipping.", "Mark unsourced claims [needs source] and submit an authorized operation: propose request through registry-events.py rather than deleting silently."], failure_modes: ["Keeps titles over the character limit.", "Ships the unsubstantiated claim unflagged.", "Invents a rating figure to substantiate the claim."]}
```
```yaml
{id: product-feed-optimizer-sim-004, type: eval-case, status: simulated, target_skill: product-feed-optimizer, scenario: "Clean feed — group approved products into feed-driven asset/listing groups.", input_summary: "Feed passes diagnostics with all required attributes; user wants PMax listing groups keyed on product_type and brand; DR goal.", expected_behavior: ["Confirm attribute completeness, then build a listing-group tree keyed on real feed fields (product_type, brand, custom labels).", "Note which segments carry disapproval risk, if any.", "Recommend ad-account-auditor as the next skill to score the Offer dimension."], failure_modes: ["Groups on fields not present in the feed.", "Skips straight to scoring instead of handing off to the auditor.", "Rewrites titles that were already compliant, bloating the diff."]}
```
```yaml
{id: product-feed-optimizer-sim-005, type: eval-case, status: simulated, target_skill: product-feed-optimizer, scenario: "Neither a feed export nor a diagnostics list is provided.", input_summary: "User asks to fix Shopping disapprovals but supplies no feed file, no diagnostics, and no landing page.", expected_behavior: ["Return status NEEDS_INPUT and name the missing feed export and disapproval list.", "Do not fabricate feed rows, disapproval causes, or attribute values.", "State exactly which own-data export is required to proceed."], failure_modes: ["Invents a plausible feed and audits it.", "Proceeds with a generic checklist as if it had data.", "Emits DONE despite having no product data."]}
```
