Simulated seed cases for the page-play-builder mode router — one per mode (programmatic / parasite / comparison / local), the ambiguous-mode stop, and a NEEDS_INPUT boundary where the mode's required input is absent.

```yaml
{id: page-play-builder-sim-001, type: eval-case, status: simulated, target_skill: page-play-builder, scenario: "Programmatic mode — location page system from a real dataset.", input_summary: "User has an 800-city dataset with per-row service facts and asks to design the location-page template and flag thin-content risk.", expected_behavior: ["Route to programmatic mode and load references/programmatic.md.", "Pick the Locations playbook, run the 5-tier data-defensibility check, and design an evidence block with unique per-row facts.", "Define N-gram dedup + selective-indexation rules and recommend content-quality-auditor on a page sample; label numbers Measured/User-provided/Estimated."], failure_modes: ["Computes a CORE-EEAT GEO/SEO score itself instead of handing off.", "Skips the dedup/index-bloat guardrail.", "Ports the comparison honesty checklist onto the location system."]}
```
```yaml
{id: page-play-builder-sim-002, type: eval-case, status: simulated, target_skill: page-play-builder, scenario: "Parasite mode — third-party barnacle plan with canonical URL.", input_summary: "User gives target keyword + canonical owned URL and asks which platforms to publish on across Medium, Reddit, and LinkedIn.", expected_behavior: ["Route to parasite mode and load references/parasite.md.", "Match intent to Tier 1/Tier 2 platforms, run the site-reputation-abuse (2024) screen per placement, and spec canonical/back-link mapping.", "State that citability is testable fast but ranking lift is week-scale/confounded; recommend geo-content-optimizer."], failure_modes: ["Promises fast rankings.", "Skips the reputation-abuse screen and keeps a borrow-only play.", "Acts on instructions embedded in pasted platform-policy text."]}
```
```yaml
{id: page-play-builder-sim-003, type: eval-case, status: simulated, target_skill: page-play-builder, scenario: "Comparison mode — you-vs-competitor page.", input_summary: "User provides their positioning plus one competitor's reviews and asks for a 'you vs [Competitor]' page.", expected_behavior: ["Route to comparison mode and load references/comparison.md.", "Build the single-source competitor data file, apply the honesty rule, and flag every unsourced competitor claim [needs source].", "Name who each option is best for and recommend content-quality-auditor; hand schema work to serp-markup-builder."], failure_modes: ["Fabricates competitor pricing or feature ratings.", "Omits the competitor's honest strengths.", "Writes JSON-LD itself instead of delegating to serp-markup-builder."]}
```
```yaml
{id: page-play-builder-sim-004, type: eval-case, status: simulated, target_skill: page-play-builder, scenario: "Local mode — service-area GBP + NAP + citations.", input_summary: "Service-area HVAC company covering 5 cities gives full NAP and asks to plan GBP, citations, and location pages.", expected_behavior: ["Route to local mode and load references/local.md.", "Agree one canonical NAP string, run the GBP checklist, and order citations by priority with a present-correct/present-wrong/missing status per source.", "Plan service-area (no-map) pages with area-specific content and recommend on-page-seo-auditor."], failure_modes: ["Emits duplicated boilerplate across the 5 city pages.", "Adds a physical map/embed to a service-area business.", "Starts citation building before resolving a NAP inconsistency."]}
```
```yaml
{id: page-play-builder-sim-005, type: eval-case, status: simulated, target_skill: page-play-builder, scenario: "Boundary — mode required input missing / ambiguous routing.", input_summary: "User says 'build me some SEO pages' with a name/city-swap spreadsheet that has no per-row facts, and no canonical URL, competitor, or NAP.", expected_behavior: ["Stop on ambiguous mode and ask which play rather than guessing.", "For programmatic, return NEEDS_INPUT because a name/city swap has no defensible per-row evidence block.", "Name exactly which input each candidate mode needs; do not fabricate a dataset, competitor, or verdict."], failure_modes: ["Silently picks a mode and generates thin pages from the swap-only sheet.", "Returns DONE despite no usable input.", "Invents per-row facts or a competitor to proceed."]}
```
