```yaml
{id: routing-prove-campaign-value-roi-calculator-001, type: eval-case, status: simulated, target_skill: roi-calculator, scenario: "User supplies spend and revenue and asks what the return was, not for a finished report.", input_summary: "What was our ROAS on the Q2 influencer campaign? We spent $25K and made $72K.", expected_behavior: ["Route to roi-calculator because the user supplied spend plus results and wants ROI/ROAS computed.", "Compute direct ROI/ROAS with inputs and formula shown rather than narrating a methodology.", "Do not hand off to report-generator yet — building the full slide/written report is its job, not this one."], failure_modes: ["Routes to report-generator before any ROI math is computed.", "Routes to performance-analyzer instead of calculating ROI from the supplied numbers.", "Answers a ROAS figure with no formula or benchmark status."]}
```
```yaml
{id: core-direct-roi-and-emv-roi-calculator-001, type: eval-case, status: simulated, target_skill: roi-calculator, scenario: "Full campaign with spend breakdown, reach, impressions, conversions, and revenue available; user wants the numbers proven.", input_summary: "Calculate ROI for our beauty campaign: $40K spend, $150K revenue, 3.2M impressions, 1,200 conversions, 800 new customers.", expected_behavior: ["Compute direct ROI = (Revenue - Investment)/Investment x 100 and ROAS = Revenue/Investment with inputs and formula shown.", "State each headline metric against a benchmark with a pass/fail status (e.g. ROAS vs the 3:1 Beauty/Skincare benchmark) and add EMV plus cost-efficiency metrics (CPM, CPA, CAC).", "Write a bottom-line assessment (profitable/break-even/loss) and 1-3 recommendations, then save to memory/influencer/roi-calculator/YYYY-MM-DD-<topic>.md and promote headline numbers to hot-cache.", "Mark EMV as an estimate for directional comparison only, per the EMV caveat."], failure_modes: ["Reports a single ROI number with no formula, benchmark, or pass/fail status.", "Omits the bottom-line assessment and recommendations required by Done-when.", "Presents EMV as an absolute measured return instead of a directional estimate.", "Skips the memory write or hot-cache promotion of headline numbers."]}
```
```yaml
{id: edge-missing-investment-roi-calculator-001, type: eval-case, status: simulated, target_skill: roi-calculator, scenario: "User wants ROI but only provides revenue and reach — no spend breakdown, so no methodology can be computed honestly.", input_summary: "We pulled in $90K and reached 1.8M people. What's our influencer ROI?", expected_behavior: ["Stop and ask for the total investment / spend breakdown since ROI, ROAS, and every cost-efficiency metric divide by investment.", "Declare NEEDS_INPUT rather than inventing a spend figure or returning a fabricated ROI.", "Offer to proceed with a clearly-labeled estimate (e.g. assumed spend) only if the user confirms, flagging the result DONE_WITH_CONCERNS if assumptions are used."], failure_modes: ["Fabricates an investment number to force an ROI calculation.", "Returns ROI/ROAS while silently treating spend as zero or guessing it.", "Proceeds to completion without flagging the missing input as NEEDS_INPUT."]}
```
```yaml
{id: framework-cvi-pending-veto-roi-calculator-001, type: eval-case, status: simulated, target_skill: roi-calculator, scenario: "User asks for the C3 Campaign Value Index but only ROI data exists; a content compliance veto is also flagged.", input_summary: "Give me the CVI for this campaign — ROI looks strong, but legal flagged one post for a missing FTC disclosure.", expected_behavior: ["Score ROI 0-100 (Return/Orchestration/Impact) per the c3 roi-campaign benchmark and emit it as the ROI scope score.", "Because ACE (fit-scorer) and ART (content-reviewer) scope scores are unavailable, emit the ROI score and mark CVI pending (needs ACE/ART) instead of guessing the missing scopes.", "Surface the content compliance issue as an ART T1/T2 veto that caps the CVI rollup rather than averaging it away — do not let a strong ROI mask the blocked scope."], failure_modes: ["Computes a CVI from ROI alone by assuming or inventing ACE/ART values.", "Averages the flagged disclosure veto into the rollup instead of capping it.", "Reports CVI as final when ACE and ART are absent."]}
```
