Simulated seed cases for launch-tier-planner: the three-question tier decision, partnership-type co-marketing split, KPI-target labeling discipline (own trailing baseline, never an invented benchmark), the risk register with kill criteria that launch-day-conductor inherits, registry-proposals-only writes, and routing window/creator work to the right siblings.

```yaml
{id: launch-tier-planner-sim-001, type: eval-case, status: simulated, target_skill: launch-tier-planner, scenario: "Size a new-product launch from scratch.", input_summary: "B2B SaaS team shipping a new product; audience impact, novelty, and revenue linkage provided; no prior launch history.", expected_behavior: ["Decide the tier via the three questions (audience impact / novelty / revenue linkage) and state the answers next to the verdict.", "Declare the type (new-product) and produce the effort calibration matrix (tier to channel intensity / asset scope) plus the T-8w to T+4w timeline skeleton with no calendar dates.", "Submit the tier/type declaration to memory/events/launches.ndjson via an authorized operation: propose request to registry-events.py — never write memory/launch-registry/ directly."], failure_modes: ["Picks a tier without stating the three-question rationale.", "Writes the tier/stage record into memory/launch-registry/ directly instead of candidates.", "Picks a launch date instead of leaving it to launch-window-planner."]}
```
```yaml
{id: launch-tier-planner-sim-002, type: eval-case, status: simulated, target_skill: launch-tier-planner, scenario: "Scope boundary: user bundles date-picking and creator outreach into the sizing request.", input_summary: "User asks: plan our launch, pick the best week, and line up influencers to post about it.", expected_behavior: ["Do the sizing work in scope: tier decision, type declaration, effort matrix, risk register.", "Route the date/window choice to launch-window-planner instead of choosing a week.", "Route the creator-channel campaign to campaign-planner (influencer discipline) instead of planning creator outreach."], failure_modes: ["Picks the launch week itself.", "Plans creator outreach or briefs inside this skill.", "Silently drops the out-of-scope requests instead of naming the owning sibling."]}
```
```yaml
{id: launch-tier-planner-sim-003, type: eval-case, status: simulated, target_skill: launch-tier-planner, scenario: "Labeling discipline: KPI targets requested with no baseline data.", input_summary: "User asks for D0/W1/M1 launch targets; no analytics export or prior launch history provided.", expected_behavior: ["Ask for own trailing baselines (analytics export or prior launch records) before setting targets; do not invent one.", "Frame targets relative to the user's own trailing rate, never as an absolute industry benchmark ('a good launch gets N signups').", "Label every projected number Estimated with its assumption stated, and baseline numbers Measured or User-provided."], failure_modes: ["States an absolute benchmark number as fact.", "Presents an Estimated projection as Measured.", "Projects D0/W1/M1 targets from nothing without flagging the missing baseline."]}
```
```yaml
{id: launch-tier-planner-sim-004, type: eval-case, status: simulated, target_skill: launch-tier-planner, scenario: "Partnership launch with a risk register the runbook can inherit.", input_summary: "Joint launch with a partner company; user wants the risk register and rollback plan for launch day.", expected_behavior: ["Declare type partnership and name the co-launch partners plus the co-marketing responsibility split (channel ownership, shared-copy approval, one authoritative date/stage reference).", "Build the risk register with likelihood x blast-radius, a named owner, mitigation, and kill criteria / rollback thresholds phrased as checkable conditions against the user's own baselines.", "Note that launch-day-conductor lifts the kill criteria into its go/rollback observation windows unchanged."], failure_modes: ["Skips the co-marketing responsibility split for the partnership type.", "Writes vague risks with no owner, threshold, or kill criterion.", "Invents a numeric rollback threshold instead of anchoring it to the user's own baseline."]}
```
```yaml
{id: launch-tier-planner-sim-005, type: eval-case, status: simulated, target_skill: launch-tier-planner, scenario: "Tier-inflation guard: a small feature asked for as a flagship moment.", input_summary: "User wants a Tier 1 all-channel launch for a minor settings improvement; a Tier 1 launch shipped three weeks ago.", expected_behavior: ["Run the three questions and recommend the lower tier with the rationale stated, not just comply with Tier 1.", "Flag the launch-stacking guardrail (spacing since the last Tier-1 moment burns the same audience — a RAMP M guardrail, not a veto).", "Scope the effort matrix to the recommended tier (e.g., no press kit for a changelog-level note)."], failure_modes: ["Rubber-stamps Tier 1 without checking audience impact / novelty / revenue linkage.", "Ignores the recent Tier-1 moment and the audience-fatigue guardrail.", "Builds a full Tier-1 asset scope for a changelog-level change."]}
```
