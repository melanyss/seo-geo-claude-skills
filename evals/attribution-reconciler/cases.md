Simulated seed cases for the standing attribution-reconciliation workbook: order-ID truth-set matching, Meta+Google de-dup, window/currency normalization, attribution-model comparison, incrementality from a holdout, the NEEDS_INPUT missing-truth-set path, and a guardrail BLOCK when asked to reconcile against a platform count.

```yaml
{id: attribution-reconciler-sim-001, type: eval-case, status: simulated, target_skill: attribution-reconciler, scenario: "Meta and Google both claim the same orders — classic stacked double-count.", input_summary: "GA4 order-ID export (1,000 real orders) + Meta conversion export (700 claimed) + Google conversion export (550 claimed); DR goal.", expected_behavior: ["Match every platform conversion to the order-ID truth set as matched / double-counted / unmatched.", "Report de-duped count per platform and the double-count rate.", "Hand de-duped real-order counts to roi-calculator for ROAS — do not compute the ratio here."], failure_modes: ["Sums Meta + Google claimed counts (1,250) as if all real.", "Computes ROAS itself instead of delegating to roi-calculator.", "Collapses matched/double-counted/unmatched into one number."]}
```
```yaml
{id: attribution-reconciler-sim-002, type: eval-case, status: simulated, target_skill: attribution-reconciler, scenario: "Differing attribution windows and currencies must be normalized before matching.", input_summary: "Meta exports on a 7-day-click window in USD, Google on a 30-day window in EUR; GA4 order export in USD; monthly cadence.", expected_behavior: ["Re-scope both platforms to a common window aligned to the truth-set order timestamps before matching.", "Convert all values to one currency at a stated rate.", "Only then build the match table and de-duped counts."], failure_modes: ["Matches raw counts across mismatched windows.", "Leaves mixed currencies in the workbook.", "Treats the 30-day claims as comparable to 7-day claims."]}
```
```yaml
{id: attribution-reconciler-sim-003, type: eval-case, status: simulated, target_skill: attribution-reconciler, scenario: "Geo holdout exists — read incrementality and compare to last-click.", input_summary: "Two-week geo test: test-region and control-region order exports plus platform spend; user wants lift vs what last-click claimed.", expected_behavior: ["Compute lift of test region over control (incremental orders ÷ exposed).", "Compare the incremental result to the last-click attributed count.", "Also show a second attribution model on the de-duped real orders."], failure_modes: ["Infers lift from attribution alone with no control.", "Reports last-click as if it were incrementality.", "Skips the model-comparison view."]}
```
```yaml
{id: attribution-reconciler-sim-004, type: eval-case, status: simulated, target_skill: attribution-reconciler, scenario: "GA4/ecommerce order-ID export is missing — no truth set.", input_summary: "User supplies only the Meta and Google conversion exports and asks to reconcile; no order-ID export attached.", expected_behavior: ["Return status NEEDS_INPUT and name the missing GA4/ecommerce order-ID export.", "Do not reconcile against any platform's reported count as a substitute truth set.", "List exactly what to attach to proceed."], failure_modes: ["Uses Meta's reported count as the truth set.", "Fabricates a de-duped number with no order export.", "Emits DONE despite the missing required input."]}
```
```yaml
{id: attribution-reconciler-sim-005, type: eval-case, status: simulated, target_skill: attribution-reconciler, scenario: "User asks to trust the platform count and run the R2 veto/RQS gate here.", input_summary: "User says 'just use Meta's 700 conversions as truth and give me a SHIP/BLOCK veto verdict on attribution'.", expected_behavior: ["Refuse to treat the platform count as the truth set — the truth set is the order IDs.", "Decline to run the R2 veto or RQS gate (that is ad-account-auditor's point-in-time job) and redirect there.", "Offer to reconcile against an order-ID export instead."], failure_modes: ["Runs an R2 veto and emits a SHIP/BLOCK verdict.", "Accepts the platform-reported count as ground truth.", "Duplicates the ad-account-auditor gate scope."]}
```
