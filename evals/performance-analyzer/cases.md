```yaml
{id: routing-analyze-campaign-performance-analyzer-001, type: eval-case, status: simulated, target_skill: performance-analyzer, scenario: "User wants to evaluate finished influencer campaign results and compare creators, not compute dollar return.", input_summary: "Analyze how our spring launch influencer campaign did and tell me which creators worked best.", expected_behavior: ["Route to performance-analyzer because the request is to evaluate campaign results and rank creators, its core trigger.", "Do NOT route to roi-calculator — this skill explicitly excludes dollar-level return math and hands that off.", "Ask for campaign name, date range, and any platform exports / influencer reports / GA / promo-code data that are missing rather than blocking."], failure_modes: ["Routes to roi-calculator for what is a performance read.", "Routes to report-generator before any analysis exists.", "Invents reach/ER numbers instead of requesting inputs."]}
```
```yaml
{id: core-scorecard-rankings-attribution-performance-analyzer-001, type: eval-case, status: simulated, target_skill: performance-analyzer, scenario: "User supplies platform exports, GA conversions, and promo-code redemptions for a 10-creator campaign with stated targets.", input_summary: "Here are the exports for our summer skincare campaign (10 creators, IG + TikTok), GA conversions, and promo-code uses — analyze performance vs our 2M reach and 3.5% ER targets.", expected_behavior: ["Score core metrics (reach, ER, clicks, conversions) against target AND benchmark with a performance verdict, satisfying the first Done-when gate.", "Rank top and bottom influencers with reasons and name the content patterns/formats that worked (second Done-when gate).", "Attribute conversions by method (promo code / UTM / direct / estimated) and write 3-5 ranked learnings (third Done-when gate).", "Write the analysis to memory/influencer/performance-analyzer/YYYY-MM-DD-<campaign>.md and recommend roi-calculator as the primary next skill."], failure_modes: ["Reports raw totals with no vs-target / vs-benchmark verdict.", "Lumps all conversions together instead of splitting by attribution method.", "Skips the ranked learnings or omits roster renew/drop calls.", "Stops without naming a next best skill."]}
```
```yaml
{id: edge-missing-conversion-data-performance-analyzer-001, type: eval-case, status: simulated, target_skill: performance-analyzer, scenario: "User asks for full performance analysis but provides only reach/engagement screenshots — no conversion, sales, or promo-code data and no targets.", input_summary: "Analyze our campaign's performance and ROI — here are the influencers' engagement screenshots.", expected_behavior: ["Run the engagement-side analysis (scorecards, rankings, sentiment) on what is provided rather than blocking entirely.", "Stop and ask for conversion / sales / promo-code data and targets, since the conversion-attribution Done-when gate cannot be met without them.", "Declare NEEDS_INPUT for the missing conversion inputs, or DONE_WITH_CONCERNS if delivering a partial read, and flag that dollar-level ROI belongs to roi-calculator, not this skill."], failure_modes: ["Fabricates conversions, revenue, or an ROI ratio to fill the gap.", "Claims DONE despite the unmet conversion-attribution gate.", "Silently drops the ROI part of the request without flagging it."]}
```
```yaml
{id: quality-measured-vs-estimated-handoff-performance-analyzer-001, type: eval-case, status: simulated, target_skill: performance-analyzer, scenario: "User provides hard promo-code and UTM conversions but wants the analysis to also credit halo / view-through influence, then asks what to run next.", input_summary: "Include the influence we can't directly track and give me the full picture, then set up next steps.", expected_behavior: ["Label every metric as Measured (promo-code / UTM / direct) versus Estimated (estimated influence) in the attribution-by-method breakdown, never presenting modeled numbers as tracked.", "Keep estimated-influence figures clearly separated so the scorecard verdict rests on measured data.", "Emit a handoff summary and route to roi-calculator (primary) to convert measured performance into dollar return, with report-generator and fit-scorer as alternates.", "Honor the visited-set / max-depth-3 termination rule and surface open loops instead of looping skills."], failure_modes: ["Blends estimated influence into measured conversions without labeling.", "Overstates certainty or uses fabricated precision on modeled numbers.", "Recommends an unrelated skill or re-runs an already-visited skill.", "Performs dollar-level ROI math itself instead of handing off to roi-calculator."]}
```
