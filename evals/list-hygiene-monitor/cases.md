Simulated seed cases for the scheduled list-hygiene watch: engagement-recency cohorting, bounce/complaint trend vs baseline, suppression-drift/leakage, the re-permission/sunset/prune worklist, scope-guard boundaries against deliverability-qa / consent-registry / email-quality-auditor, and the missing-baseline NEEDS_INPUT path.

```yaml
{id: list-hygiene-monitor-sim-001, type: eval-case, status: simulated, target_skill: list-hygiene-monitor, scenario: "Routine scheduled watch on a retention list with a prior baseline.", input_summary: "ESP engagement export (last-open/click per subscriber) + bounce/complaint report + last quarter's baseline; retention goal; monthly cadence.", expected_behavior: ["Cohort by 30/90/180/365-day engagement recency with Measured counts.", "Trend bounce + spam-complaint vs benchmark AND the prior baseline with deltas.", "Emit a sized re-permission / sunset / prune worklist and the SEND-S list-hygiene + SEND-E decay reads, then recommend reactivation-specialist."], failure_modes: ["Reports point-in-time rates with no baseline delta.", "Uses promotional weights on a retention list.", "Computes an EQS or fires an S1/N1 verdict itself."]}
```
```yaml
{id: list-hygiene-monitor-sim-002, type: eval-case, status: simulated, target_skill: list-hygiene-monitor, scenario: "No prior baseline supplied — trend cannot be computed.", input_summary: "First-ever hygiene run: engagement export + bounce/complaint report but no earlier baseline; newsletter goal.", expected_behavior: ["Mark the bounce/complaint trend NEEDS_INPUT and report the point-in-time read only.", "Still cohort the list and build the worklist from the current export.", "Name the missing baseline and set this run as the baseline for next time — do not invent a delta."], failure_modes: ["Fabricates a trend direction with no baseline.", "Refuses to produce any worklist for lack of baseline.", "Presents Estimated cohort counts as Measured."]}
```
```yaml
{id: list-hygiene-monitor-sim-003, type: eval-case, status: simulated, target_skill: list-hygiene-monitor, scenario: "Suppression leakage — an opt-out still receiving active sends.", input_summary: "consent-registry suppression history shows unsubscribes that reappear on the active send list; complaint rate creeping toward 0.1%.", expected_behavior: ["Flag suppression leakage as a hard S flag and route it to the auditor as an N1 candidate without verdicting N1.", "Recommend a send-hold / auditor gate before the next campaign.", "Do NOT chain to a reactivation campaign against the unclean list."], failure_modes: ["Issues its own N1 verdict.", "Treats a suppressed-but-mailed address as a normal dormant cohort.", "Recommends reactivation-specialist despite the leakage."]}
```
```yaml
{id: list-hygiene-monitor-sim-004, type: eval-case, status: simulated, target_skill: list-hygiene-monitor, scenario: "Large never-engaged cohort dragging inbox placement.", input_summary: "Engagement export shows 15% of the active list never-engaged / >365d; bounces still under benchmark but rising vs baseline; promotional goal.", expected_behavior: ["Size the never-engaged cohort and state the reputation cost of not pruning in numbers.", "Flag the rising bounce trend under S even though the absolute number is still under benchmark.", "Split the cohort into sunset vs prune buckets and emit the SEND-S/E reads."], failure_modes: ["Passes S list-hygiene by-default because absolute bounce is under benchmark.", "Lumps never-engaged into a single vague 'clean up' bucket.", "Ignores the rising trend because today's number passes."]}
```
```yaml
{id: list-hygiene-monitor-sim-005, type: eval-case, status: simulated, target_skill: list-hygiene-monitor, scenario: "User asks for a pre-send authentication check — wrong skill boundary.", input_summary: "User pastes a DMARC RUA report and asks 'is this send authenticated and will it land?' with no engagement/bounce export.", expected_behavior: ["State this skill is the recurring hygiene watch, not the one-time auth pre-flight.", "Redirect the SPF/DKIM/DMARC/S1 check to deliverability-qa.", "Return NEEDS_INPUT for the hygiene inputs (engagement + bounce/complaint export) it actually requires."], failure_modes: ["Attempts an S1 authentication verdict from the DMARC report.", "Fabricates hygiene cohorts with no engagement export.", "Fails to route the auth question to deliverability-qa."]}
```
