Simulated seed cases for search-term mining: converting-query harvest, negative-keyword build, the n-gram waste report, the recurring maintenance-diff shape, and the NEEDS_INPUT path when the export lacks a conversion column.

```yaml
{id: search-term-miner-sim-001, type: eval-case, status: simulated, target_skill: search-term-miner, scenario: "Harvest converting queries into new keywords/ad-groups.", input_summary: "Search-terms export with query/cost/conversions columns; DR goal; several converting queries not yet keywords.", expected_behavior: ["Route each converting query above the threshold to add-keyword or move-to-ad-group.", "Label harvested counts Measured from the export.", "Emit the S score with the typed profile named and recommend ad-account-auditor."], failure_modes: ["Harvests queries that are already keywords.", "Estimates conversion counts instead of reading the export.", "Attempts to re-design account structure."]}
```
```yaml
{id: search-term-miner-sim-002, type: eval-case, status: simulated, target_skill: search-term-miner, scenario: "Build a standing negative-keyword list from wasted spend.", input_summary: "Export shows queries with meaningful cost and zero conversions; user wants a negatives list.", expected_behavior: ["Flag zero-conversion high-cost queries and state the cost floor used.", "Assign each a match type (exact/phrase) and a level (ad-group/campaign/shared list).", "Propose durable negatives as pending-decision items, not direct decisions."], failure_modes: ["Negates queries on clicks alone with no cost floor.", "Omits match type or level.", "Writes negatives to decisions.md without user approval."]}
```
```yaml
{id: search-term-miner-sim-003, type: eval-case, status: simulated, target_skill: search-term-miner, scenario: "Produce the n-gram waste report.", input_summary: "Large search-terms export; user asks which tokens are draining spend without converting.", expected_behavior: ["Tokenize non-converting queries into 1-/2-/3-grams and sum Measured cost per token.", "Rank the top spend-draining tokens and propose the highest-cost ones as shared-list negatives.", "Deliver figures labeled Measured, never invented."], failure_modes: ["Ranks by impressions instead of cost.", "Fabricates token costs not present in the export.", "Includes converting queries in the waste tally."]}
```
```yaml
{id: search-term-miner-sim-004, type: eval-case, status: simulated, target_skill: search-term-miner, scenario: "Recurring prune delivered as a maintenance diff.", input_summary: "Fresh weekly search-terms export against an existing structure with prior negatives.", expected_behavior: ["Deliver add / negate / move rows, not a full re-structure.", "Skip queries already covered by existing negatives.", "Recommend ad-account-auditor and pass the updated negatives as S evidence."], failure_modes: ["Returns a full account restructure instead of a diff.", "Re-negates queries already on the list.", "Recurses back into campaign-architect after it was already visited this chain."]}
```
```yaml
{id: search-term-miner-sim-005, type: eval-case, status: simulated, target_skill: search-term-miner, scenario: "Export is missing the conversion column — cannot mine.", input_summary: "User pastes a search-terms report with query/impressions/clicks/cost but no conversions column.", expected_behavior: ["Return status NEEDS_INPUT and name the missing conversion column.", "Do not harvest or negate on clicks alone.", "Do not fabricate a conversion signal or emit an S score as Measured."], failure_modes: ["Harvests or negates using clicks as a proxy for conversions.", "Emits DONE despite the missing column.", "Invents conversion figures to proceed."]}
```
