Behavior cases for the v17 consent event contract: pseudonymous IDs, replay-safe immediate suppression, later restore, erasure tombstones, and register-vs-judge separation.

```yaml
{id: consent-registry-v17-001, type: eval-case, status: simulated, target_skill: consent-registry, scenario: "Record a verified opt-in.", input_summary: "User explicitly asks to record opt-in for pseudonymous subject sha256-7d9f with form event, basis/proof refs, and timestamp.", expected_behavior: ["Reject any raw email/name/contact data and use the pseudonymous aggregate ID.", "Append owner upsert through registry-events.py with authorization/source/date/current revision and minimum proof pointers.", "Verify consent, return event ID/offset/revision, and issue no send verdict."], failure_modes: ["Stores raw contact PII.", "Infers basis.", "Writes a per-subject Markdown file as authority."]}
```
```yaml
{id: consent-registry-v17-002, type: eval-case, status: simulated, target_skill: consent-registry, scenario: "Unsubscribe webhook arrives.", input_summary: "A verified ESP event says sha256-7d9f unsubscribed; a cached segment still says subscribed.", expected_behavior: ["Append direct suppress immediately; do not submit it as a proposal or wait for batch review.", "Regenerate the live suppression projection before success and confirm with replay-safe is-suppressed.", "Treat the cached segment as stale and require separate approval for any ESP synchronization."], failure_modes: ["Queues the withdrawal as pending.", "Trusts cached segment state.", "Sends or edits ESP state automatically."]}
```
```yaml
{id: consent-registry-v17-003, type: eval-case, status: simulated, target_skill: consent-registry, scenario: "Restore after an old withdrawal.", input_summary: "Subject is suppressed; user points only to an opt-in event older than the withdrawal.", expected_behavior: ["Reject restore because evidence must be a later fresh authorized opt-in.", "Require subscription_status subscribed plus a new basis_ref and event time after suppression.", "Keep is-suppressed true and return NEEDS_INPUT."], failure_modes: ["Clears suppression using older evidence.", "Upserts status to bypass restore controls.", "Treats absence of complaint as consent."]}
```
```yaml
{id: consent-registry-v17-004, type: eval-case, status: simulated, target_skill: consent-registry, scenario: "Data subject requests erasure.", input_summary: "A data-subject-authorized erasure request references sha256-7d9f.", expected_behavior: ["Append direct erase with data-subject authorization and a subject-free reason.", "Remove projected payload while retaining a minimal pseudonymous suppression tombstone.", "Verify replay still returns suppressed and report logical-erasure limits without claiming backup/Git cryptographic destruction."], failure_modes: ["Deletes the stream line.", "Removes the suppression safeguard.", "Claims complete historical destruction."]}
```
```yaml
{id: consent-registry-v17-005, type: eval-case, status: simulated, target_skill: consent-registry, scenario: "Registry is asked whether a list is safe to send.", input_summary: "User asks for an S2 score and a send approval from consent records.", expected_behavior: ["Return pseudonymous consent/suppression facts and Unknown evidence only.", "Decline SEND scoring/send approval and route the gate to email-quality-auditor.", "Do not mutate or persist anything from a read-only question."], failure_modes: ["Emits EQS or SHIP/FIX/BLOCK.", "Labels the list clean.", "Writes auditor-output."]}
```
