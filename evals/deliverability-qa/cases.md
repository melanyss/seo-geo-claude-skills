Simulated seed cases for SEND-S scoring, the S1 authentication pre-flight (pass / `p=none` partial / no-DMARC veto-candidate), the missing inbox-placement/DMARC NEEDS_INPUT path, and the S2 consent-context boundary (context only, never a verdict here).

```yaml
{id: deliverability-qa-sim-001, type: eval-case, status: simulated, target_skill: deliverability-qa, scenario: "Healthy sender, auth aligned, inbox placement good — send-ready.", input_summary: "DNS export with SPF+DKIM+DMARC aligned & passing, DMARC RUA report clean, seed-list test 96% inbox, ESP report bounce 0.4% and complaint 0.02%; promotional profile.", expected_behavior: ["Set S1 flag = pass (SPF+DKIM+DMARC aligned and passing).", "Mark each S sub-item from evidence and name the promotional profile (S=0.30).", "State the signal is send-ready and hand off to email-quality-auditor without computing EQS."], failure_modes: ["Computes an EQS or applies a veto cap itself.", "Passes list-hygiene by default instead of citing the 0.4%/0.02% numbers.", "Omits the typed profile or the S1 flag."]}
```
```yaml
{id: deliverability-qa-sim-002, type: eval-case, status: simulated, target_skill: deliverability-qa, scenario: "Young program at DMARC p=none but SPF/DKIM aligned — partial, not a veto.", input_summary: "DNS shows DMARC policy p=none, SPF and DKIM aligned and passing; RUA report present and clean; cold-outbound profile.", expected_behavior: ["Set S1 flag = partial (p=none young program, SPF/DKIM aligned) — a flag, not an auto-veto.", "Cite the send-benchmark S1 carve-out (mirrors the ROAS iOS-ATT modeled-data carve-out).", "Emit SEND-S evidence under the cold-outbound profile (S=0.35) and hand off to the auditor."], failure_modes: ["Fires S1 as a veto-candidate on p=none despite aligned SPF/DKIM.", "Caps or blocks the score itself.", "Silently upgrades p=none to pass."]}
```
```yaml
{id: deliverability-qa-sim-003, type: eval-case, status: simulated, target_skill: deliverability-qa, scenario: "No DMARC record at all — S1 veto-candidate, route to auditor.", input_summary: "DNS export has SPF only; no DMARC record found and DKIM unaligned; ESP complaint rate 0.14%; retention goal.", expected_behavior: ["Set S1 flag = veto-candidate (no DMARC record / auth unaligned) and route to email-quality-auditor without capping the score itself.", "Flag the 0.14% spam-complaint rate as over the <0.1% red-line benchmark under S.", "Score the verifiable S sub-items and state exactly what to fix (publish DMARC, align DKIM)."], failure_modes: ["Treats a missing DMARC record as partial or pass.", "Applies a veto cap itself instead of handing off to the typed auditor scorer.", "Buries the no-DMARC finding in generic advice."]}
```
```yaml
{id: deliverability-qa-sim-004, type: eval-case, status: simulated, target_skill: deliverability-qa, scenario: "DMARC RUA report and inbox-placement test both absent — NEEDS_INPUT, not pass.", input_summary: "User provides only an ESP deliverability report; no DMARC RUA report and no seed-list inbox-placement test; promotional goal.", expected_behavior: ["Mark the authentication sub-item and the inbox-placement sub-item NEEDS_INPUT (not pass-by-default).", "Return status NEEDS_INPUT and name the missing DMARC RUA report and seed-list test.", "Score the sub-items that are verifiable and do not fabricate an S1 pass or an inbox-placement number."], failure_modes: ["Marks authentication or inbox placement Pass with no DMARC/seed evidence.", "Emits DONE despite the missing exports.", "Invents an inbox-placement percentage."]}
```
```yaml
{id: deliverability-qa-sim-005, type: eval-case, status: simulated, target_skill: deliverability-qa, scenario: "List consent unrecorded — note S2 context, do not verdict it here.", input_summary: "Auth passes and inbox placement is good, but consent-registry has no opt-in timestamp or lawful basis on file for the list; cold-outbound goal.", expected_behavior: ["Consult consent-registry and mark the S2 consent context NEEDS_INPUT (no record on file).", "Explicitly leave the S2 veto verdict to email-quality-auditor and pass the context forward.", "Score SEND-S on the verifiable sub-items and recommend consent-registry to record lawful basis before the auditor can clear S2."], failure_modes: ["Declares an S2 veto or a purchased-list pass itself.", "Treats a missing consent record as pass-by-default.", "Skips the consent-registry consult entirely."]}
```
