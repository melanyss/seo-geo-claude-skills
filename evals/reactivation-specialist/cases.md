Simulated seed cases for the reactivation program — lapsed-cohort definition, staged offer ladder, re-consent capture, sunset-confirm/suppression rule, the SEND N engagement-decay + preference-center sub-item read, and the scope-guard + NEEDS_INPUT boundaries.

```yaml
{id: reactivation-specialist-sim-001, type: eval-case, status: simulated, target_skill: reactivation-specialist, scenario: "Standard win-back with an incentive available.", input_summary: "ESP engagement export (no-open 90d), 12% lapsed tail, discount incentive available; Retention/Newsletter goal.", expected_behavior: ["Define the lapsed cohort by the 90-day no-open window and report cohort size Measured from the export.", "Stage a soft → incentive → last-chance offer ladder with timing and an exit-on-re-engagement condition.", "Add a re-consent capture step and a sunset-confirm rule that terminally re-permissions or suppresses each subject.", "Read the SEND N engagement-decay + preference-center sub-items and name the Retention goal-weight column."], failure_modes: ["Designs a looping ladder with no stop/sunset.", "Reports cohort size as Measured when no export was given.", "Rolls up the full 0–100 N dimension or computes EQS."]}
```
```yaml
{id: reactivation-specialist-sim-002, type: eval-case, status: simulated, target_skill: reactivation-specialist, scenario: "No consent record on file for the lapsed cohort.", input_summary: "User wants to sunset a purchased/legacy list segment but consent-registry has no lawful-basis record for these subjects.", expected_behavior: ["Return NEEDS_INPUT and name the missing consent record (S2 boundary the registry owns).", "Do not design a suppression rule against subjects whose lawful basis is unknown.", "Route to consent-registry to establish the record before proceeding."], failure_modes: ["Designs a sunset/suppression rule with no consent basis.", "Treats absent consent as pass-by-default.", "Adjudicates S2 itself instead of deferring to consent-registry."]}
```
```yaml
{id: reactivation-specialist-sim-003, type: eval-case, status: simulated, target_skill: reactivation-specialist, scenario: "No incentive available — soft-only ladder.", input_summary: "Engagement export present, no discount/offer to give; user asks for a re-permission sweep on a 180-day no-click cohort.", expected_behavior: ["Define the cohort by the 180-day no-click window.", "Design the ladder with the incentive step marked optional/conditional and continue silently (no stop).", "Keep the soft re-engagement + last-chance + re-consent + sunset steps intact."], failure_modes: ["Stops to ask for an incentive instead of proceeding.", "Invents a discount that was not offered.", "Drops the re-consent or sunset step because no incentive exists."]}
```
```yaml
{id: reactivation-specialist-sim-004, type: eval-case, status: simulated, target_skill: reactivation-specialist, scenario: "Fragile cohort with rising complaints — frequency guardrail.", input_summary: "Lapsed tail with spam-complaint rate creeping toward 0.1%; user wants an aggressive multi-touch win-back.", expected_behavior: ["Cap the reactivation touches (3–4 across the ladder) and honor quiet hours.", "Flag over-frequency on the decayed cohort as a high-severity SEND-E guardrail, not an N1 veto.", "Never enroll already-suppressed or over-cap subjects.", "Hand the program to email-quality-auditor for the N1/EQS rollup."], failure_modes: ["Scores over-frequency as an N1 fail.", "Designs an uncapped daily blast to the fragile cohort.", "Enrolls suppressed subjects from the export."]}
```
```yaml
{id: reactivation-specialist-sim-005, type: eval-case, status: simulated, target_skill: reactivation-specialist, scenario: "User asks for the everyday welcome/cart flows instead.", input_summary: "Prompt mixes 'win back lapsed users' with 'and also set up my welcome and abandoned-cart flows'.", expected_behavior: ["Design only the reactivation program (cohort + ladder + re-consent + sunset).", "Scope-guard the welcome/cart request to email-sequence-designer and hand off.", "Do not compute EQS or run vetoes."], failure_modes: ["Designs welcome/cart flows in this skill.", "Silently drops the reactivation ask to build lifecycle flows.", "Computes the full N dimension or the EQS."]}
```
