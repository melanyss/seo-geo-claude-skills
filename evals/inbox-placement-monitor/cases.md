Simulated seed cases for post-send inbox-placement tracking: per-provider seed-test read, Postmaster/SNDS reputation trend, the send-over-send regression delta, the missing-provider NEEDS_INPUT path, and the scope boundary against deliverability-qa (auth pre-flight) and email-quality-auditor (EQS/vetoes).

```yaml
{id: inbox-placement-monitor-sim-001, type: eval-case, status: simulated, target_skill: inbox-placement-monitor, scenario: "Healthy post-send read across all providers, no regression.", input_summary: "Seed-list test (Gmail/Outlook/Yahoo/Apple all inbox-dominant) + Gmail Postmaster + Microsoft SNDS export + prior send baseline; promotional goal.", expected_behavior: ["State inbox/spam/promotions per mailbox provider from the seed test.", "Read Postmaster + SNDS reputation trend with direction and number, label each Measured.", "Report 'no regression vs baseline', emit the SEND-S placement snapshot naming the typed profile, treat holding placement as a terminal healthy read."], failure_modes: ["Collapses providers into one aggregate placement number.", "Computes an EQS or fires a veto.", "Fabricates a delta when placement is unchanged."]}
```
```yaml
{id: inbox-placement-monitor-sim-002, type: eval-case, status: simulated, target_skill: inbox-placement-monitor, scenario: "Placement regression after the last send — Gmail spam drift + reputation downgrade.", input_summary: "Seed test shows Gmail inbox 94% -> 61%, Postmaster domain reputation High -> Medium, spam-rate 0.08% -> 0.16%; prior baseline provided.", expected_behavior: ["Name the regression per provider with magnitude (Gmail inbox -33pt).", "Flag the Postmaster downgrade and the spam-rate crossing 0.1% as regressions under S.", "Promote the regression to open-loops and route to deliverability-qa to fix the root-cause auth/reputation."], failure_modes: ["Reports 'reputation looks off' with no number.", "Runs the S1 auth pre-flight itself instead of handing to deliverability-qa.", "Marks the spam-rate crossing as a veto."]}
```
```yaml
{id: inbox-placement-monitor-sim-003, type: eval-case, status: simulated, target_skill: inbox-placement-monitor, scenario: "Seed test missing a mailbox provider — cannot read Apple placement.", input_summary: "Seed test covers Gmail/Outlook/Yahoo but has no Apple/iCloud seeds; Postmaster + SNDS present; retention goal.", expected_behavior: ["Mark Apple/iCloud placement NEEDS_INPUT, not pass-by-default.", "Return status NEEDS_INPUT and name the missing provider export.", "Still read the three covered providers and the reputation trend, do not fabricate an Apple placement number."], failure_modes: ["Marks Apple as inbox with no seed data.", "Emits DONE despite the missing provider.", "Invents an Apple placement percentage."]}
```
```yaml
{id: inbox-placement-monitor-sim-004, type: eval-case, status: simulated, target_skill: inbox-placement-monitor, scenario: "First run with no prior baseline.", input_summary: "Seed-list test + Postmaster + SNDS export provided but no prior send on file to compare against; cold-outbound profile.", expected_behavior: ["State there is no prior baseline and record this run as the baseline for next time.", "Read per-provider placement and the reputation snapshot without a delta.", "Emit the SEND-S placement snapshot with the cold-outbound profile (S=0.35), labeling metrics Measured/User-provided/Estimated."], failure_modes: ["Fabricates a send-over-send delta with no baseline.", "Refuses to produce any read because no baseline exists.", "Silently substitutes a different SEND profile."]}
```
```yaml
{id: inbox-placement-monitor-sim-005, type: eval-case, status: simulated, target_skill: inbox-placement-monitor, scenario: "User asks for a pre-send go/no-go, not post-send tracking.", input_summary: "User pastes a placement snapshot and asks 'is this safe to send / what's my EQS' before the next broadcast.", expected_behavior: ["Restate the scope boundary: this skill tracks post-send placement, it does not compute EQS or run the S1/S2/N1/D1 vetoes.", "Hand the placement snapshot to email-quality-auditor for the EQS gate.", "Respect the visited-set / max-depth 3 termination rule and stop rather than chaining onward."], failure_modes: ["Computes an EQS or issues a SHIP/FIX/BLOCK verdict itself.", "Runs the auth pre-flight in place of deliverability-qa.", "Chains past max-depth without the visited-set check."]}
```
