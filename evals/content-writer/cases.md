Simulated seed cases for the two-mode content-writer: new-draft writing with CORE-EEAT constraints, refresh decay diagnosis and republish strategy, mode inference, the refresh-vs-rewrite gate, and the NEEDS_INPUT no-target boundary.

```yaml
{id: content-writer-sim-001, type: eval-case, status: simulated, target_skill: content-writer, scenario: "New draft from a keyword.", input_summary: "User asks to write an SEO article on 'kubernetes autoscaling' targeting that keyword; no existing URL.", expected_behavior: ["Select mode: new.", "Produce title options, meta description, H1/H2 structure, a snippet-ready block, and internal/external link recommendations.", "Cite or flag [needs source] every source-needing claim and hand off to content-quality-auditor."], failure_modes: ["Invents statistics or citations to fill R01/R02.", "Computes a CORE-EEAT score itself instead of handing to the auditor gate.", "Stuffs the keyword unnaturally."]}
```
```yaml
{id: content-writer-sim-002, type: eval-case, status: simulated, target_skill: content-writer, scenario: "Refresh a decayed page with analytics.", input_summary: "User provides a URL plus GA4 showing organic traffic down 45% YoY and two dated 2022 stats.", expected_behavior: ["Select mode: refresh and flag the >30% decline as Measured.", "Diagnose decay drivers, produce a prioritized update plan with a Changes Made block.", "Apply the 20-50% last-updated-date republish treatment and set 7/14/28/56-day readback against a control."], failure_modes: ["Labels the analytics drop Estimated.", "Ships a date-only edit with no substantive updates.", "Skips the republish-date strategy or control comparison."]}
```
```yaml
{id: content-writer-sim-003, type: eval-case, status: simulated, target_skill: content-writer, scenario: "Mode inference — 'refresh' request with no existing URL.", input_summary: "User says 'refresh my post about cloud hosting' but provides only a topic, no URL or prior version.", expected_behavior: ["Detect the mismatch and run mode: new (do not fabricate a prior version).", "Note the mismatch once, then draft net-new content.", "Confirm keyword/intent inputs before writing."], failure_modes: ["Invents a prior version to 'refresh'.", "Stops as BLOCKED instead of routing to new.", "Silently produces a refresh plan for content that does not exist."]}
```
```yaml
{id: content-writer-sim-004, type: eval-case, status: simulated, target_skill: content-writer, scenario: "Refresh-vs-rewrite decision gate.", input_summary: "Existing URL but >50% of content is stale and search intent has shifted from informational to transactional.", expected_behavior: ["Stop and ask: refresh in place vs rewrite as net-new via --mode new.", "State the >50%-stale and intent-shift findings as the reason.", "Do not auto-pick a path or fabricate the missing decision."], failure_modes: ["Silently refreshes a page that should be rewritten.", "Rewrites without surfacing the choice.", "Guesses the user's intent without asking."]}
```
```yaml
{id: content-writer-sim-005, type: eval-case, status: simulated, target_skill: content-writer, scenario: "No target keyword and no URL — NEEDS_INPUT boundary.", input_summary: "User says 'write me some content' with no topic, keyword, or URL, and none inferable from context.", expected_behavior: ["Return status NEEDS_INPUT naming exactly what is missing (topic/keyword or existing URL).", "Present the two starting options (new draft vs refresh) instead of guessing a topic.", "Do not invent a keyword or emit a draft."], failure_modes: ["Picks an arbitrary topic and drafts anyway.", "Emits DONE with no target.", "Fails to name the missing input."]}
```
