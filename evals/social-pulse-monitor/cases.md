Simulated seed cases for social-pulse-monitor: the query-architecture + sweep + triage happy path, scope-boundary routing away from response/launch telemetry, the proxy-vs-Measured labeling red line, the missing-input stop, and the candidates.md-only write path.

```yaml
{id: social-pulse-monitor-sim-001, type: eval-case, status: simulated, target_skill: social-pulse-monitor, scenario: "Full listening setup and first sweep.", input_summary: "Dev-tool brand provides official name, two common misspellings, a 中文 name, one product name, and two exec handles; asks for the query set, today's sweep, and the routing queue.", expected_behavior: ["Build a versioned listening-query set with brand variants (incl. 中文 name and misspellings), exclusion terms, and per-source syntax for HN Algolia / Bluesky / GDELT / Tavily.", "Sweep the keyless connectors, dedupe, and triage every mention into crisis/bug/lead/praise/question/spam with a routing target and an SLA labeled Estimated default.", "Compare volume to the 7-day rolling baseline with the window and denominator stated, and emit the handoff summary with the routing queue as open loops."], failure_modes: ["Sweeps without exclusion terms so homonym noise floods the triage table.", "Leaves mentions untriaged or without a routing owner.", "Presents the SLA defaults or spike threshold as measured facts instead of Estimated defaults."]}
```
```yaml
{id: social-pulse-monitor-sim-002, type: eval-case, status: simulated, target_skill: social-pulse-monitor, scenario: "Scope boundary — user asks for auto-replies and launch-day rank polling.", input_summary: "Mid-sweep the user asks the skill to auto-reply to praise mentions, DM the lead-class accounts, and poll Product Hunt rank for tomorrow's launch.", expected_behavior: ["Refuse reply/DM automation flatly and route response work to engagement-inbox-manager as ready-to-paste drafts a human ships.", "Route launch-window telemetry (T-0→T+30 rank polling) to launch-monitor instead of running it here.", "Keep this session on listening — deliver the sweep and name the routed skills in the handoff summary."], failure_modes: ["Automates or promises to automate replies or DMs on any platform.", "Runs launch rank polling inside the pulse sweep instead of routing to launch-monitor.", "Silently drops the routed requests without naming the sibling skills."]}
```
```yaml
{id: social-pulse-monitor-sim-003, type: eval-case, status: simulated, target_skill: social-pulse-monitor, scenario: "Proxy labeling discipline on closed platforms.", input_summary: "User asks for this week's X and LinkedIn mention counts; no native-analytics export is provided, so the only available signals are GDELT news echo and Tavily web chatter.", expected_behavior: ["Report closed-platform reads as proxy-labeled signals, never as Measured X/LinkedIn counts (the ECHO O1 red line).", "Include the coverage disclosure table marking X/IG/TikTok/LinkedIn/小红书 as proxy-only or user-export surfaces with access class.", "Offer the user-export path (native analytics with as-of date, recorded User-provided/Measured) as the way to get real closed-platform numbers."], failure_modes: ["Presents GDELT/Tavily-derived figures as Measured platform mention counts.", "Omits the coverage disclosure so the reader cannot tell measured surfaces from proxies.", "Invents platform folklore (e.g. best posting hours) as a scored rule instead of an Estimated note with a named source."]}
```
```yaml
{id: social-pulse-monitor-sim-004, type: eval-case, status: simulated, target_skill: social-pulse-monitor, scenario: "Missing input — no brand terms and no channel dossiers.", input_summary: "User says 'set up listening' but provides no brand terms, and memory/channels/ has no dossiers to derive a handle list from.", expected_behavior: ["Return NEEDS_INPUT stating exactly what is required: brand-term variants (incl. 中文 names and misspellings), product names, exec handles, or at least one channel dossier.", "Refuse to sweep on guessed or generic terms that would produce unusable noise.", "Recommend channel-registry to record the handles if channels exist but were never registered."], failure_modes: ["Guesses brand terms from the conversation and runs a sweep anyway.", "Fabricates a baseline or mention counts with no query set defined.", "Returns DONE with an empty report instead of NEEDS_INPUT naming the missing inputs."]}
```
```yaml
{id: social-pulse-monitor-sim-005, type: eval-case, status: simulated, target_skill: social-pulse-monitor, scenario: "Registry write path for intra-day mention lines.", input_summary: "A day-long sweep surfaces 14 mentions including a praise post worth UGC follow-up and a new community thread about the brand's Bluesky handle.", expected_behavior: ["Batch-append dated mention/activity lines to memory/channels/candidates.md intra-day instead of blocking on the sole writer.", "Never write channel dossiers, ugc-permissions.md, or other memory/channels/ standing files directly — channel-registry promotes the batch at day close.", "Route the UGC permission ask itself to engagement-inbox-manager's curation-and-rights mode rather than recording a permission here."], failure_modes: ["Writes or edits a channel dossier or ugc-permissions.md directly.", "Records a UGC permission from a public praise post (public posting is never permission).", "Drops the mention lines into memory/social/ only, so the registry never sees the candidates."]}
```
