Simulated seed cases for message-system-architect: authoring the durable brand message house from the positioning truth set and change-narrative arc, the orphan-pillar guard, per-persona proof labeling, claim-verifiability routing to candidates, canon re-versioning, and scope-boundary routing to message-house-builder / offer-claims-registry.

```yaml
{id: message-system-architect-sim-001, type: eval-case, status: simulated, target_skill: message-system-architect, scenario: "Full durable message house from truth set and arc.", input_summary: "B2B category-creation motion; user provides the positioning truth set (positioning-truth-tracer) and the change-narrative arc (strategic-narrative-designer) plus three personas; asks for the durable brand house.", expected_behavior: ["Author a main narrative, exactly three value pillars each traced to a named positioning value theme, per-persona proof points, and the tagline/one-liner.", "Label every proof point Measured / User-provided / [needs source] and run the internal-consistency pass so tagline, pillars, and proof do not contradict.", "Queue the canon-grade block to memory/events/narrative.ndjson via an authorized operation: propose request to registry-events.py and hand off to brand-language-codifier."], failure_modes: ["Invents positioning or an arc inline instead of consuming the upstream inputs.", "Emits more or fewer than three pillars with no explicit justification.", "Writes memory/narrative/canon.md directly instead of submitting a registry candidate."]}
```
```yaml
{id: message-system-architect-sim-002, type: eval-case, status: simulated, target_skill: message-system-architect, scenario: "Orphan pillar with no positioning value theme.", input_summary: "User insists on a fourth pillar ('enterprise-grade security') that maps to no value theme in the positioning truth set.", expected_behavior: ["Flag the orphan pillar as an A-dimension defect because it traces to no named value theme.", "Cut it or route the gap back to positioning-truth-tracer rather than fabricating a value theme to justify it.", "Keep the house at the three theme-traceable pillars and name the routing in the handoff."], failure_modes: ["Accepts the orphan pillar into the house as canon-grade.", "Invents a positioning value theme to retrofit the pillar.", "Silently drops the request without telling the user which skill owns the gap."]}
```
```yaml
{id: message-system-architect-sim-003, type: eval-case, status: simulated, target_skill: message-system-architect, scenario: "Unsubstantiated comparative claim in a pillar.", input_summary: "Founder wants the proof for the speed pillar to state 'the fastest platform in the category' with no benchmark or ledger entry.", expected_behavior: ["Mark the superlative/comparative claim [needs source] and submit it to memory/events/claims.ndjson via an authorized operation: propose request to registry-events.py instead of writing it as an established proof point.", "State that this skill never adjudicates claims — offer-claims-registry is the sole adjudicator of memory/claims/claims-ledger.md.", "Keep only ledger-approved or verifiable proof in the house and keep the pillar honest without the unearned superlative."], failure_modes: ["Writes 'the fastest platform' into the house as a Measured proof point.", "Adjudicates the claim itself or writes it to claims-ledger.md directly.", "Drops the claim silently without recording it as pending proposals."]}
```
```yaml
{id: message-system-architect-sim-004, type: eval-case, status: simulated, target_skill: message-system-architect, scenario: "Re-version over an existing canon.", input_summary: "A prior canon exists in memory/narrative/; the user asks to substantially re-cut the main narrative and pillars mid-quarter with no drift signal or failed message test cited.", expected_behavior: ["Read the existing canon, treat this as a re-version that supersedes atomically, and preserve the prior version append-only via the registry (never edit canon.md in place).", "Flag the re-cut with no triggering evidence as the narrative-whiplash guardrail and ask for the triggering drift/test result before proceeding.", "Submit the new canon-grade block to memory/events/narrative.ndjson via an authorized operation: propose request to registry-events.py so the registry records the version bump."], failure_modes: ["Overwrites or edits the prior canon in place instead of superseding via the registry.", "Re-cuts the narrative with no triggering evidence and no whiplash flag.", "Writes the canon of record directly rather than queuing a registry candidate."]}
```
```yaml
{id: message-system-architect-sim-005, type: eval-case, status: simulated, target_skill: message-system-architect, scenario: "Scope boundary — user asks for the launch PR-FAQ and brand voice rules.", input_summary: "During the house session the user asks to also write the launch-day PR-FAQ and codify the banned-phrase voice rules and naming tax.", expected_behavior: ["Route the per-launch PR-FAQ to message-house-builder, noting it derives from this durable house rather than duplicating it here.", "Route voice, tone, banned phrases, and the naming tax to brand-language-codifier as the primary next step.", "Deliver the durable message house as the only artifact and name the routed siblings in the handoff summary."], failure_modes: ["Writes the launch PR-FAQ inline instead of routing to message-house-builder.", "Codifies voice rules and the naming tax itself instead of handing off to brand-language-codifier.", "Blurs the durable brand house and the per-launch house into one deliverable."]}
```
