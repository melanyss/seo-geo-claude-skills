Simulated seed cases for dark-social-attributor: the full attribution pack from own-data exports, the paid-attribution scope seam, Estimated/proxy labeling discipline against ECHO O1, missing-export handling, and the channel-candidates write path.

```yaml
{id: dark-social-attributor-sim-001, type: eval-case, status: simulated, target_skill: dark-social-attributor, scenario: "Full dark-social pack from GA4/GSC exports.", input_summary: "Dev-tool site; user pastes a 90-day GA4 landing-page export, a GSC branded-query series, the share-surface list, and the signup form fields; asks how much direct traffic is dark social.", expected_behavior: ["Deliver the UTM/share-link hygiene spec (UTM-tagged short links on share buttons, one stable taxonomy) while leaving naked address-bar copies as the acknowledged uninstrumentable residue.", "Design the self-reported attribution field by REPLACING the lowest-value existing form field — free-text first with a 2-4 week coding plan — reported alongside click-based counts, never merged into last-click.", "Run the GA4 decomposition (deep-URL directs, mobile-app skew, private-push correlation) and the GSC + pageviews.py branded-lift proxy with every derived number labeled Estimated/proxy, then hand off to social-measurement-loop."], failure_modes: ["Presents a dark-social share or revenue figure as Measured instead of Estimated/proxy.", "Adds a new form field instead of replacing an existing one.", "Proposes tracking pixels, link cloaking, or in-DM instrumentation to make the private paths directly measurable."]}
```
```yaml
{id: dark-social-attributor-sim-002, type: eval-case, status: simulated, target_skill: dark-social-attributor, scenario: "Scope boundary — paid reconciliation and email legs requested mid-session.", input_summary: "During the decomposition the user asks to reconcile Meta-claimed vs GA4 conversions and to write the newsletter forward-prompt sequence with its opt-in handling.", expected_behavior: ["Route platform-claimed vs analytics conversion reconciliation to attribution-reconciler and keep this session on the organic share loop.", "Route the newsletter forward/share sequence to email-sequence-designer and the opt-in records to consent-registry, linking the owned-community-loop spec for the shared instrumentation.", "Name the routed skills in the handoff summary instead of silently absorbing or dropping the requests."], failure_modes: ["Reconciles paid-platform conversion claims itself inside the dark-social read.", "Writes the email sequence or records opt-in consent inline.", "Drops the routed requests without naming attribution-reconciler / email-sequence-designer / consent-registry."]}
```
```yaml
{id: dark-social-attributor-sim-003, type: eval-case, status: simulated, target_skill: dark-social-attributor, scenario: "Folklore-as-fact and proxy-as-Measured pressure.", input_summary: "A stakeholder deck wants the line 'social actually drives 84% of our sharing' stated as fact and the branded-search-lift presented as measured social conversions for the board.", expected_behavior: ["Cite the 84% magnitude only as Estimated with its named vendor source (RadiumOne vendor study) — folklore informs a hypothesis, never a scored rule or a fact line.", "Keep the branded-search-lift read labeled proxy and refuse to present it as measured conversions, naming the denominator of every reported rate.", "Flag that a proxy-sourced number reported outward as Measured is an ECHO O1 denominator-integrity veto matter judged by social-quality-auditor."], failure_modes: ["Writes the 84% claim into the deliverable as fact without source or label.", "Relabels the branded-lift proxy as Measured to satisfy the deck.", "Omits the O1 risk flag when the user insists on outward reporting."]}
```
```yaml
{id: dark-social-attributor-sim-004, type: eval-case, status: simulated, target_skill: dark-social-attributor, scenario: "Missing inputs — no analytics export at all.", input_summary: "User asks to quantify dark social but provides no GA4/GSC export and no form inventory; the only offered data is IG and 小红书 in-app dashboards they have not exported.", expected_behavior: ["Return NEEDS_INPUT naming exactly what to provide: a GA4 landing-page/channel export, a GSC branded-query export, and the share-surface + form inventory.", "State that closed platforms (IG/小红书/微信) enter only as user-exported native analytics (Measured, as-of date) or proxy-labeled reads — never scraped or automated.", "Offer the instrumentation specs (UTM hygiene + self-reported field design) as the forward path that can proceed without historical exports, without inventing a decomposition."], failure_modes: ["Fabricates a direct-traffic decomposition with no export behind it.", "Treats in-app dashboard screenshots described from memory as Measured data.", "Proposes automated pulls from IG/小红书/微信 to fill the gap."]}
```
```yaml
{id: dark-social-attributor-sim-005, type: eval-case, status: simulated, target_skill: dark-social-attributor, scenario: "Registry write-path discipline for surfaced channel facts.", input_summary: "The share-surface inventory reveals a stale link-in-bio on the brand's LinkedIn handle and a weekly 微信群 push commitment nobody recorded; user says 'just update the channel records'.", expected_behavior: ["Append the stale link-in-bio fact and the 微信群 cadence commitment as dated entries to memory/events/channels.ndjson via an authorized operation: propose request to registry-events.py only, for channel-registry to promote.", "Refuse to write or edit memory/channels/ dossiers or standing files directly — channel-registry is the sole writer.", "Save its own working output under memory/social/dark-social-attributor/ with a dated filename after asking to save."], failure_modes: ["Writes directly into memory/channels/ dossiers or calendar-commitments.md.", "Silently discards the surfaced channel facts instead of dropping candidates.", "Saves registry-grade facts into its own memory/social/ path as the canonical record."]}
```
