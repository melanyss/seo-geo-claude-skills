Simulated seed cases for subject-line-lab: variant generation + labeling, the four heuristic pre-scores (spam / length-truncation / emoji / preview render), the rank-and-cut shortlist, the scope boundary against send-experiment-designer / deliverability-qa / email-quality-auditor, a false-scarcity claim flag, and the NEEDS_INPUT nothing-to-score path.

```yaml
{id: subject-line-lab-sim-001, type: eval-case, status: simulated, target_skill: subject-line-lab, scenario: "Generate + pre-score a fresh variant set for a promo, then rank.", input_summary: "Offer + from-name given, no candidates; promo mode; asks for 5 variants ranked by pre-score.", expected_behavior: ["Draft 5 angle-labeled variants with stable SUBJ-A..E ids and one preheader each.", "Pre-score every variant on spam / desktop+mobile truncation / emoji / inbox preview, labeling char counts Measured and render limits Estimated.", "Return a ranked shortlist naming survivors vs cuts with one-line reasons and recommend send-experiment-designer."], failure_modes: ["Emits variants with no ids or angle labels.", "Skips one of the four heuristics.", "Presents the pre-score as an EQS pass/fail verdict."]}
```
```yaml
{id: subject-line-lab-sim-002, type: eval-case, status: simulated, target_skill: subject-line-lab, scenario: "Pasted candidates where the promise truncates on mobile.", input_summary: "6 pasted subjects, one buries the offer word past ~40 chars; from-name given; promo mode.", expected_behavior: ["Count characters (Measured) and compare to the ~30-40 mobile limit (Estimated).", "Flag the buried-promise variant as a cut, not just any overflow, and keep front-loaded overflow.", "Render the mobile inbox preview showing the truncated line and rank it below promise-intact variants."], failure_modes: ["Cuts every overflowing subject including front-loaded ones.", "Treats the render limit as Measured/hard.", "Fails to show the truncated preview the recipient sees."]}
```
```yaml
{id: subject-line-lab-sim-003, type: eval-case, status: simulated, target_skill: subject-line-lab, scenario: "Cold-outbound set with emoji and an ALL-CAPS + RE: fakery subject.", input_summary: "4 B2B cold candidates; one leads with an emoji, one uses ALL-CAPS + a deceptive RE: prefix with no prior thread.", expected_behavior: ["Flag any emoji in cold-outbound mode per the emoji rule.", "Flag the ALL-CAPS run and the deceptive RE: prefix from the spam-trigger checklist as pattern hits (Estimated).", "State a clean pre-score is not an inbox-placement guarantee and route the full S scan to deliverability-qa; do not score SEND-S here."], failure_modes: ["Passes emoji in a cold-outbound subject.", "Claims an inbox-placement / filter verdict.", "Runs or reports a SEND-S dimension score instead of flagging and handing off."]}
```
```yaml
{id: subject-line-lab-sim-004, type: eval-case, status: simulated, target_skill: subject-line-lab, scenario: "A subject invents false scarcity / an unsupported figure.", input_summary: "User asks to punch up a subject to say 'only 3 left, 90% off' but supplied no inventory or discount basis.", expected_behavior: ["Mark the scarcity + 90% claim [needs source] and drop a one-line candidate in memory/claims/candidates.md rather than shipping it.", "Flag it as a downstream D1 claim risk the auditor would veto and never fabricate the figure.", "Recommend offer-claims-registry to resolve the wording, then return to lock the variant."], failure_modes: ["Invents a scarcity number to make the subject punchier.", "Silently drops the claim with no flag.", "Asserts it computed or cleared the D1 veto itself."]}
```
```yaml
{id: subject-line-lab-sim-005, type: eval-case, status: simulated, target_skill: subject-line-lab, scenario: "Nothing to score — no candidates and no offer/angle.", input_summary: "User says 'pre-score my subject lines' but pastes nothing and gives no offer or angle to generate from.", expected_behavior: ["Return status NEEDS_INPUT naming exactly what is missing (candidates or an offer/angle).", "Do not fabricate subject variants to have something to score.", "Continue silently only on non-blocking gaps (from-name placeholder, drafted preheader marked Estimated)."], failure_modes: ["Invents placeholder subjects and scores them.", "Emits DONE with an empty variant set.", "Stops to ask for optional from-name instead of proceeding with a placeholder."]}
```
