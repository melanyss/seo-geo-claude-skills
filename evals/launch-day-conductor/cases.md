Simulated seed cases for launch-day-conductor: the two-record pre-conditions hard gate, hour-blocked runbook generation with owners, the forced CONTINUE/ROLLBACK observation-window verdict with Measured/Estimated labeling, scope routing to community-launch-runner / press-media-relations, and the T-0 proposals-only registry write discipline.

```yaml
{id: launch-day-conductor-sim-001, type: eval-case, status: simulated, target_skill: launch-day-conductor, scenario: "Launch day requested with no gate verdict on file.", input_summary: "User says 'run my launch day tomorrow'; memory/audits/launch/ has no launch-readiness-auditor artifact and memory/launch-registry/ has no authoritative date record.", expected_behavior: ["Stop at the pre-conditions hard gate with status NEEDS_INPUT — no runbook is generated.", "Name both missing records and route to the owning skills: launch-readiness-auditor for the SHIP verdict, launch-registry for the authoritative date.", "Do not treat a verbal 'we are ready' as a substitute for the gate artifact."], failure_modes: ["Generates the runbook anyway without the SHIP verdict or date record.", "Accepts a FIX or BLOCK verdict as good enough to proceed.", "Invents a launch date instead of requiring the registry record."]}
```
```yaml
{id: launch-day-conductor-sim-002, type: eval-case, status: simulated, target_skill: launch-day-conductor, scenario: "Happy path: build the hour-blocked runbook for a gated T2 launch.", input_summary: "SHIP verdict and registry date on file; channel plan with 4 channels and named owners; kill criteria in the launch-tier-planner risk register.", expected_behavior: ["Produce a dated runbook with morning (irreversible pushes: release, embargo lift, store go-live, email broadcast), daytime (monitoring loop), and evening (consolidation) blocks.", "Every row carries a named owner, an irreversible flag, an observation window, and a data source.", "Every irreversible action is followed by a scheduled binary CONTINUE/ROLLBACK verdict point with thresholds traced to the pre-declared kill criteria."], failure_modes: ["Runbook rows lack owners or observation windows.", "Invents rollback thresholds on the day instead of pulling pre-declared kill criteria.", "Sequences the embargo lift without checking the registry embargo record."]}
```
```yaml
{id: launch-day-conductor-sim-003, type: eval-case, status: simulated, target_skill: launch-day-conductor, scenario: "Post-push adjudication with mixed data sources.", input_summary: "Release pushed; user pastes own GA4 funnel export and error-tracker readout, plus a Hacker News rank and a claim that 'posting before 9am PT doubles votes'.", expected_behavior: ["Adjudicate a binary CONTINUE or ROLLBACK against the pre-declared thresholds within the fixed observation window — no third option, no silent drift.", "Label sources: GA4/error-tracker readings Measured, stakeholder statements User-provided, platform timing lore Estimated with the source named (community folklore, minimaxir/hacker-news-undocumented).", "Refuse to use the posting-hour folklore as a runbook criterion or verdict input."], failure_modes: ["Issues a hedged 'keep watching' instead of a binary verdict at window close.", "Treats the vote-timing folklore as a hard rule or decision criterion.", "Presents the platform rank or self-reported counts as Measured own-analytics data."]}
```
```yaml
{id: launch-day-conductor-sim-004, type: eval-case, status: simulated, target_skill: launch-day-conductor, scenario: "Scope boundary: content and media asks arrive mid-window.", input_summary: "During the daytime block the user asks the conductor to draft the Product Hunt submission copy, answer whether self-upvoting is allowed, and reply to a journalist email.", expected_behavior: ["Route submission copy and platform-rule questions to community-launch-runner; route the journalist reply to press-media-relations.", "Keep conducting: log the asks as runbook items with owners instead of executing them inline.", "Classify the journalist email as an intake item, not a P0-P3 incident."], failure_modes: ["Drafts the channel submission copy or adjudicates platform rules itself.", "Writes the media reply instead of handing off.", "Drops the asks without logging an owner and follow-up in the runbook."]}
```
```yaml
{id: launch-day-conductor-sim-005, type: eval-case, status: simulated, target_skill: launch-day-conductor, scenario: "P0 rollback and end-of-day registry discipline.", input_summary: "Checkout breaks after the store go-live (P0); rollback executed; day closes with 6 status lines to record (channels live, embargo lifted, rollback).", expected_behavior: ["Run the P0 playbook: rollback steps with an owner, a holding comms line, and a dated ROLLBACK verdict in the incident log.", "Append all submission/status lines as dated entries to memory/events/launches.ndjson via an authorized operation: propose request to registry-events.py per the T-0 offset-ordered proposal resolution clause — never write canonical registry files.", "Close the day with a D0 snapshot (own analytics Measured; platform self-reported labeled as such), a next-day queue from open P2/P3 items, and a handoff to launch-monitor plus launch-registry for the batch promote."], failure_modes: ["Writes the canonical launch-registry dossier directly instead of the append-only event protocol.", "Buries the P0 in the daytime loop without a rollback verdict or comms line.", "Merges platform self-reported counts into the Measured D0 snapshot."]}
```
