Simulated seed cases for the merged performance-monitor skill: report-mode multi-metric snapshot, alert-mode threshold config, mode inference, the CORE-EEAT/CITE scope guard (report never scores), and a NEEDS_INPUT boundary when no comparison period or baseline exists.

```yaml
{id: performance-monitor-sim-001, type: eval-case, status: simulated, target_skill: performance-monitor, scenario: "Report mode — monthly stakeholder snapshot.", input_summary: "GA4 export + Search Console + rank data for last calendar month vs prior; executive audience.", expected_behavior: ["Select mode: report and set the comparison period.", "Fill each section or mark it Not yet evaluated; tag every metric Measured/User-provided/Estimated.", "Emit recommendations with owner/priority/expected impact and a handoff summary for memory/monitoring/."], failure_modes: ["Reports a metric without a source tag.", "Fabricates a value instead of Not yet evaluated.", "Omits the prior-period comparison."]}
```
```yaml
{id: performance-monitor-sim-002, type: eval-case, status: simulated, target_skill: performance-monitor, scenario: "Alert mode — ranking-drop notifications.", input_summary: "Domain + top 20 keywords with a supplied 28-day baseline; wants to be told when they drop.", expected_behavior: ["Select mode: alert from future-tense intent.", "Define trigger/threshold/priority per category tied to the supplied baseline, labeled Measured.", "Output category counts, Critical/High/Medium/Low response plan, and delivery routing."], failure_modes: ["Builds a backward-looking report instead of alerts.", "Sets arbitrary thresholds untied to the baseline.", "Adds categories the user did not request."]}
```
```yaml
{id: performance-monitor-sim-003, type: eval-case, status: simulated, target_skill: performance-monitor, scenario: "Scope guard — report must not compute CORE-EEAT or CITE.", input_summary: "Report request for a domain with no prior auditor gate run for authority or content quality.", expected_behavior: ["Mark Domain Authority (CITE) and Content Quality (CORE-EEAT) sections Not yet evaluated.", "Do NOT compute a CITE or CORE-EEAT score or run vetoes (T03/T05/T09, T04/C01/R10).", "Recommend domain-authority-auditor / content-quality-auditor as the gate for those scores."], failure_modes: ["Invents a CITE or CORE-EEAT score.", "Runs a veto check itself.", "Reports an estimated score as measured."]}
```
```yaml
{id: performance-monitor-sim-004, type: eval-case, status: simulated, target_skill: performance-monitor, scenario: "Mode ambiguous — request fits neither tense.", input_summary: "User says 'help me keep an eye on SEO for example.com' with no period and no threshold intent.", expected_behavior: ["Detect mode ambiguity and stop at the Decision Gate.", "Offer numbered options: report a snapshot now, alert for ongoing monitoring, or both.", "Return status NEEDS_INPUT without guessing a mode."], failure_modes: ["Silently defaults to one mode.", "Runs both without asking.", "Proceeds with an invented period or threshold."]}
```
```yaml
{id: performance-monitor-sim-005, type: eval-case, status: simulated, target_skill: performance-monitor, scenario: "Boundary — report requested but no comparison period exists.", input_summary: "Single current-period export, no prior-period data, no period stated in context.", expected_behavior: ["Stop at the report Decision Gate and offer last-30-vs-prior-30, last-month-vs-prior-month, or custom range.", "Return status NEEDS_INPUT naming the missing comparison period.", "Do not emit a report with a one-sided (no-delta) metric table."], failure_modes: ["Produces a report with no prior-period column.", "Picks a comparison window silently.", "Marks status DONE despite the missing baseline."]}
```
