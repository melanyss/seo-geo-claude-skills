Simulated seed cases for SEND EQS scoring, veto behavior, single-veto cap arithmetic with floor rounding, multi-veto `BLOCKED`, the NEEDS_INPUT paths for a missing DMARC report and a missing consent record, and the auditor handoff/artifact gate.

```yaml
{id: email-quality-auditor-sim-001, type: eval-case, status: simulated, target_skill: email-quality-auditor, scenario: "Healthy promotional program, no vetoes, ready to send.", input_summary: "ESP campaign + flow export, DMARC RUA (SPF/DKIM/DMARC aligned & passing), inbox-placement test, GA4 revenue export, consent record on file; promotional goal; vector approx S=80 E=75 N=70 D=78.", expected_behavior: ["Confirm and state the Promotional goal column.", "Compute EQS = floor(weighted, promotional column) = floor(76.6) = 76 and state the column used.", "Emit SHIP verdict with cap_applied: false and recommend performance-analyzer or send-experiment-designer."], failure_modes: ["Uses retention or cold-outbound weights silently.", "Reports an email KPI (open rate / inbox-placement %) as the EQS.", "Omits cap_applied or raw_overall_score."]}
```
```yaml
{id: email-quality-auditor-sim-002, type: eval-case, status: simulated, target_skill: email-quality-auditor, scenario: "Authentication broken (no DMARC record at all), single veto — cap arithmetic.", input_summary: "ESP export present, but a DNS check shows no DMARC record and SPF is unaligned; promotional goal; raw weighted EQS would be 76.", expected_behavior: ["Fire S1 as a veto (no DMARC record = veto, not p=none Partial).", "Cap the affected dimension and weighted overall at min(76, 60) = 60 with cap_applied: true and math.floor rounding.", "Emit FIX verdict, keep raw_overall_score (76) separate from final_overall_score (60), and route the fix to deliverability-qa."], failure_modes: ["Treats a missing DMARC record as p=none and only flags Partial.", "Averages the veto away.", "Caps the dimension but not the weighted overall, or raises a sub-60 dimension up to 60."]}
```
```yaml
{id: email-quality-auditor-sim-003, type: eval-case, status: simulated, target_skill: email-quality-auditor, scenario: "No consent record and no DMARC report supplied — NEEDS_INPUT, not pass-by-default.", input_summary: "User provides an ESP campaign export and GA4 revenue but no consent record from consent-registry and no DMARC RUA report; retention goal.", expected_behavior: ["Set S2 = NEEDS_INPUT (no consent record) and the S1 authentication sub-items = NEEDS_INPUT (no DMARC report).", "Return status NEEDS_INPUT and name both missing inputs (consent record + DMARC report).", "Do not fabricate a consent pass, an authentication pass, or a SHIP verdict."], failure_modes: ["Marks S2 or S1 as Pass with no record on file.", "Emits DONE despite missing required inputs.", "Invents an opt-in timestamp or a DMARC result."]}
```
```yaml
{id: email-quality-auditor-sim-004, type: eval-case, status: simulated, target_skill: email-quality-auditor, scenario: "Two or more vetoes fail — must BLOCK.", input_summary: "One-click list-unsubscribe is missing / non-functional (N1) and a promotional email makes an unsubstantiated discount claim with no required disclosure (D1); promotional goal.", expected_behavior: ["Flag both N1 and D1 as vetoes.", "Set status: BLOCKED, cap_applied: false, retain raw_overall_score, omit final_overall_score.", "List both vetoes with plain-language fixes (N1 -> email-sequence-designer / unsubscribe path, D1 -> offer-claims-registry); do not output a SHIP or numeric final score."], failure_modes: ["Applies the single-veto cap only.", "Emits a final_overall_score while BLOCKED.", "Hides one veto inside generic recommendations or leaks veto IDs to the user."]}
```
```yaml
{id: email-quality-auditor-sim-005, type: eval-case, status: simulated, target_skill: email-quality-auditor, scenario: "Young program at DMARC p=none plus over-frequency fatigue — neither is a veto.", input_summary: "New sending domain: SPF and DKIM aligned & passing, DMARC policy at p=none; send cadence is past the engagement-decay point; consent on file; retention goal.", expected_behavior: ["Score the S1 authentication sub-item Partial and flag p=none, do NOT fire the S1 veto.", "Treat over-frequency / list fatigue as a high-severity guardrail under E, not a veto.", "Emit DONE_WITH_CONCERNS, compute EQS with the retention column, preserve open loops, and pass the Artifact-Gate self-check with cap_applied: false."], failure_modes: ["Fires the S1 veto on a p=none young program with SPF/DKIM passing.", "Treats over-frequency as a veto and caps the EQS on it alone.", "Writes to a bare memory/ path bypassing the Artifact Gate, or omits class: auditor-output frontmatter."]}
```
