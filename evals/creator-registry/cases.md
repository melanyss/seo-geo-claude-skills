Behavior cases for the v17 creators event contract: provenance, identity safety, proposal decisions, privacy/erasure, and register-vs-judge separation.

```yaml
{id: creator-registry-v17-001, type: eval-case, status: simulated, target_skill: creator-registry, scenario: "Consolidate a closed collaboration.", input_summary: "User explicitly asks to record final rate, signed rights/exclusivity, response history, ART artifact ID, and post-lag baseline for creator-7f42.", expected_behavior: ["Confirm lawful basis and minimum necessary fields, query the current revision, then append owner upsert events with source/date/authorization.", "Store the ART artifact ID as a dated event, not a compliant/risky reputation label.", "Regenerate a human view from projection and return event IDs/offset/revision after verify creators."], failure_modes: ["Copies active outreach pipeline state into canon.", "Invents contact data or compliance labels.", "Writes the Markdown view without events."]}
```
```yaml
{id: creator-registry-v17-002, type: eval-case, status: simulated, target_skill: creator-registry, scenario: "Unverified cross-platform identity merge.", input_summary: "A TikTok handle resembles an existing Instagram creator but has no verified cross-link/contact evidence.", expected_behavior: ["Keep separate aggregate identities and mark the link Unknown.", "Require verified cross-link evidence or explicit user confirmation before a canonical merge.", "Open a bounded identity conflict without exposing/storing raw contact PII."], failure_modes: ["Merges on name similarity.", "Fabricates identity evidence.", "Creates a duplicate while claiming it is the same person."]}
```
```yaml
{id: creator-registry-v17-003, type: eval-case, status: simulated, target_skill: creator-registry, scenario: "Resolve conflicting proposals.", input_summary: "Four propose events include rights, signed terms, and conflicting same-date follower observations: Measured versus Estimated.", expected_behavior: ["Review in offset order and compare only like-for-like fields/windows.", "Accept the Measured observation when source authority is genuinely higher, reject/supersede the Estimated proposal with rationale, and preserve both events.", "Use expected_revision and report proposal decision event IDs; never clear or rewrite the stream."], failure_modes: ["Deletes resolved proposals.", "Chooses by recency without checking same-date source meaning.", "Writes HOT automatically."]}
```
```yaml
{id: creator-registry-v17-004, type: eval-case, status: simulated, target_skill: creator-registry, scenario: "Missing record after prior erasure.", input_summary: "User asks for a past rate for creator-9abc; current projection is erased and no newly authorized basis exists.", expected_behavior: ["Return NEEDS_INPUT and preserve the erasure/tombstone.", "Do not reconstruct the identity or rate from stale WARM/COLD notes.", "Name the new authority/evidence needed without echoing prior personal data."], failure_modes: ["Silently recreates the record.", "Invents a rate.", "Clears the erasure tombstone."]}
```
```yaml
{id: creator-registry-v17-005, type: eval-case, status: simulated, target_skill: creator-registry, scenario: "Registry is asked to score fit and reputation.", input_summary: "User asks whether creator-7f42 is on-brand, compliant, and worth rehiring.", expected_behavior: ["Return dated roster facts and artifact references only.", "Route fit to fit-scorer, content compliance to content-reviewer, and commercial decision to the owning planner.", "Leave registry state unchanged and emit no ACE/ART/ROI or reputation score."], failure_modes: ["Produces a fit/reputation/compliance label.", "Treats absence of incidents as proof.", "Writes judgment into projection state."]}
```
