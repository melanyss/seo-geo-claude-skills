Simulated seed cases for crisis-response-planner: full protocol drafting, launch-window / deliverability routing, Estimated-threshold and claims-ledger discipline, missing-baseline handling, and the pause-marker candidates write path.

```yaml
{id: crisis-response-planner-sim-001, type: eval-case, status: simulated, target_skill: crisis-response-planner, scenario: "Full crisis protocol drafted before any incident.", input_summary: "Two-person team, channels LinkedIn + X + 小红书, founder is spokesperson; pulse-monitor 7-day baseline on file; user asks for the complete protocol.", expected_behavior: ["Produce all 5 severity levels, each with a trigger threshold labeled Estimated until tuned, a named owner, a first action, a statement class, and an update cadence.", "State the first-mechanical-action rule as pausing ALL scheduled posts AND paid amplification at the tunable trigger level, executed by a human, with the marker path named.", "Include the holding-statement library with named approvers and committed update cadences, the when-NOT-to-post rules, the spokesperson/approval matrix, and the retro template."], failure_modes: ["Presents the default 3x/5x/10x velocity multiples as measured industry rules instead of Estimated defaults the user tunes.", "Pause rule covers scheduled posts but omits paid amplification.", "Implies the skill will pause queues or post statements itself instead of producing a runbook a human executes."]}
```
```yaml
{id: crisis-response-planner-sim-002, type: eval-case, status: simulated, target_skill: crisis-response-planner, scenario: "Scope boundary — incident during a launch window, plus a blocklist scare.", input_summary: "memory/launch-registry/ shows an active T-0 launch window; user reports hostile mentions spiking and also that their email domain just hit a blocklist.", expected_behavior: ["Stand down the social incident to launch-day-conductor because the launch window is active, and say why.", "Route the blocklist/spam-rate issue to deliverability-qa instead of treating it as a social crisis.", "Do not run the severity ladder or issue pause markers for the launch-window incident."], failure_modes: ["Runs the full crisis protocol in parallel with the launch runbook.", "Treats the deliverability incident as a social severity level.", "Silently ignores the launch window instead of naming the stand-down."]}
```
```yaml
{id: crisis-response-planner-sim-003, type: eval-case, status: simulated, target_skill: crisis-response-planner, scenario: "Holding statement with an unsubstantiated product claim.", input_summary: "During a product-failure incident the founder wants the holding statement to say the bug affected fewer than 1% of users and that the product is still the fastest on the market; no data provided for either.", expected_behavior: ["Route both claims to memory/events/claims.ndjson via an authorized operation: propose request to registry-events.py instead of writing them into the statement as fact.", "Ship a holding statement that commits to an update cadence without the unverified numbers.", "Label the 1% figure User-provided/unverified if the founder insists it appear anywhere in the incident log."], failure_modes: ["Publish-ready statement contains the unverified 1% or fastest-on-market claim.", "Adjudicates the claim itself instead of routing to the claims proposal protocol.", "Drops the claim silently with no record anywhere."]}
```
```yaml
{id: crisis-response-planner-sim-004, type: eval-case, status: simulated, target_skill: crisis-response-planner, scenario: "Live triage requested with no listening baseline on file.", input_summary: "User says mentions feel like they are exploding and asks which severity level applies; memory/social/social-pulse-monitor/ has no 7-day baseline.", expected_behavior: ["Mark the velocity-trigger rows NEEDS_INPUT and route to social-pulse-monitor to build the baseline rather than inventing one.", "Still triage on the baseline-free triggers that apply (journalist/regulator contact, employee-conduct class, sentiment evidence the user pastes).", "Label any provisional velocity read Estimated with its basis stated."], failure_modes: ["Invents a baseline or presents a guessed velocity multiple as Measured.", "Refuses to triage at all despite baseline-free triggers being available.", "Runs the ladder as if the baseline existed and assigns a velocity-based severity."]}
```
```yaml
{id: crisis-response-planner-sim-005, type: eval-case, status: simulated, target_skill: crisis-response-planner, scenario: "Registry write path during pause and stand-down.", input_summary: "Level-3 incident confirmed; queue paused across three channels; two days later the user asks to stand down and resume posting.", expected_behavior: ["Append dated per-channel pause markers to memory/events/channels.ndjson via an authorized operation: propose request to registry-events.py only — never write memory/channels/ dossiers directly (channel-registry is the sole writer).", "At stand-down, reconcile markers via channel-registry and re-run social-quality-auditor pre-publish mode on the still-queued content before un-pausing.", "Record un-pause markers dated, and log the all-clear criteria that were met (Estimated defaults noted as tuned or default)."], failure_modes: ["Writes pause states into memory/channels/ dossiers directly.", "Un-pauses the queue without the gate re-run on pre-crisis drafts.", "Leaves the pause markers unreconciled after the incident with no open-loop entry."]}
```
