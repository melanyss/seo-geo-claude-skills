Simulated seed cases for platform-norm-profiler: dated norm-card maintenance with platform-documented vs Estimated-folklore labeling, staleness flagging, closed-platform evidence discipline, scope routing to channel-portfolio-planner, and the channel-registry candidates write path.

```yaml
{id: platform-norm-profiler-sim-001, type: eval-case, status: simulated, target_skill: platform-norm-profiler, scenario: "Refresh 小红书 and LinkedIn norm cards before a calendar build.", input_summary: "Active channels per memory/channels/ are xiaohongshu and linkedin; user asks for a full organic-engagement refresh of both cards.", expected_behavior: ["Pull official rule-center/help-doc rows first and label them platform-documented (Measured) with doc URL and retrieval date.", "Record 小红书 search+saves weighting and LinkedIn link-in-first-comment as Estimated-folklore with named sources, never as scored rules.", "Stamp last-verified and review-by dates on every row and extend the existing pack cards in references/platforms/ in place — never a second pack."], failure_modes: ["Labels a folklore weighting claim platform-documented or presents it as Measured.", "Writes a shadow norm card under memory/ instead of extending the references/platforms/ pack.", "Leaves rows undated so the staleness rule has nothing to check."]}
```
```yaml
{id: platform-norm-profiler-sim-002, type: eval-case, status: simulated, target_skill: platform-norm-profiler, scenario: "Scope boundary — user asks channel strategy and post drafts mid-profile.", input_summary: "While profiling the TikTok card the user asks whether the brand should even run TikTok and requests next week's posts for it.", expected_behavior: ["Route the run/not-run channel decision to channel-portfolio-planner instead of deciding it inline.", "Route post drafting to social-creative-builder and keep this session on the norm card rows.", "Deliver the refreshed card as the only artifact and name both routed skills in the handoff summary."], failure_modes: ["Decides the channel portfolio inside the norm-card session.", "Drafts the posts itself instead of handing off.", "Silently drops the routed requests without naming the sibling skills."]}
```
```yaml
{id: platform-norm-profiler-sim-003, type: eval-case, status: simulated, target_skill: platform-norm-profiler, scenario: "Folklore pushed as a hard rule.", input_summary: "User insists the X card encode 'post at 6pm Tuesdays, exactly 5 hashtags' as a hard pass/fail rule, citing a growth-hacker thread.", expected_behavior: ["Record the claim as Estimated-folklore with the thread as the named source and a last-verified date.", "State that folklore never becomes a scored rule — ECHO keeps it out of sub-items by design; only official docs earn platform-documented (Measured).", "Keep proxy or community-observed numbers labeled as such so no downstream read presents them as Measured."], failure_modes: ["Encodes the posting-hour/hashtag lore as a scored pass/fail rule on the card.", "Labels the thread-sourced claim platform-documented or Measured.", "Drops the lore entirely instead of recording it as labeled Estimated with its source."]}
```
```yaml
{id: platform-norm-profiler-sim-004, type: eval-case, status: simulated, target_skill: platform-norm-profiler, scenario: "Missing input on a closed 中文 platform.", input_summary: "User requests a 视频号 norm card but provides no export or pasted rules, and the official 运营规范 pages sit behind a login wall.", expected_behavior: ["Return NEEDS_INPUT naming exactly what to provide: pasted or screenshotted official rule-center text, or a native analytics export.", "Refuse any automated fetch against 中文 platforms — manual-package/user-export access class only, citing the 风控/封号 red line.", "Invent no char limits or fold cutoffs; leave undocumented rows empty and flagged rather than filled with guesses."], failure_modes: ["Fabricates spec values to complete the card.", "Attempts to scrape or automate a login-walled 中文 platform.", "Fills the card from memory of another platform's specs without labeling or source."]}
```
```yaml
{id: platform-norm-profiler-sim-005, type: eval-case, status: simulated, target_skill: platform-norm-profiler, scenario: "Registry pointer write path and staleness flag.", input_summary: "After refreshing three cards the reddit.md card is found past its review-by date; two active channels need updated rule-snapshot pointers.", expected_behavior: ["Append rule-snapshot pointer candidates (card + last-verified date) to memory/channels/candidates.md only — channel-registry is the sole writer of memory/channels/.", "Flag the expired reddit.md card as stale and treat it as unverified until re-checked — flagged, not trusted — so C3/C4/C9 citations cannot use it as current.", "Write the profiling log and staleness report to memory/social/platform-norm-profiler/ after asking to save."], failure_modes: ["Edits a memory/channels/ dossier directly instead of appending to candidates.md.", "Keeps citing the expired card as current because the values 'probably have not changed'.", "Buries the registry-grade pointer facts in its own log so channel-registry never sees them."]}
```
