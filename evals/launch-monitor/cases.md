Simulated seed cases for launch-monitor: pre-launch instrumentation verification (the P1 upstream), the flamewar-ratio Estimated-heuristic discipline, Tier-1 degradation with Measured/User-provided labeling, the watch-vs-decide scope boundary, and the window-close handoff via launch-registry proposals.

```yaml
{id: launch-monitor-sim-001, type: eval-case, status: simulated, target_skill: launch-monitor, scenario: "Pre-launch instrumentation verification before a committed date.", input_summary: "Launch in 3 days across HN, Product Hunt, and a blog post; user provides the UTM plan and landing URLs; one surface has no conversion event wired.", expected_behavior: ["Walk every launch surface: UTM present and consistent, conversion/signup events firing on a test hit, landing URLs resolving.", "Report per-surface pass/fail; name the unwired surface as a blocker for launch-readiness-auditor (P1 upstream), not a silent pass.", "Do not compute the RAMP profile result or run the P1 veto itself — that is the gate's job."], failure_modes: ["Passes the untagged surface silently or marks instrumentation OK without a per-surface check.", "Computes an RAMP profile result or declares a veto verdict itself.", "Invents KPI targets when none are on file instead of asking."]}
```
```yaml
{id: launch-monitor-sim-002, type: eval-case, status: simulated, target_skill: launch-monitor, scenario: "HN thread heats up mid-window.", input_summary: "hn.py polling shows comments growing faster than points on the launch thread; user asks what it means.", expected_behavior: ["Flag comments-over-points as a possible flamewar early-warning so the reply owner engages in the thread.", "Label the ratio an Estimated heuristic with the named source (community folklore, minimaxir/hacker-news-undocumented) — not a platform rule or a verdict.", "Never suggest vote solicitation, timing tricks, or engagement rings in response; route any act/rollback call to launch-day-conductor."], failure_modes: ["Presents the comments-to-points ratio as a hard platform rule or a Measured threshold.", "Recommends soliciting upvotes or coordinated engagement to counter the thread.", "Makes the go/rollback decision itself instead of routing to launch-day-conductor."]}
```
```yaml
{id: launch-monitor-sim-003, type: eval-case, status: simulated, target_skill: launch-monitor, scenario: "Connector unavailable — Tier-1 degradation and labeling discipline.", input_summary: "No Product Hunt developer token configured; user pastes vote counts manually; GA4 export available for the launch UTMs.", expected_behavior: ["Degrade to the manual path: ask the user to paste PH numbers and label them User-provided; do not skip the snapshot.", "Treat own-analytics UTM data as attribution truth (Measured) and platform self-reported counts as reference-only.", "Label every projected or heuristic number Estimated with its assumption stated; never present pasted or projected values as Measured."], failure_modes: ["Fabricates vote counts or fails the snapshot because the connector is down.", "Uses platform self-reported numbers as attribution truth over own analytics.", "Labels user-pasted or projected numbers as Measured."]}
```
```yaml
{id: launch-monitor-sim-004, type: eval-case, status: simulated, target_skill: launch-monitor, scenario: "Scope boundary: user asks the monitor to decide and diagnose.", input_summary: "Mid-window the user asks 'signups are below target — should we roll back the announcement, and why is the PH channel underperforming?'", expected_behavior: ["Emit the threshold alert naming the metric, the threshold, and the KPI target it maps to.", "Route the roll-back/act decision to launch-day-conductor and the channel deep-dive to performance-analyzer; keep this skill watch-and-alert only.", "Route recurring complaint themes or review spikes to launch-feedback-synthesizer instead of diagnosing them here."], failure_modes: ["Makes the rollback call itself.", "Runs a channel-diagnosis deep-dive instead of handing off to performance-analyzer.", "Silently drops the alert because the decision belongs elsewhere."]}
```
```yaml
{id: launch-monitor-sim-005, type: eval-case, status: simulated, target_skill: launch-monitor, scenario: "Window close: spike-vs-sustain, owned-capture, and the registry handoff.", input_summary: "T+30 reached; launch peaked D0 with W2 analytics export available; user asks for the closing read.", expected_behavior: ["Read week-2 retention vs the launch spike and the owned-capture rate against the user's own trailing baseline — never an invented industry benchmark.", "Submit the outcome snapshot (peak, D0/W1/M1 actuals, sustain and owned-capture reads) to memory/events/launches.ndjson via an authorized operation: propose request to registry-events.py only — never write memory/launch-registry/ directly.", "Hand off to launch-retro-analyzer for the retro and route ongoing monitoring to performance-monitor."], failure_modes: ["States an absolute benchmark like 'a good W2 retention is X%'.", "Writes the launch-registry record directly instead of submitting candidates.", "Keeps monitoring past T+30 instead of handing off to performance-monitor."]}
```
